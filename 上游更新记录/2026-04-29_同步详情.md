# 2026-04-29 上游同步检查

> **⚠️ 决策反转**：本文记录的是**当日早些时候**的"暂不同步"决策。
> 当天后续讨论复刻基线维护原则时，确认项目定位是"持续追上游最新进展"，
> 因此撤销"暂不同步"决策，已升级两个 submodule。
> **完整升级记录见 [`2026-04-29_submodule升级.md`](./2026-04-29_submodule升级.md)**。

---

**原结论：主仓库无更新，submodules 有更新但暂不同步**

---

## 📌 检查信息

- **检查时间**：2026-04-29
- **主仓库 (Seeed-Projects/reBot-DevArm)**：✅ 无新提交（最新仍是 `c337d44`，已包含在本地）
- **上次同步**：2026-04-26（reBot v1.0 → v1.1）

---

## 📦 Submodules 更新情况

| Submodule | 新提交数 | 状态 | 是否同步 |
|-----------|---------|------|---------|
| `software/MotorBridge` | **50** | 大量更新 | ❌ 暂不同步 |
| `software/reBotArm_control_py` | 0 | 最新 | — |
| `遥操作/StarArm_102` | **4** | 模型更新 | ❌ 暂不同步 |

---

## 🔍 MotorBridge 主要变化

最新提交（5 条）：
- `f7037b0` chore: add linguist-detectable=false for mdx/md files
- `1472242` chore(python): bump motorbridge package version to 0.2.4
- `99ecc65` fix(robstride): improve parameter compatibility and scan defaults
- `a9667da` docs(readme): add motorbridge-studio repo link
- `73c9949` **refactor(repo): extract studio UI out of motorbridge**

**关键变化**：
- 版本从 0.1.8 → **0.2.4**
- **Studio UI 拆分**到独立仓库 `motorbridge-studio`
- RobStride 参数兼容性修复
- 多个 dependabot 安全更新（rand 0.8.6 等）

**新增分支**：`dependabot/cargo/rand-0.8.6`
**新增 tag**：`v0.1.8` → `v0.2.4` 共 7 个版本

## 🔍 StarArm_102 主要变化

最新提交（4 条）：
- `d777473` update: README Joint Limit
- `a8248e5` docs:update README
- `028d737` **update: 更新新的模型**
- `195e105` docs: update README and hardware overview

**关键变化**：模型文件更新 + README 更新

---

## 🚫 暂不同步的原因

### MotorBridge（50 个提交）
1. **改动太大**：包括架构重构（拆分 Studio UI）、版本升级（0.1.8 → 0.2.4）
2. **当前任务无关**：当前在调试电机硬件（用 DMTool），不依赖 MotorBridge 代码
3. **风险高**：Studio UI 拆分可能影响现有依赖关系
4. **等用到再说**：等真正需要写控制代码时再评估升级

### StarArm_102（4 个提交）
1. **当前任务无关**：当前调试达妙电机，遥操作主臂还没用上
2. **新模型可以等**：模型文件更新不紧急，等用到遥操作再同步

---

## ✅ 后续行动

- [x] 记录本次检查结果
- [ ] 当电机调试完成、开始写控制代码时，再评估 MotorBridge 升级
- [ ] 当开始遥操作开发时，再同步 StarArm_102 新模型

---

## 📝 同步命令参考（将来用）

```bash
# 同步 MotorBridge
cd software/MotorBridge
git fetch
git checkout main
git pull origin main
cd ../..
git add software/MotorBridge
git commit -m "更新 MotorBridge submodule 到 v0.2.4"

# 同步 StarArm_102
cd 遥操作/StarArm_102
git fetch
git checkout main
git pull origin main
cd ../..
git add 遥操作/StarArm_102
git commit -m "更新 StarArm_102 submodule（新模型）"
```
