# 2026-05-06 上游同步

**结论：主仓库已同步到 Seeed 最新；ROS2 源码未进入主仓库，而是以独立工作区 submodule 接入。**

---

## 📌 同步信息

- **同步时间**：2026-05-06
- **主仓库上游**：`Seeed-Projects/reBot-DevArm`
- **同步前本地 commit**：`4b3156b`
- **上游最新 commit**：`511e140`
- **本地 merge commit**：`9fe4cf2`
- **新增 ROS2 submodule commit**：`6a41652`

---

## 📦 仓库状态

| 仓库 | 当前本地 | 上游最新 | 结果 |
|------|----------|----------|------|
| `Seeed-Projects/reBot-DevArm` | `4b3156b` | `511e140` | ✅ 已合并 |
| `software/MotorBridge` | `f7037b0` | `f7037b0` | ✅ 无更新 |
| `software/reBotArm_control_py` | `5ae4d56` | `upstream/develop` 有 `5537201 fix G` | ⚠️ 本次未同步，需单独验证 |
| `software/reBotArmController_ROS2` | `d3a415e` | `d3a415e` | ✅ 新增 submodule，origin/upstream 当前一致 |
| `遥操作/StarArm_102` | `d777473` | `d777473` | ✅ 无更新 |

---

## 🔍 Seeed 主仓库变化

上游新增 5 个 commit：

| commit | 说明 |
|--------|------|
| `cee6c94` | Updated ROS2 Integration |
| `d62a154` | Merge branch 'main' into main |
| `f02b7b5` | Merge pull request #23 from EclipseaHime017/main |
| `96f00b1` | Update README.md |
| `511e140` | Update README_zh.md |

实际文件变化只有两个：

```text
README.md    | 4 ++--
README_zh.md | 4 ++--
```

### 具体内容

1. `README.md`
   - `ROS2 (Humble)` 从进行中改为 `ROS2 Integration` 完成
   - 新增 ROS2 Integration Guide 链接
   - Isaac Sim 预计时间从 `2026.04.20` 改为 `2026.06.20`

2. `README_zh.md`
   - `ROS2 (Humble)` 从进行中改为 `ROS2 集成` 完成
   - 新增中文 ROS2 使用教程链接
   - Isaac Sim 仿真延期到 `2026.06.20`

---

## ⚠️ 关键判断：ROS2 没有放进主仓库

本次 `a7d923b..511e140` 的 diff 只包含 `README.md` 和 `README_zh.md`，没有新增：

- `src/`
- `package.xml`
- `CMakeLists.txt`
- ROS2 launch/config
- URDF/mesh 的 ROS2 包目录

所以官方 README 里的“ROS2 完成”不是指 `Seeed-Projects/reBot-DevArm` 主仓库内置了 ROS2 源码，而是指向独立 ROS2 工作区。

---

## ➕ 新增 ROS2 submodule

因为 ROS2 项目体量和职责都足够独立，本次没有复制源码进主仓库，而是按项目规则新增 submodule：

| 项目 | 值 |
|------|----|
| 路径 | `software/reBotArmController_ROS2` |
| submodule URL | `https://github.com/fanhao375/reBotArmController_ROS2.git` |
| branch | `main` |
| 当前 commit | `d3a415e` |
| 个人 fork | `fanhao375/reBotArmController_ROS2` |
| 当前可用上游 | `EclipseaHime017/reBotArmController_ROS2` |

子模块内 remote：

```text
origin   https://github.com/fanhao375/reBotArmController_ROS2.git
upstream https://github.com/EclipseaHime017/reBotArmController_ROS2.git
```

当前 `origin/main...upstream/main` 为 `0 0`，说明 fork 和当前上游一致。

---

## 🚫 Seeed 官方 ROS2 仓库状态

用户浏览器打开了：

```text
https://github.com/Seeed-Projects/reBotArmController_ROS2
```

但 2026-05-06 通过 Git 和 GitHub API 检查：

```text
git ls-remote https://github.com/Seeed-Projects/reBotArmController_ROS2.git
→ Repository not found

GitHub API /repos/Seeed-Projects/reBotArmController_ROS2
→ 404
```

因此当前不把 `Seeed-Projects/reBotArmController_ROS2` 作为 upstream。等它真正公开后，再评估是否把 ROS2 submodule 的上游从 `EclipseaHime017` 切到 `Seeed-Projects`。

---

## ⚠️ 本次未同步的项目

### reBotArm_control_py upstream/develop

检查到 `vectorBH6/reBotArm_control_py` 的 `upstream/develop` 有 1 个新提交：

```text
5537201 fix G
```

涉及文件：

- `example/10_gravity_compensation_lock.py`
- `example/9_gravity_compensation.py`
- `reBotArm_control_py/actuator/arm.py`
- 一个被跟踪的 `__pycache__/arm.cpython-310.pyc`

**本次不同步原因**：

- 这是功能性控制代码，不是 README 级别低风险改动
- 影响重力补偿和 `RobotArm` 执行层，需要按 `复刻基线维护原则.md` 的 submodule 升级流程单独验证
- 上游提交里仍包含 `.pyc`，合入前要判断是否需要清理或保留

**下次触发条件**：

- 准备测试重力补偿前
- 用户明确要求升级 `reBotArm_control_py`
- 建立 `sync/rebotarm-control-py-2026-05-06` 分支并跑最小验证

---

## ✅ 已完成动作

- [x] `git fetch --all --prune`
- [x] 合并 `upstream/main` 到主仓库 main
- [x] 新增 `software/reBotArmController_ROS2` submodule
- [x] 在 ROS2 submodule 内配置 `upstream`
- [x] 更新 `.gitmodules`
- [x] 更新 `项目总览.md`
- [x] 更新 `software/README_zh.md`
- [x] 更新 `复刻基线维护原则.md`
- [x] 更新 `Git操作指南.md`
- [x] 更新 `操作日志.md`

---

## ⚠️ 未完成验证

- [ ] ROS2 环境下 `colcon build`
- [ ] `rclpy` / `motorbridge` / `reBotArm_control_py` 三者同环境 import
- [ ] `bringup.launch.py` / `driver_only.launch.py` 启动验证
- [ ] 真机 ROS2 topic/service/action 调用验证

当前只完成了源码结构和 Git 依赖管理验证；运行验证需要 ROS2 Humble 环境和硬件上下文。

---

## 🔄 回滚方式

```bash
# 回到本次 ROS2 接入和上游同步之前
git checkout 4b3156b
git submodule update --init --recursive

# 或者只取消 ROS2 submodule 接入（需在未 push 前操作）
git revert 6a41652
git submodule update --init --recursive
```
