# 2026-05-13 上游同步

**结论：两批同步——(1) 主仓 `Seeed-Projects/reBot-DevArm` 引入 LICENSE 协议大更新（CC BY-NC-SA 4.0 → CERN-OHL-W 2.0 硬件 + Apache 2.0 软件双协议）+ 关节5线缆约束/电机外壳 STEP 文件修正，直接 merge upstream/main；(2) MotorBridge 升级 v0.2.6 → v0.2.8（9 个 commit 全部 RobStride 路径），fork main 已 push、主仓走 sync 分支已合入；Windows Python 3.12 + WSL2 Python 3.10 两端 Python wheel 升级到 0.2.8。**

---

## 📌 同步信息

- **检查/同步时间**：2026-05-13
- **触发条件**：用户主动询问上游状态 + 看到 MotorBridge 0.2.8 发版后明确同意同步（"那也更新加上吧"）
- **本仓 sync 分支**：`sync/motorbridge-2026-05-13`
- **submodule 指针**：`b9f7065` → `a2b50ed`（MotorBridge）
- **fork main 推送**：`b9f7065..a2b50ed` → `fanhao375/motorbridge:main`（fast-forward，无本地分歧）
- **主仓 upstream merge**：`ae5f123` + `b738018` → main（直接 merge，按 §三 主仓流程）

---

## 📦 各仓库状态

| 仓库 | 当前 commit | 上游最新 commit | 新增提交数 | 是否同步 |
|------|------------|---------------|-----------|---------|
| **`Seeed-Projects/reBot-DevArm`** | merged | `ae5f123` | **2** | ✅ 已同步 |
| **`software/MotorBridge`** | **`a2b50ed`** | `a2b50ed`（upstream/main） | **9** | ✅ 已同步 |
| `software/reBotArmController_ROS2` | `d3a415e` | `d3a415e` | 0 | — |
| `software/reBotArm_control_py` | `062bef9` | `5537201`（upstream/develop） | 0 | — |
| `遥操作/StarArm_102` | `d777473` | `d777473` | 0 | — |

---

## 🔍 主仓上游变化（`Seeed-Projects/reBot-DevArm` 2 个 commit）

### `b738018` The open-source license has been migrated from CC BY-SA-NC to CERN-OHL-W 2.0

⭐ **协议体系重构**——从单一 CC 协议拆成"硬件 + 软件"双协议：

- **硬件设计**：CERN-OHL-W 2.0（[ohwr.org/cern_ohl_w_v2.txt](https://ohwr.org/cern_ohl_w_v2.txt)）
- **固件代码**：Apache 2.0（[apache.org/licenses/LICENSE-2.0](https://www.apache.org/licenses/LICENSE-2.0)）
- **关键影响**：CERN-OHL-W 是"弱互惠"开放硬件协议，**允许商业使用**（之前 CC BY-NC-SA 4.0 明确禁止）

涉及文件：

- `LICENSE` 全量替换（676 → 287 行，重写为 CERN-OHL-W 文本）
- `README.md` / `README_zh.md` / `README_Fr.md` / `README_JP.md` / `README_es.md`：5 个语言版本同步更新协议徽章 + 许可证章节

### `ae5f123` fix Motor_Couver.step

3D 打印件 STEP 文件修复：

- `hardware/reBot_B601_DM/3D_Printed_Parts/01_Joint5_Cable Restraint_A.step`（**新增**，3378 行）
- `hardware/reBot_B601_DM/3D_Printed_Parts/01_Motor_Cover.step`（重大修订，6410 行新版本替换 3459 行旧版）

⚠️ 与本工程**完全无关**——我们用的是完整套装出货零件，不自己 3D 打印。这部分文件可视作"附赠的可二次开发资源"。

---

## 🔍 MotorBridge 上游变化（`b9f7065..a2b50ed`，9 个 commit）

整体改动：10 个文件，+988/-41 行

```text
CHANGELOG.md                             |  17 +
Cargo.lock                               |  20 +-
Cargo.toml                               |   2 +-
bindings/python/pyproject.toml           |   2 +-
bindings/python/setup.py                 |   2 +-
docs/en/testing.md                       |   2 +-
docs/zh/testing.md                       |   2 +-
motor_vendors/robstride/src/motor.rs     |   9 +-
motor_vendors/robstride/src/registers.rs | 103 +-
release_test_notes/0.2.8.md              | 870 +
```

### 9 个 commit 全部属于 RobStride 电机分支

```text
faba15f feat: add RobStride model-specific parameter tables
bd0d2cd fix: keep RobStride runtime table to protocol params
4289a22 fix: complete RobStride runtime parameter handling
40ffc37 feat: add RobStride zero range option
4ca3948 restore: return motorbridge to v0.2.6 tree
c074439 fix: complete RobStride section 4 parameters
0db9ec1 fix: set RobStride zero range during zeroing
93114f7 chore: bump motorbridge release to 0.2.8
a2b50ed docs: annotate RobStride runtime parameter ranges
```

核心改动：

- `motor_vendors/robstride/src/registers.rs`（+103 行）：RobStride 型号专属参数表
- `motor_vendors/robstride/src/motor.rs`（+9 行）：零点范围处理
- 版本号 `0.2.6` → `0.2.8`（Cargo.toml / pyproject.toml / setup.py 三处）
- 新增 `release_test_notes/0.2.8.md`（+870 行）：发版测试报告

---

## 🎯 对本项目影响评估

### 达妙路径

✅ **完全不受影响**——9 个 commit 全在 `motor_vendors/robstride/`，`motor_vendors/damiao/` 一行未改。Python binding 也只动了 `pyproject.toml` 和 `setup.py` 两个版本号字段，实际功能代码（abi.py / core.py / cli.py）零变化。

### 配置兼容性

`reBotArm_control_py/config/arm.yaml` 和 `gripper.yaml`：

- 全部 `vendor: "damiao"`，不走 RobStride 分支
- 即便将来切到 RobStride，新版仅扩展 RobStride 寄存器和零点范围处理，对外 API（`add_robstride_motor` / `robstride_ping_host_id` 等）签名未变

### Python 环境

- 升级前：Windows Python 3.12 + WSL2 Ubuntu Python 3.10 都是 **wheel 0.2.6**
- 升级方式：`pip install -U motorbridge==0.2.8`（PyPI 已发布预编译 wheel）
- 升级后：Windows + WSL2 两端均为 **wheel 0.2.8**，submodule 源码层与 Python 环境层一致

---

## ⚙️ 同步操作

### 1. fork 内合入 upstream（fast-forward）

```text
cd software/MotorBridge
git fetch upstream
git merge --ff-only upstream/main
# Updating b9f7065..a2b50ed → Fast-forward
git push origin main
# fanhao375/motorbridge: b9f7065..a2b50ed main -> main
```

### 2. 主仓走 MotorBridge sync 分支

```text
git checkout -b sync/motorbridge-2026-05-13
git add software/MotorBridge
git commit -F .git_msg.txt
# → 12df10f 升级 MotorBridge submodule 到 a2b50ed（v0.2.6 → v0.2.8）
git checkout main
git merge --no-ff sync/motorbridge-2026-05-13
# → d6ef5be 合入 sync/motorbridge-2026-05-13
```

### 3. 主仓直接 merge upstream（LICENSE + STEP）

按 `复刻基线维护原则.md` §三 规则，**主仓自身**变更不走 sync 分支：

```text
git merge upstream/main --no-edit
# → 02ba6eb Merge remote-tracking branch 'upstream/main'
#   引入 b738018（LICENSE）+ ae5f123（STEP）
```

### 4. Python wheel 双端升级

```text
# Windows
E:\Python312\python.exe -m pip install -U motorbridge==0.2.8
# 0.2.6 → 0.2.8 ✅

# WSL2 Ubuntu
pip3 install -U motorbridge==0.2.8
# 0.2.6 → 0.2.8 ✅
```

---

## ✅ 验证结果

### 静态验证（已完成）

| 项目 | 结果 |
|------|------|
| `motor_vendors/damiao/` 0.2.6 → 0.2.8 改动 | ✅ 零改动 |
| `bindings/python/` 实际代码改动 | ✅ 仅版本号 |
| Windows 端 `import motorbridge` | ✅ wheel 0.2.8 |
| WSL2 端 `import motorbridge` | ✅ wheel 0.2.8 |
| `importlib.metadata.version("motorbridge")` 双端 | ✅ 返回 0.2.8 |

### 运行验证

- [x] Python 包能 import（双端均通过）
- [x] 升级后版本号确认 0.2.8
- [ ] 真机 CAN 通信 / 电机扫描 → 待用户上电（今日计划）

---

## 🛠️ 本次修改文件清单

- `software/MotorBridge`（submodule 指针 `b9f7065` → `a2b50ed`）
- 主仓直接同步：`LICENSE` / `README*.md` / 2 个 STEP 文件
- `上游更新记录/2026-05-13_同步详情.md`（新增本文件）
- `操作日志.md`（追加 2026-05-13 条目）
- `项目总览.md`（更新记录表加一行）
- `复刻基线维护原则.md`（最后更新日期 + 各仓状态）

---

## 🔄 回滚方式

```bash
# 回到本次同步前
git checkout main
git reset --hard baseline-2026-05-09
git submodule update --init --recursive
# fork 端若需回滚远端 MotorBridge：
# git -C software/MotorBridge push --force-with-lease origin b9f7065:main
# 强制推送须用户明确确认（规则 7）
```

---

## 📝 下次检查触发条件

- MotorBridge 再有 v0.2.9 / v0.3.x 发版
- 主仓有功能性（非仅 STEP/LICENSE）改动
- reBotArm_control_py upstream/develop 推进
- 真机调试时若发现 0.2.8 行为异常 → 回滚到 baseline-2026-05-09（MotorBridge 在 v0.2.6）
