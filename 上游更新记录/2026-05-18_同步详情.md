# 2026-05-18 上游同步

**结论：两批同步 —— MotorBridge v0.2.8 → v0.3.2（9 commit，主要 RobStride 协议规范化 + Python CLI 修复 + 脚本重命名，达妙路径基本零改动）+ reBotArmController_ROS2 92ceb76 → 682d12f（4 commit，+853/-241 行 29 文件，含 launch 重命名 + GripperCommand.srv 新增 + safe_home 行为改 + rviz.launch 删）；Python 环境双端升级 wheel 到 0.3.2。**

> 📌 **Subagent 复核**：本次同步前按 memory 规则 spawn 了 general-purpose subagent 独立复核分析，抓到我 2 个初判错误（MotorBridge commit 数 5 → 实际 9 / ROS2 仓"漏合"判断错误），修正后才执行同步。详见 §"Subagent 复核摘要"。

---

## 📌 同步信息

- **检查/同步时间**：2026-05-18
- **触发条件**：用户主动询问"上游最近有更新嘛"
- **本仓 sync 分支**：`sync/upstream-2026-05-18`
- **submodule 指针变化**：
  - MotorBridge：`681f021` → `bcfc3c1`
  - reBotArmController_ROS2：`92ceb76` → `682d12f`
- **fork main 推送**：
  - `fanhao375/motorbridge`: `681f021..bcfc3c1`（fast-forward）
  - `fanhao375/reBotArmController_ROS2`: `92ceb76..682d12f`（fast-forward）

---

## 📦 各仓库状态

| 仓库 | 当前 commit | 上游最新 | 新增提交数 | 是否同步 |
|------|------------|---------|-----------|---------|
| `Seeed-Projects/reBot-DevArm` | merged | merged | 0 | — |
| **`software/MotorBridge`** | **`bcfc3c1`** | `bcfc3c1` | **9** | ✅ 已同步 |
| **`software/reBotArmController_ROS2`** | **`682d12f`** | `682d12f` | **4** | ✅ 已同步 |
| `software/reBotArm_control_py` | `062bef9`（fork develop） | `062bef9`（upstream/develop） | **0** | — develop 无改动 |
| `遥操作/StarArm_102` | `d777473` | `d777473` | 0 | — |

---

## 🔍 MotorBridge 上游变化（`681f021..bcfc3c1`，9 commit）

### Release 时间线

| commit | 含义 |
|---|---|
| `2719b50` ci 调整 + `36c6cd2` LICENSE | 非功能 |
| `dc05608` **release: v0.2.9** | 大改 cli.py（442 行）+ 删 slcan_restart.sh / 新增 canable_restart.sh |
| `b3b35ac` keep python sdist source-only | 打包修复 |
| `10ec02a` **release: v0.3.0** | RobStride 协议规范化（protocol.rs +93 行）+ ws_gateway 重构 + 测试方案 slcan→socketcan |
| `a106434` **release: v0.3.1** | Python CLI timing fix |
| `f2d5794` Harden RobStride mode switching | RobStride 模式切换健壮性 |
| `9908d0f` fix robstride fault report handling | RobStride 故障报告处理 |
| `bcfc3c1` **release: v0.3.2** | 当前最新 |

### 达妙路径影响 ✅ 基本零改动

| 路径 | 真实改动（subagent 实测）|
|---|---|
| `motor_vendors/damiao/src/controller.rs` | **仅 1 行 typo 级**（来自 v0.2.9 全 vendor 统一） |
| `motor_vendors/damiao/` 其他 | 零 |
| `bindings/python/src/motorbridge/core.py` | 仅追加 8 行（非破坏） |
| Python `add_damiao_motor` API | 未变 |
| `Mode` 枚举 | 未变 |

### 重要重命名 ⚠️（旧引用会断）

- ❌ `scripts/slcan_restart.sh` → ✅ `scripts/canable_restart.sh`
- ❌ `tools/reliability/templates/linux_slcan_endurance_*.json` → ✅ `linux_socketcan_endurance_*.json`

**本仓影响**：装机烧录指南目前没引用这些脚本路径，**零影响**。

### v0.3.x 大版本号是为啥

不是 API breaking change，是 RobStride 协议规范化的语义标识。对达妙用户无感。

---

## 🔍 reBotArmController_ROS2 上游变化（`92ceb76..682d12f`，4 commit）

### 文件改动 +853/-241 行 29 文件

### 4 个 commit 详情

| commit | 时间 | 内容 |
|---|---|---|
| `62d1e18` | 2026-05-14 | MoveIt 集成扩展，**重命名 `driver_only.launch.py` → `driver.launch.py`**（72% 相似度） |
| `49b0384` | 2026-05-14 | ⭐ **新增 `rebotarm_msgs/srv/GripperCommand.srv`**；**`safe_home()` 行为改**：不再自动闭合夹爪；`hardware_manager.py` / `ros_actions.py` / `ros_services.py` 大改集成夹爪 |
| `9f83976` | 2026-05-14 | 文档更新 |
| `682d12f` | 2026-05-15 | 删除 `rviz.launch.py` + 删除 `driver_only_no_robot.launch.py` |

### ⚠️ 破坏性变更清单

| 项 | 影响 |
|---|---|
| `driver_only.launch.py` → `driver.launch.py` 重命名 | 引用旧名的脚本/文档会断 |
| `rviz.launch.py` 删除 | 引用此 launch 的脚本会断 |
| `safe_home()` 不再自动闭合夹爪 | 依赖旧行为的代码会异常 |
| 新增 `GripperCommand.srv` | 非破坏（新增） |

### 本仓影响 ✅ 当前装机阶段零影响

本仓**还没消费这些 msg/launch**——目前装机阶段只跑 LeRobot 路径不跑 ROS2。等真要跑 ROS2 工作流时按新接口写脚本即可。

---

## 🎯 Python wheel 双端升级（0.2.8 → 0.3.2）

```text
# Windows
E:\Python312\python.exe -m pip install -U motorbridge==0.3.2
# 0.2.8 → 0.3.2 ✅

# WSL2 Ubuntu
pip install -U motorbridge==0.3.2
# 0.2.8 → 0.3.2 ✅
```

**双端验证**：

```text
Win: importlib.metadata.version('motorbridge') = '0.3.2' ✅
WSL: importlib.metadata.version('motorbridge') = '0.3.2' ✅
```

---

## ⚙️ 同步操作流程

### 1. fork MotorBridge fast-forward + push

```text
cd software/MotorBridge
git checkout main
git merge --ff-only upstream/main
# Updating 681f021..bcfc3c1 → Fast-forward
git push origin main
# fanhao375/motorbridge: 681f021..bcfc3c1
```

### 2. fork reBotArmController_ROS2 fast-forward + push

```text
cd software/reBotArmController_ROS2
git checkout main
git merge --ff-only upstream/main
# Updating 92ceb76..682d12f → Fast-forward
git push origin main
# fanhao375/reBotArmController_ROS2: 92ceb76..682d12f
```

### 3. 主仓 sync 分支

```text
git checkout -b sync/upstream-2026-05-18
git add software/MotorBridge software/reBotArmController_ROS2
git commit -F .git_msg.txt
# → 0a19218 同步 submodule
git checkout main
git merge --no-ff sync/upstream-2026-05-18
# → 23d5a48 合入 sync/upstream-2026-05-18
```

### 4. Python wheel 双端升级（见上）

---

## ✅ 验证结果

### 静态验证（subagent 复核已通过）

| 项目 | 结果 |
|------|------|
| MotorBridge `motor_vendors/damiao/` 改动 | ✅ 1 行 typo 级（业务零影响） |
| MotorBridge `bindings/python/` API | ✅ 仅追加，不破坏 |
| ROS2 仓 4 commit 全部"真新"（非漏合） | ✅ 时间戳 5-14 ~ 5-15 |
| Python wheel 双端 0.3.2 | ✅ |

### 运行验证

- [x] git 操作全程无冲突
- [x] Python wheel `import motorbridge` 双端通过
- [ ] 真机重力补偿 demo（待用户跑 §6.4 验证 0.3.2 跟硬件兼容）
- [ ] ROS2 stack 重新 colcon build（待真要跑 ROS2 时做）

---

## 🛡️ Subagent 复核摘要

按 `feedback_upstream_sync_review.md` memory 规则，本次同步前 spawn 了 general-purpose subagent 独立复核。

**抓到 2 个我的初判错误**：

1. ⚠️ **MotorBridge commit 数错**：我初判 5 个，实际 9 个（漏列 `dc05608` v0.2.9 + `2719b50` / `b3b35ac` / `36c6cd2`）
2. ⚠️ **ROS2 仓 4 个 commit 错判为"漏合"**：实际是 5-14 同步后 upstream 又真新推了 4 个 commit

**复核证实的事实**：

- MotorBridge 达妙路径基本零改动（只 1 行 typo 级）
- v0.3.x major bump 不是 breaking change，是 RobStride 协议规范化
- PyPI 已发 0.3.2（建议升级）
- ROS2 仓 launch 重命名 + safe_home 行为变化是真破坏性变更

**新发现（subagent 独立抓到的，我没说的）**：

- v0.2.9 删 `scripts/slcan_restart.sh` 改 `canable_restart.sh`
- ROS2 仓 `682d12f` 还删除了 `driver_only_no_robot.launch.py`

---

## 🛠️ 本次修改文件清单

- `software/MotorBridge`（submodule 指针 `681f021` → `bcfc3c1`）
- `software/reBotArmController_ROS2`（submodule 指针 `92ceb76` → `682d12f`）
- `上游更新记录/2026-05-18_同步详情.md`（新增本文件）
- `操作日志.md`（追加 2026-05-18 条目）
- `项目总览.md`（最后同步时间 + 记录表加 2026.05.18）
- `复刻基线维护原则.md`（各仓状态更新 + 最后更新日期）
- Python 环境：Win + WSL 双端 wheel 0.3.2

---

## 🔄 回滚方式

```bash
# 回到本次同步前
git checkout main
git reset --hard baseline-2026-05-14
git submodule update --init --recursive

# Python wheel 回滚（如真机问题）
pip install -U motorbridge==0.2.8
```

---

## 📝 下次检查触发条件

- MotorBridge 再发 v0.3.3+ 或新功能版本
- ROS2 仓 launch 改动稳定后回头补"跟 LeRobot 阶段路线对接"
- reBotArm_control_py upstream/develop 推进
- 主仓有功能性变更
