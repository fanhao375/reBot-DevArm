# 2026-05-21 上游同步

**结论：MotorBridge v0.3.2 → v0.3.5（8 commit，达妙路径有 bugfix 但 API 100% 兼容）+ 主仓 1 commit（OSHW 认证 + 社区反馈，无功能影响）+ Python wheel 双端升级 0.3.5。**

> 📌 **Subagent 复核纠正了我的初判**：commit message `96dddf3 feat: support damiao observation parameter stream` 让我误以为是 motor 层达妙改动，实际是 `integrations/ws_gateway/` 层加 WebSocket 流式订阅能力，motor_vendors/damiao/ 没动。真正的达妙改动是别的 commit 里的 bugfix。

---

## 📌 同步信息

- **检查/同步时间**：2026-05-21（周四）
- **触发条件**：用户主动询问上游
- **本仓 sync 分支**：`sync/upstream-2026-05-21`
- **submodule 指针变化**：
  - MotorBridge：`bcfc3c1` → `96dddf3`（v0.3.2 → v0.3.5）
- **fork main 推送**：
  - `fanhao375/motorbridge`: `bcfc3c1..96dddf3`（fast-forward）

---

## 📦 各仓库状态

| 仓库 | 当前 commit | 上游最新 | 新增提交数 | 是否同步 |
|------|------------|---------|-----------|---------|
| **`Seeed-Projects/reBot-DevArm`** | merged | `d6ed5f3` | **1** | ✅ 已同步 |
| **`software/MotorBridge`** | **`96dddf3`** | `96dddf3` | **8** | ✅ 已同步 |
| `software/reBotArmController_ROS2` | `682d12f` | `682d12f` | 0 | — |
| `software/reBotArm_control_py` | `062bef9`（fork develop） | `062bef9`（upstream/develop） | 0 | — develop 无改动 |
| `遥操作/StarArm_102` | `d777473` | `d777473` | 0 | — |

---

## 🔍 主仓变化（1 commit）

### `d6ed5f3` add oshw certification mark and add Feedback from the community

- 新增 `media/certification-mark-CN000024-stacked.png` + `wide.png`（OSHW 开源硬件认证标识）
- 新增 `community/from_Henderson.jpg` / `from_Linyan.png` / `from_Sameer.png`（社区反馈图片）
- README 加 OSHW badge

**对本工程**：无功能影响，纯展示性。

---

## 🔍 MotorBridge v0.3.2 → v0.3.5（8 commit）

### Release 时间线

| commit | 含义 |
|---|---|
| `f7037e6` chore: align package metadata + lint config | 非功能 |
| `9dec7c2` fix: stop controller polling on drop | ⭐ **Drop impl 自动清线程**（之前必须显式 shutdown）|
| `e36cefd` ci: allow websocket handshake error size lint | CI |
| `bc24a7b` **release: prepare v0.3.5** | release |
| `24c891a` fix: expose robstride state sequence | RobStride |
| `70d3f40` Revert "fix: expose robstride state sequence" | revert |
| `503fb15` feat: stream robstride observation parameters | RobStride 流式订阅 |
| `96dddf3` feat: support damiao observation parameter stream | ⭐ **ws_gateway 层达妙流式订阅**（不是 motor 层）|

### 达妙路径真实改动（subagent 实测）

**3 个文件 +115/-13 行**：

| 文件 | 改动 |
|---|---|
| `motor_vendors/damiao/src/motor.rs` | `ensure_control_mode()` 重写——register 10 超时不再阻止写入，写后 3 次重试验证（10ms 间隔）；修复 `write_register_f32/u32` 报错文案颠倒 bug |
| `motor_vendors/damiao/src/controller.rs` | `open_socketcan` → `open_can_bus`（跨平台后端选择器，旧名保留为兼容别名）|
| `motor_abi/src/vendor_params/damiao.rs` | 参数函数签名 `&mut MotorHandle` → `&MotorHandleInner`（ABI 内部 Rust 重构，FFI 函数名/签名未变）|

### API 兼容性

| 项 | 状态 |
|---|---|
| Python `add_damiao_motor()` | ✅ 签名未变 |
| `Motor.send_mit / send_pos_vel / send_vel` | ✅ 未变 |
| `Mode` 枚举 | ✅ 未变 |
| `ensure_mode()` | ✅ 签名未变，**行为增强**（自带重试，鲁棒性提升）|
| `Controller.from_dm_serial / from_can` | ✅ 未变 |
| `set_zero_position / request_feedback` | ✅ 未变 |
| **新增** `motorbridge.__version__` / `get_version()` | 🟢 新功能 |
| 关闭后调用句柄行为 | 🟢 从 null 改为抛 `CallError`（防呆）|

### 利好

- 🟢 新增版本元数据（`motorbridge.__version__`）
- 🟢 `_ptr` 改 `_require_open()` 守卫：关闭后调用抛清晰异常
- 🟢 `CoreController` 加 `Drop` impl：进程退出自动停 polling 线程
- 🟢 ws_gateway 让远端能 WebSocket 订阅达妙参数流（Web UI 增强）
- 🟢 `ensure_mode` 重试逻辑改进——`lerobot-robot-seeed-b601` 里 `num_retry=9` 后续可降到 2-3

### 跨仓影响（lerobot-robot-seeed-b601）

- `pyproject.toml` 没锁 motorbridge 版本（README 让用户手装）→ **不存在版本绑死问题**
- 调用的 API 全部在 v0.3.5 保持兼容
- 升级后**反而有利好**（Drop polling 清理 + ensure_mode 内置重试）

---

## 🎯 Python wheel 双端升级（0.3.2 → 0.3.5）

```text
# Windows
E:\Python312\python.exe -m pip install -U motorbridge==0.3.5  → 0.3.5 ✅

# WSL2
pip install -U motorbridge==0.3.5  → 0.3.5 ✅
```

**双端验证**：
```text
Win: importlib.metadata.version('motorbridge') = '0.3.5' ✅
WSL: importlib.metadata.version('motorbridge') = '0.3.5' ✅
```

---

## ⚙️ 同步操作流程

```text
# 1. fork MotorBridge fast-forward + push
cd software/MotorBridge
git checkout main
git merge --ff-only upstream/main
# Updating bcfc3c1..96dddf3 → Fast-forward
git push origin main

# 2. 主仓 sync 分支
cd ../..
git checkout -b sync/upstream-2026-05-21
git add software/MotorBridge
git commit -F .git_msg.txt
# → 6f5ffc5 同步 submodule

git checkout main
git merge --no-ff sync/upstream-2026-05-21
# → 4f1c2b2

# 3. 主仓 merge upstream/main（OSHW README）
git merge upstream/main --no-edit
# → dced175

# 4. Python wheel 双端升级到 0.3.5
```

---

## ✅ 验证结果

### Subagent 复核 + 静态验证

| 项目 | 结果 |
|------|------|
| MotorBridge 达妙路径真实改动 | ✅ 3 文件 bugfix，无 API 破坏 |
| Python binding `add_damiao_motor` API | ✅ 签名未变 |
| `Mode / ensure_mode / set_zero` 等 | ✅ 全部兼容 |
| Python wheel 双端 0.3.5 | ✅ |
| 跨仓影响（lerobot-robot-seeed-b601）| ✅ 不影响且有利好 |

### 运行验证

- [x] git 操作全程无冲突
- [x] Python wheel `import motorbridge` 双端通过（含新 `__version__`）
- [ ] 真机重力补偿 demo（待用户跑 §6.4 验证 0.3.5 跟硬件兼容，预期无差异）

---

## 🛡️ Subagent 复核摘要

按 `feedback_upstream_sync_review.md` + `feedback_subagent_review_routine.md` memory 规则，本次同步前 spawn 了 general-purpose subagent 独立复核。

**抓到 1 个我的初判错误**：

⚠️ 我说 `96dddf3 feat: support damiao observation parameter stream` 是"首次达妙路径功能改动" → **错了**。
- 实际：该 commit 改的是 `integrations/ws_gateway/` 层（10 个文件），`motor_vendors/damiao/` 和 `motor_abi/` 都没碰
- v0.3.5 真正的达妙路径改动**全是 bugfix**（不是新功能）

**没有 subagent 我会**：
- 误判达妙路径有新功能改动
- 可能给出过度保守的建议（推迟同步、加额外验证步骤）

---

## 🛠️ 本次修改文件清单

- `software/MotorBridge`（submodule 指针 `bcfc3c1` → `96dddf3`）
- 主仓 README + media/community 图片（merge upstream/main）
- `上游更新记录/2026-05-21_同步详情.md`（新增本文件）
- `操作日志.md`（追加 2026-05-21 条目）
- `项目总览.md`（最后同步时间 + 记录表加 2026.05.21）
- `复刻基线维护原则.md`（MotorBridge 当前状态 → v0.3.5 + 最后更新日期）
- Python 环境：Win + WSL 双端 wheel 0.3.5

---

## 🔄 回滚方式

```bash
git checkout main
git reset --hard baseline-2026-05-18
git submodule update --init --recursive
pip install -U motorbridge==0.3.2
```

---

## 📝 下次检查触发条件

- MotorBridge v0.3.6+ 或新功能版本
- reBotArm_control_py upstream/develop 推进（一直没动）
- ROS2 仓有新破坏性变更
- 装机阶段 1 重力补偿跑通后回头补"真机验证 v0.3.5"
