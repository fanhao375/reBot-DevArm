# 2026-05-25 上游同步

**结论：MotorBridge v0.3.5 → v0.3.7（2 个 squash-release commit，达妙路径零改动，全是 RobStride + Windows PCAN + ws_gateway 增强）+ Python wheel 双端升级 0.3.7。**

> 📌 **Subagent 复核**：本次同步前按 memory 规则 spawn subagent 独立复核，跟我初判一致——达妙路径真零改动，scan 改动是独立子命令不影响 follower 适配器。

---

## 📌 同步信息

- **检查/同步时间**：2026-05-25（周一）
- **触发条件**：用户主动询问上游
- **本仓 sync 分支**：`sync/upstream-2026-05-25`
- **submodule 指针变化**：
  - MotorBridge：`96dddf3` → `a6c14d7`（v0.3.5 → v0.3.7）
- **fork main 推送**：
  - `fanhao375/motorbridge`: `96dddf3..a6c14d7`（fast-forward）

---

## 📦 各仓库状态

| 仓库 | 当前 commit | 上游最新 | 新增提交数 | 是否同步 |
|------|------------|---------|-----------|---------|
| `Seeed-Projects/reBot-DevArm` | `d6ed5f3` | `d6ed5f3` | 0 | — |
| **`software/MotorBridge`** | **`a6c14d7`** | `a6c14d7` | **2** | ✅ 已同步 |
| `software/reBotArmController_ROS2` | `682d12f` | `682d12f` | 0 | — |
| `software/reBotArm_control_py` | `062bef9`（develop） | `062bef9`（develop） | 0 | — develop 无改动（main 有 `8427281 add pip` 我们不追） |
| `遥操作/StarArm_102` | `d777473` | `d777473` | 0 | — |

---

## 🔍 MotorBridge v0.3.5 → v0.3.7（2 commit）

只有 2 个 squash-release commit（v0.3.6 + v0.3.7），所有改动打包在两个 release commit 里。

### v0.3.6（`041e27d`，2026-05-21 14:46）

**Fixed**：
- Windows PCAN RobStride 多 controller 抢通道 bug
- 扫描后误报 "controller has no motor"
- ws_gateway RobStride 跳 ID

**Added**：
- Python scan 回归测试（`tests/test_scan.py` +75 行）

**Changed**：
- 三端版本号 0.3.6（Cargo.toml / pyproject.toml / _version.py）

### v0.3.7（`a6c14d7`，2026-05-21 18:04）

**Added**：
- `MOTORBRIDGE_WS_DEBUG` 环境变量
- `capabilities` op + `batch_scan` op
- `scan_progress` 流式事件
- PCAN 错误码 `INITIALIZE`/`ILLHW` 名称映射

**Fixed**：
- RobStride 单 host 扫描实时打印
- ws_gateway request-level channel+model
- Windows PCAN 时序 clamp
- Windows 重复扫描会话释放

**Changed**：三端 0.3.7

---

## 🎯 对项目影响评估

### 达妙路径 ✅ 完全零改动

| 文件 | 改动 |
|---|---|
| `motor_vendors/damiao/` | **0 行**（实测 `git diff --stat 96dddf3..upstream/main -- motor_vendors/damiao/` 空输出）|
| `motor_abi/` | **0 行** |
| `bindings/python/src/motorbridge/core.py` | **0 行** |
| `bindings/python/src/motorbridge/__init__.py` | **0 行** |

### API 兼容性

| 项 | 状态 |
|---|---|
| `Controller.add_damiao_motor()` | ✅ 未变 |
| `Motor.send_mit / send_pos_vel / send_vel` | ✅ 未变 |
| `Mode` 枚举 | ✅ 未变 |
| `ensure_mode()` | ✅ 未变 |
| `set_zero_position` / `request_feedback` | ✅ 未变 |
| WebSocket `{"op":"scan"}` JSON 协议 | ✅ 向后兼容（旧字段未删未改）|

### Web UI 行为

- 达妙用户：**无变化**
- RobStride 用户：扫描有实时进度（增强）
- 新增 `batch_scan` / `capabilities` op 是**纯新增**，不影响现有 op

### 跨仓影响

- `reBotArm_control_py`：4 处 `from motorbridge import` 全是 `Controller, Mode, CallError`，**不调 `motorbridge.cli.scan`** → 零影响
- `lerobot-robot-seeed-b601`：3 个 follower 文件，跟 motorbridge 只在注释里提，**不调 scan** → 零影响

---

## 🎯 Python wheel 双端升级（0.3.5 → 0.3.7）

```text
# Windows
E:\Python312\python.exe -m pip install -U motorbridge==0.3.7
# Downloaded motorbridge-0.3.7-cp312-cp312-win_amd64.whl (950 kB)
# 0.3.5 → 0.3.7 ✅

# WSL2
pip install -U motorbridge==0.3.7
# 0.3.5 → 0.3.7 ✅
```

**双端验证**：
```text
Win: importlib.metadata.version('motorbridge') = '0.3.7' ✅
WSL: importlib.metadata.version('motorbridge') = '0.3.7' ✅
```

---

## ⚙️ 同步操作流程

```text
# 1. fork MotorBridge fast-forward + push
cd software/MotorBridge
git checkout main
git merge --ff-only upstream/main
# Updating 96dddf3..a6c14d7 → Fast-forward
git push origin main

# 2. 主仓 sync 分支
cd ../..
git checkout -b sync/upstream-2026-05-25
git add software/MotorBridge
git commit -F .git_msg.txt
# → c8957b6 同步 submodule
git checkout main
git merge --no-ff sync/upstream-2026-05-25
# → 556aca1

# 3. Python wheel 双端升级到 0.3.7
```

---

## ✅ 验证结果

### Subagent 复核 + 静态验证

| 项目 | 结果 |
|------|------|
| 达妙路径 `motor_vendors/damiao/` 改动 | ✅ 0 行（实测确认）|
| Python `Controller / Motor / Mode` API | ✅ 100% 兼容 |
| WebSocket `{"op":"scan"}` 协议 | ✅ 向后兼容 |
| Python wheel 双端 0.3.7 | ✅ |
| 跨仓零依赖 scan CLI | ✅（reBotArm_control_py + lerobot-robot-seeed-b601）|

### 运行验证

- [x] git 操作全程无冲突
- [x] Python wheel `import motorbridge` 双端通过
- [ ] 真机重力补偿 demo（待用户跑 §6.4 验证 0.3.7 跟硬件兼容，预期无差异）

---

## 🛡️ Subagent 复核摘要

按 `feedback_upstream_sync_review.md` + `feedback_subagent_review_routine.md` memory 规则，本次同步前 spawn subagent 复核。

**这次跟我初判完全一致**——subagent 实测确认：
- 达妙路径真零改动
- scan 改动是独立子命令，对 follower 适配器流程无影响
- ws_gateway 350 行大改全是 RobStride/Windows PCAN 增强
- CHANGELOG 没 1 条达妙相关
- 跨仓零调用关系

**没有 subagent**：仍可能凭"v0.3.6 / v0.3.7 都是 RobStride/Windows 修复"草率判断，subagent 帮助实测验证了"达妙真零改动"这个关键事实。

---

## 🛠️ 本次修改文件清单

- `software/MotorBridge`（submodule 指针 `96dddf3` → `a6c14d7`）
- `上游更新记录/2026-05-25_同步详情.md`（新增本文件）
- `操作日志.md`（追加 2026-05-25 条目）
- `项目总览.md`（最后同步时间 + 记录表加 2026.05.25）
- `复刻基线维护原则.md`（MotorBridge → v0.3.7 + 最后更新日期）
- Python 环境：Win + WSL 双端 wheel 0.3.7

---

## 🔄 回滚方式

```bash
git checkout main
git reset --hard baseline-2026-05-21
git submodule update --init --recursive
pip install -U motorbridge==0.3.5
```

---

## 📝 下次检查触发条件

- MotorBridge v0.3.8+ 或新功能版本
- reBotArm_control_py upstream/develop 推进（一直没动）
- ROS2 仓有新破坏性变更
- 装机阶段 1 重力补偿跑通后回头补"真机验证 v0.3.7"
