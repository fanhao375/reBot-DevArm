# 2026-05-28 上游同步

**结论：MotorBridge v0.3.7 → v0.3.9（2 个 squash-release commit，达妙路径零改动）+ 主仓 4 commit（GEM-4 社区视频 + UVC32 摄像头支架 STEP + 电源 BOM 文档完善）+ Python wheel 双端升级 0.3.9。**

> 📌 **本次同步触发时间是 5-27，subagent 复核也在 5-27 完成，但用户当天没拍板。5-28 当天执行同步。**

---

## 📌 同步信息

- **检查/同步时间**：5-27 检查 + subagent 复核 → 5-28 执行同步
- **触发条件**：用户主动询问上游
- **本仓 sync 分支**：`sync/upstream-2026-05-28`
- **submodule 指针变化**：
  - MotorBridge：`a6c14d7` → `23c2299`（v0.3.7 → v0.3.9）
- **主仓 merge upstream/main**：直接合（按 §三 规则）
- **fork main 推送**：
  - `fanhao375/motorbridge`: `a6c14d7..23c2299`（fast-forward）

---

## 📦 各仓库状态

| 仓库 | 当前 commit | 上游最新 | 新增提交数 | 是否同步 |
|------|------------|---------|-----------|---------|
| **`Seeed-Projects/reBot-DevArm`** | merged | `3d4e282` | **4** | ✅ 已同步 |
| **`software/MotorBridge`** | **`23c2299`** | `23c2299` | **2** | ✅ 已同步 |
| `software/reBotArmController_ROS2` | `682d12f` | `682d12f` | 0 | — |
| `software/reBotArm_control_py` | `062bef9`（develop） | `062bef9`（develop） | 0 | — |
| `遥操作/StarArm_102` | `d777473` | `d777473` | 0 | — |

---

## 🔍 主仓 4 个 commit

| commit | 时间 | 内容 |
|---|---|---|
| `550519f` | 2026-05-25 16:09 | GEM-4 README 加 YouTube 链接 + community/GEM-4.png（1.3MB）|
| `e72a40f` | 2026-05-25 16:12 | GEM-4 follow-up（README 1 行）|
| `c9a7110` | 2026-05-25 21:26 | 5 语言 README 更新 + **新增 `UVC32_mount.step`**（1699 行 STEP，腕部摄像头支架）|
| `3d4e282` | 2026-05-25 22:06 | 5 语言 hardware readme 加"关于固定 / 关于电源"章节，列具体 BOM（24V 14.6A 电源 / 6 寸 G 夹 / 3 插头）|

### ⚠️ 重要纠正

`GEM-4: Gemma Embodied 4 Physical Assistance` **不是 reBot 官方推 Gemma 模型集成**——是别人用 Google Gemma 模型控 reBot Arm 做了 demo 视频，上游把视频链接 + 一张截图加到了 README "Feedback from the community" 表格里。性质等同于"用户晒图"。

### 对项目影响

- ✅ 全是文档 / 资源类增量，零代码改动
- ✅ 零风险
- 🟢 UVC32 摄像头支架可加入未来"装摄像头"阶段考虑（详见 `遥操作与LeRobot待办.md §4 平行支线 1：摄像头`）

---

## 🔍 MotorBridge v0.3.7 → v0.3.9（2 commit）

只有 2 个 squash-release commit，所有改动打包在两个 release commit 里。

### v0.3.8（`bb34133`）

详见 `release_test_notes/0.3.8.md`，全是 RobStride / Windows PCAN 增强。

### v0.3.9（`23c2299`）

详见 `release_test_notes/0.3.9.md`，新增 `bindings/python/examples/robstride_posvel_timing_demo.py` 等。

### 达妙路径 ✅ 完全零改动（subagent 5-27 实测）

| 文件 | 改动 |
|---|---|
| `motor_vendors/damiao/` | **0 行** |
| `motor_abi/` | RobStride 相关纯加法（不破坏达妙） |
| `bindings/python/src/motorbridge/core.py` | 不影响达妙调用 |

### API 兼容性

| 项 | 状态 |
|---|---|
| `Controller.from_dm_serial / from_can` | ✅ 未变 |
| `Motor.send_mit / send_pos_vel / send_vel` | ✅ 未变 |
| `Mode` 枚举（POS_VEL / FORCE_POS / MIT 等）| ✅ 未变 |
| `ensure_mode` / `set_zero_position` / `request_feedback` | ✅ 未变 |
| ⚠️ `request_feedback` RobStride 分支行为变更 | 不影响达妙 |

### 跨仓影响

- `reBotArm_control_py`：调用的 motorbridge API 100% 兼容
- `lerobot-robot-seeed-b601` follower 适配器：100% 兼容

---

## 🎯 Python wheel 双端升级（0.3.7 → 0.3.9）

```text
# Windows
E:\Python312\python.exe -m pip install -U motorbridge==0.3.9  → 0.3.9 ✅

# WSL2
pip install -U motorbridge==0.3.9  → 0.3.9 ✅
```

**双端验证**：

```text
Win: importlib.metadata.version('motorbridge') = '0.3.9' ✅
WSL: importlib.metadata.version('motorbridge') = '0.3.9' ✅
```

---

## ⚙️ 同步操作流程

```text
# 1. fork MotorBridge fast-forward + push
cd software/MotorBridge
git checkout main
git merge --ff-only upstream/main
git push origin main
# Updating a6c14d7..23c2299 → Fast-forward

# 2. 主仓 sync 分支
cd ../..
git checkout -b sync/upstream-2026-05-28
git add software/MotorBridge
git commit -F .git_msg.txt
# → e150c82 同步 submodule

git checkout main
git merge --no-ff sync/upstream-2026-05-28
# → 50fb1ca

# 3. 主仓 merge upstream/main（GEM-4 + UVC32 + 电源）
git merge upstream/main --no-edit
# → 50af470

# 4. Python wheel 双端升级到 0.3.9
```

---

## ✅ 验证结果

### Subagent 复核（5-27 完成）

| 项目 | 结果 |
|------|------|
| 达妙路径 `motor_vendors/damiao/` 改动 | ✅ 0 行 |
| Python API 兼容性 | ✅ 100% |
| WebSocket `{"op":"scan"}` 协议 | ✅ 向后兼容 |
| Python wheel 双端 0.3.9 | ✅ |
| 跨仓 follower 适配器影响 | ✅ 0 影响 |

### 运行验证

- [x] git 操作全程无冲突
- [x] Python wheel `import motorbridge` 双端通过
- [ ] 真机重力补偿 demo（待用户跑 §6.4 验证 0.3.9 跟硬件兼容）

---

## 🛡️ Subagent 复核摘要

按 `feedback_upstream_sync_review.md` + `feedback_subagent_review_routine.md` memory 规则，本次同步前（5-27）已 spawn subagent 独立复核。

**复核确认事实**：
- 达妙路径 0 行改动
- API 100% 兼容
- 跨仓零调用影响
- 主仓 4 commit 是文档 / 资源类，零风险
- 主仓 GEM-4 不是新功能，是社区视频链接

---

## 🛠️ 本次修改文件清单

- `software/MotorBridge`（submodule 指针 `a6c14d7` → `23c2299`）
- 主仓直接同步：`community/GEM-4.png` + `hardware/reBot_B601_DM/UVC32_mount.step` + 5 语言 README + 5 语言 hardware readme
- `上游更新记录/2026-05-28_同步详情.md`（新增本文件）
- `操作日志.md`（追加 2026-05-28 条目）
- `项目总览.md`（最后同步时间 + 记录表加 2026.05.28）
- `复刻基线维护原则.md`（MotorBridge → v0.3.9 + 最后更新日期）
- Python 环境：Win + WSL 双端 wheel 0.3.9

---

## 🔄 回滚方式

```bash
git checkout main
git reset --hard baseline-2026-05-25
git submodule update --init --recursive
pip install -U motorbridge==0.3.7
```

---

## 📝 下次检查触发条件

- MotorBridge v0.3.10+ 或新功能版本
- 主仓功能性改动（非 README/STEP/社区图片类）
- reBotArm_control_py upstream/develop 推进（一直没动）
- ROS2 仓有新破坏性变更
- 装机阶段 1 重力补偿跑通后回头补"真机验证 v0.3.9"
