# 2026-06-07 上游同步检查

**结论：三仓全部同步到上游最新（主仓 squash + MotorBridge v0.4.5 + ROS2 Humble 修复），Python wheel Windows/WSL 系统端升 0.4.5，LeRobot 环境待用户自行升级。**

---

## 📌 检查信息

- 检查时间：2026-06-07
- 复核方式：⭐ spawn 独立 subagent 复核三仓影响（按 memory 规则）

## 📦 各仓库状态

| 仓库 | 当前 commit（同步前） | 上游最新 commit | 新增提交数 | 是否同步 |
|------|------------|---------------|-----------|---------|
| Seeed/reBot-DevArm | 基线 `6f7b33f` | `5606170` | 11 | ✅（squash） |
| MotorBridge | `23c2299` v0.3.9 | `73443c7` v0.4.5 | 27 | ✅（sync 分支） |
| reBotArm_control_py (develop) | `062bef9` | `062bef9` | 0 | — |
| reBotArmController_ROS2 | `682d12f` | `c6a4e68` | 5 | ✅（sync 分支） |
| StarArm_102 | `d777473` | `d777473` | 0 | —（⚠️ 102 装配教程依旧未发布） |

## 🔍 主要变化

### 主仓（11 commit，squash 同步为 `219c052`）
- README.md 新增 "Contributions from Developers" 社区贡献表（@danieldoradotalaveron-rb 的 ROS2 monitor/park/gravity-comp）+ 社区反馈图行
- `community/` 新增 4 张社区图（约 3.7MB）
- ⭐ `0fd962e`：**DM-power-Top Cover.STEP + Sliding Cover.STEP 修正**——XT60E 母座位置内移，方便装螺母。**打印电源外壳必须用此版**
- 无删除文件、未碰本地 readme 注记（实拍图未上传的 blockquote 完好）

### MotorBridge v0.3.9 → v0.4.5（27 commit，指针 commit `312dab7`）
- ⭐ **达妙路径增强**（v0.4.5 `f4adda0`）：`ensure_control_mode()` 模式写入重试（写→校验失败→**重写**→再校验；重试间隔 10ms→20ms；失败携带 expected/got 诊断）+ 新增 `request_fresh_state(timeout)` 要求时间戳新鲜的反馈帧（state 内部带 Instant 时间戳）。**纯增强，无既有行为变化**
- **dm-device runtime**（v0.4.3 引入）：达妙官方 USB-CAN 适配器（usb2canfd-dual / DM-LinkX-4C）的 SDK 预编译库，作为**第四种可选传输后端**。✅ 复核结论：**非强制依赖**——仅显式调用 `from_dm_device` 才触发；`auto` 对达妙只推断 dm-serial；pip wheel 不打包该库；现有 SocketCAN/dm-serial 路径完全不受影响
- v0.4.4：实验性 RobStride CiA402/MIT vendor（grep 确认零触碰 damiao）
- Python API **100% 向后兼容**（纯新增 `from_dm_device` / `abi_version` / `abi_capabilities`）
- 注：Python 绑定从来没有 `Controller.scan` 方法（scan 是 CLI 命令），验证时勿误判为 API 被删

### reBotArmController_ROS2（5 commit，指针 commit `e3d1992`）
- MoveIt2 **Humble 兼容修复**（解决 Humble vs Jazzy 不兼容）
- `RobotArm.fresh()` 用法修正（过滤 gripper_ 关节，对应 motorbridge 新增 `request_fresh_state`）
- 本仓暂未跑 ROS2，零消费影响

## ✅ 同步操作记录

- 走 `sync/upstream-2026-06-07` 分支，验证通过后 ff 合入 main，打 `baseline-2026-06-07` tag
- 主仓沿用 **squash 单亲提交**（⚠️ 永久性约束：上游历史含 94MB 实拍图 blob，**永远不能普通 merge upstream/main**，否则 blob 变为可推送对象）
- 两个 submodule 的 fork main 均已 fast-forward push（motorbridge `23c2299..73443c7`、reBotArmController_ROS2 `682d12f..c6a4e68`）
- **验证结果**：
  - [x] MotorBridge：Windows `E:\Python312` pip 装 0.4.5 + import + Controller/Mode/add_damiao_motor/from_dm_device/abi_version 全通过
  - [x] MotorBridge：WSL 系统 python3 升 0.4.5 + import 验证通过
  - [x] ROS2：三包结构（bringup/msgs/controller）+ 元数据完整；**已完成结构验证，运行验证待 ROS2 环境**
  - [x] 主仓：subagent diff 复核，无删除/无冲突
  - [ ] ⚠️ **硬件未验证**（真机 CAN 通信/重力补偿待下次上电时确认 0.4.5 正常）
  - [ ] ⚠️ **LeRobot 环境的 motorbridge 未升级**——已查实该 conda 环境（miniforge3/lerobot）**在另一台电脑上**（本机 WSL 无任何 conda 且无 LeRobot 校准缓存）。在另一台电脑：`git pull` + `git submodule update` 后，`conda activate lerobot && pip install -U motorbridge==0.4.5`
- **回滚点**：`baseline-2026-05-31` tag；wheel 回滚 `pip install motorbridge==0.3.9`

## 📝 下次检查触发条件

- 真机上电跑重力补偿验证 0.4.5（最优先）
- StarArm_102 上游 `Hardware/assembly/` 发布 102 装配教程（持续盯）
- 上游有新 release / 每周例行检查
