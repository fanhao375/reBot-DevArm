# 2026-06-13 上游同步

**结论：本日两次同步。① 上午 MotorBridge v0.4.5 → v0.4.6（低风险纯增量，达妙路径零影响，wheel 已升）。② 当日晚些 StarArm_102 d777473 → e8b871f（4 commit 新增 hover/button/mode 控制脚本，全量 ff 镜像）。主仓 Seeed / control_py / ROS2 无新。**

> ⚠️ **订正**：本文件最初写"其余 4 仓均无新"——那是上午第一次检查时的状态。当日晚些再查发现 **StarArm_102 上游推了 4 个新 commit**，见下方"补充：StarArm_102 同步"段。

---

## 📌 检查信息

- 同步时间：2026-06-13
- 起因：用户先把另一台机器推的遥操作进展（origin 领先本地 5 commit）ff 拉到本地对齐，再例行检查上游。
- 复核方式：⭐ spawn 独立 subagent 复核 MotorBridge v0.4.6 达妙影响（按 memory 规则），确认低风险。
- sync 分支：`sync/upstream-2026-06-13`，baseline tag：`baseline-2026-06-13`。

## 📦 各仓库状态

| 仓库 | 同步前 | 上游 tip | 新增 | 是否同步 |
|------|--------|---------|------|---------|
| Seeed/reBot-DevArm | `1cf0ec9` | `1cf0ec9` | 0 | —（无新；squash 法的重列假象已排除） |
| **MotorBridge** | `53fe6dd` v0.4.5 | `38b8a56` v0.4.6 | 4 | ✅（fork main ff） |
| reBotArm_control_py (develop) | `0163c08` | develop `5537201` | 0 | —（develop 无新；main `893f095` 也无新） |
| reBotArmController_ROS2 | `c6a4e68` | `c6a4e68` | 0 | —（无新） |
| **StarArm_102** | `d777473` | `e8b871f` | 4 | ✅（当日晚些发现，全量 ff，见补充段） |

## 🔍 MotorBridge v0.4.5 → v0.4.6（4 commit，指针 commit `129eee0`）

⭐ subagent 复核结论：**低风险纯增量，对 DM4340P×3 / DM4310×4 现有路径零影响**。

- `b4a839a feat: add Damiao 4340_v20 alias with 4340X display name`：
  - 达妙型号表 `DAMIAO_MODELS` **新增一条** `{model:"4340_v20", pmax:12.5, vmax:20.0, tmax:28.0}`（DM4340 高转速变种，display 名 `4340X`）+ 新增纯函数 `display_model_name`/`display_models`。
  - ⭐ **现有 4340/4340P/4310 逐字节未动**；`match_models_by_limits`/`model_limits` 函数体未改。
  - ⭐ **按 limits 反查无歧义**：4340_v20 的 vmax=20 vs 4340P 的 vmax=10，差值 10 远大于调用方 tol(~0.01)，不会把 4340P 误命中成 4340_v20；upstream 回归测试 `model_limits_and_matching_work` 仍只命中 4340+4340P。
  - `display_models` 仅被 CLI/ws scan 的**输出格式化**调用，不进电机控制路径。
- `13c6414 fix(ws_gateway): accept browser token in websocket query`：浏览器可在 WebSocket URL `?motorbridge_ws_token=` 传 token（header 之外多一路）。本机 **loopback 无 token 用法零影响**（鉴权分支只在设了 `MOTORBRIDGE_WS_TOKEN` 时触发），不碰达妙控制路径。
- `bb4e0c5` merge（无独立代码）/ `38b8a56` release v0.4.6 版本 bump。
- **Python API 100% 向后兼容**（`add_damiao_motor`/`Controller`/`Mode` 签名未变；`bindings/` 只改 version 行 + `cli/damiao.py` 加一行 4340_v20 字典项）。

## ✅ 同步操作记录

- MotorBridge fork main fast-forward `53fe6dd → 38b8a56`（待 push origin/main）。
- 走 `sync/upstream-2026-06-13` 分支，提交 submodule 指针 `129eee0` + docs commit，ff 合入 main，打 `baseline-2026-06-13` tag。
- **wheel 升级 0.4.5 → 0.4.6**：
  - [x] Windows `E:\Python312`：pip 升 0.4.6 + import + `__version__==0.4.6` + `add_damiao_motor` 验证通过
  - [x] WSL 系统 python3：pip 升 0.4.6 + `__version__==0.4.6` 验证通过
  - [ ] ⚠️ **LeRobot 环境（另一台机 `/home/pc/rebot_lerobot`，当前 0.4.5）待自升**——用户正用 0.4.5 调遥操作，建议**遥操作调稳后**再 `pip install -U motorbridge==0.4.6`（4340_v20 对其无影响，纯版本对齐）。
- ⚠️ 硬件未验证（达妙真机 CAN/重力补偿待下次上电；v0.4.6 仅新增型号别名，风险极低）。
- **回滚点**：`baseline-2026-06-11` tag；wheel 回滚 `pip install motorbridge==0.4.5`。

---

## 补充：StarArm_102 同步（当日晚些 `d777473 → e8b871f`，4 commit，指针 `97ecaec`）

当日晚些再查上游，发现 **StarArm_102（102 主臂 SDK）上游推了 4 个新 commit**。⭐ spawn 独立 subagent 复核，**全量 ff 镜像 upstream**。

- `98b4d62 加入新的力控` → 新增 `Python_SDK/stararm102_ro_force.py`
- `1d59a0a 角度控制` → 改 force 脚本
- `edfc6ee 修改控制模式` → force 改名 `stararm102_ro_hover.py` + 新增 `stararm102_ro_button.py` / `stararm102_ro_mode.py`
- `e8b871f 按键控制模式` → 精简 button 脚本

**价值（参考用，非开箱即用）**：
- `stararm102_ro_hover.py` 悬停：⚠️ **"角度静止检测(连续 1 秒 err≤阈值) + 位置锁 + 推动(err>5)切 unlocked(0x10)"近似**，**非真力矩**；真正想做的"读 current 电流估外力(err_current>700)"路径**整段被注释**，WIP。→ 回答"102 能不能漂浮"：硬件限制没变，这是位置锁近似，但有现成状态机可试。
- `stararm102_ro_button.py` 按键控制（板载按键舵机 id7，按到 180° 切阻尼 0x11）。
- `stararm102_ro_mode.py` 模式切换（hover + pinocchio 重力补偿 + 过温>65℃切 0x12）；**依赖 pinocchio/numpy 且 RNEA `get_power` 被注释、硬编码 power 顶替，不能直接跑**。
- `stararm102_ro.py` 改进：夹爪末端 `*1.5` 加 `max(0,min(...,90))` 0~90° 限幅；servo_ids/关节映射不变。

**复核要点（subagent 纠正我 1 处误判）**：
- ❌ 我初判"6MB STL 垃圾" → ✅ 实际根目录 `stararm102_description/` 的 STL 与现有 `ROS2_HUMBLE/src/stararm102_description/` **blob SHA 逐个相同，git 去重**；全量 ff 对象层面**仅新增 ~248KB**（output.txt 147KB + rosbag 52KB + 脚本 ~25KB）。
- 无删除/无破坏（唯一 R 是范围内 force→hover 改名）；对用户当前 `tools/lerobot_debug/arm102_to_b601_direct_follow.py` 遥操作工作流**零影响**（不走此 SDK）。
- ⚠️ 上游误提交垃圾（output.txt tree dump / rosbag / .vscode / 根目录重复 description / `gazebo_demo.launch_.py`）**留在 fork 历史，不进主仓可推送历史**（主仓只存指针 SHA）。
- 推荐 **(a) 全量 ff 镜像**（保持 fork=upstream 纯镜像，将来同步省事）而非 cherry-pick（会让 fork 永久分叉）。

走 `sync/upstream-2026-06-13b` 分支，ff 合入 main，打 `baseline-2026-06-13b` tag。StarArm_102 fork main ff `d777473→e8b871f`（待 push）。回滚点 `baseline-2026-06-13`。

## 📝 下次检查触发条件

- 遥操作调稳后把 LeRobot 环境 wheel 升 0.4.6
- StarArm_102 发布 102 装配教程（持续盯）；hover/mode 脚本将来做 102 力反馈/重力补偿可参考
- MotorBridge / 主仓有新 release / 每周例行检查
