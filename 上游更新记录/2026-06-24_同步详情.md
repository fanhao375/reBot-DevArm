# 2026-06-24 上游同步详情

> 例行上游检查。**5 仓里 3 仓有新**：主仓（DM 供电件装配更新）+ MotorBridge（v0.4.8）→ 已同步；control_py（develop 被强制重写并合入破坏性重构）→ 评估**不合**；ROS2 已最新、StarArm_102 无新。按 memory 规则 spawn 2 个独立 subagent 复核 control_py + MotorBridge。

## 0. 检查总览

| 仓 | 我们指针 | 上游 tip | 新增 | 结论 |
|---|---|---|---|---|
| **主仓 Seeed** | `0522186`(06-21同步点) | `67d233c` | 3（实质 `de86d01`） | 🟢 已同步（squash 单亲） |
| **MotorBridge** | `2b7b350` v0.4.7 | `123a6ad` v0.4.8 | 4 | 🟢 已同步（fork main ff） |
| **reBotArm_control_py** | `0163c08` develop | develop 强制重写→`8cf1a44` | 已分叉 | ❌ 评估不合，维持 `0163c08` |
| reBotArmController_ROS2 | `a61efe4` | `a61efe4` | 0 | ✅ 已是最新，无需动 |
| StarArm_102 | `e61f34e` | `e61f34e` | 0 | ✅ 无新 |

baseline tag：`baseline-2026-06-24`，sync 分支：`sync/upstream-2026-06-24`。

---

## 1. 主仓 Seeed `0522186..67d233c`（实质 commit `de86d01`）

实质内容 = `de86d01 Update the DM power supply assembly steps and print files, and correct errors in the RS power supply section.`（另两个 `319868a`/`67d233c` 是 merge 气泡）。⚠️ 注意 `0522186..upstream/main` 会重列我们已 squash 过的 06-18 内容（squash 重列假象），真实新增锚点是 06-21 同步点 `0522186`。

**de86d01 改了什么（与用户当前 DM 装机直接相关）：**
- **DM 供电 3D 打印件格式迁移**：删 3 个 `.STEP`（前壳/后壳/滑盖），新增 3 个 `.stp`（文本格式，~50-200KB/个）。
- **DM 打印件预览图** 3 张更新（`3D_Printed_Parts/images/`，已 track，取上游版；最大 `DM-power-Top Cover-Sliding Cover.png` 682KB→2.28MB）。
- **DM readme.md / readme_zh.md**：上游修正装配步骤——图引用 `.jpg`→`.png`、螺丝规格改 304 不锈钢十字螺丝 + 加六角螺母 M3×2.5、打印件链接指向 `.stp`、章节"总体完成"等措辞调整。
- **RS README.md**：修正 RS 电源章节螺丝 BOM 错误（304 不锈钢，数量更正）。

**同步执行（squash 单亲，遵 2026-05-31 永久约束）：**
1. `git checkout -b sync/upstream-2026-06-24` → `git merge --squash upstream/main` → 7 冲突（3 预览图 add/add + 4 README content）。
2. **3 张预览图** → 取上游（`--theirs`），它们是已 track 的打印件缩略图，非 powerstep 大图。
3. **4 个 README** → 取上游全文（`--theirs`，吸收 jpg→png/螺丝修正/.stp 链接），再把本 fork「powerstep 大图本地化」警告块**重新插回**（DM 措辞 "STEP 打印件"→"STP" 以匹配新文件）。RS README_zh 上游未改，插回警告块后 = 原 HEAD，无净变化（不进本次提交）。
4. **撤出 powerstep 大图**（`git restore --staged`，gitignore，磁盘保留）：
   - DM `Assembly_Steps/powerstep_images/`（~94MB，20 张 png）→ 撤出
   - RS `Assembly_Steps/powerstep_images/`（~42MB，squash 重列再次拽进）→ 撤出
5. **跟随上游删旧 `.STEP`**：merge 因 merge-base 不含旧 STEP 而判为"我方新增"未暂存删除，手动 `git rm` 3 个旧 `.STEP`（readme 已只指 `.stp`，旧 STEP 成死文件）。

**安全验证：**
```
git rev-list --objects origin/main..HEAD | grep powerstep  → 0 个对象
最大 blob: 2.28MB (DM-power-Top Cover-Sliding Cover.png 打印件预览图)
```
✅ DM 94MB + RS 42MB powerstep 实拍图均未进可推送历史，本地磁盘保留。提交 `fdcdc10`。

---

## 2. MotorBridge v0.4.7 → v0.4.8（4 commit，指针 `123a6ad`）

⭐ spawn 独立 subagent 逐字节复核（按 memory 规则），**对达妙 DM 路径零实质影响，风险低**。

- `19e81a7 ci/rustfmt`：纯 CI（ABI 发布改仅 tag 触发）+ rustfmt 格式化，`damiao/src/motor.rs` 82 行改动全是换行/括号重排，无逻辑变化。
- `43c341a fix(ws-gateway): scope single-motor commands to explicit targets`：单电机命令改为只寻址目标电机（enable/disable/clear_error/set_zero 等）。**仅 ws_gateway 层**——我们 DM 走 native API（`from motorbridge import Controller`；`ctrl.enable_all()`），不走 ws_gateway，零影响。
- `29305d5 feat: add RS motor status parse/display and align DM naming`：⚠️ "align DM naming" **实为给 RobStride 状态字段标签对齐 DM 命名习惯，不是给达妙电机改名**。只动 robstride/ws_gateway 共 3 文件，0 行 damiao 代码。`DAMIAO_MODELS` 表、`model_limits`、`add_damiao_motor` 逐字节未动。
- `123a6ad release: prepare v0.4.8`：版本 bump。
- **Python API 100% 向后兼容**：`bindings/python/src/` 整个 range 只改 `_version.py` 一行。

**执行**：fork 本地 `main` ff `38b8a56→123a6ad`（origin/main 原在 2b7b350），主仓 submodule 指针 `2b7b350→123a6ad`，提交 `a41410d`。
- ⏳ **fork origin/main 待 push**（ff 到 123a6ad）。
- ⏳ **wheel 升级 0.4.8 待用户跑**（对达妙无收益，纯追新；建议升级后做最小冒烟：上电→enable_all→MIT 动一两轴→确认重力补偿手感→disable_all）。

---

## 3. ⚠️ reBotArm_control_py：develop 被上游强制重写，破坏性重构合进 develop —— 评估**不合**

⭐ spawn 独立 subagent 深审（git log/diff/rev-list/cat-file 实证）。

**背景**：上游 `develop` 被 force-update `5537201→8cf1a44`，我们 pin `0163c08` 已**分叉**（merge-base 是很老的 `7a143f4`）。2026-06-13 评估过的破坏性 API 重构（当时决定"等合进 develop 再整体评估"），**现在已合进 develop**，且比当年评估更彻底。

**复核结论（6 项实证）：**
1. **重构已进 develop 且更彻底**：不只 `RobotArm→RebotArm` 改名，还引入 **JointGroup 容器架构**（`arm.mit()`→`arm.send_mit()`，调用语义级破坏，非 sed 改名能兼容）；删 `arm.py`/`arm.yaml`，新增 `rebotarm_dm.yaml`/`rebotarm_rs.yaml`/`rebotarm.yaml`；所有 example 重写。
2. **对象体积**：`0163c08..8cf1a44` 新增 blob **64MB**，其中 **63MB 是 30 个 RS STL**（`urdf/00-arm-rs_asm-v3/meshes/`，达妙用不到）+ 52 个 `.pyc`。进 git 历史后永久存在。
3. **改用户已验证手感**：重力补偿例程**运行时** kp `0→2.0`（`9_gravity_compensation.py`，会变硬/有回中力，不同于已验证的 kp=0 软拖动）；DM URDF 末端 `mass 0.55→0.45kg`（影响重力补偿 tau）。注：per-joint MIT kp（yaml）未变。
4. **本地 cherry-pick 已冗余**：idx_q(`6a4afd8`)、joint3(`2828b64`) 原版已在 `8cf1a44` 历史里，同步反而冲突。
5. **DM URDF**：joint3 已正确、mesh 用相对 `meshes/`，`8cf1a44 fix dm urdf` 实质仅 1 行 mass 改动。
6. **当前遥操作不依赖此 API**：主仓遥操作脚本走 LeRobot 的 `RebotArm102Leader` 插件，不 import control_py 的 `RobotArm`/`RebotArm`/actuator → API 重写**不会让遥操作跑不起来**。

**决策：维持 `0163c08` 不动，暂不合（选项 C）。** DM 侧真实净收益仅 "末端 mass 0.55→0.45 一行"，却要吞 63MB RS STL + 破坏性架构重写 + 改已验证手感，不划算。将来若真要那行 mass，单独 cherry-pick 一行即可（且 0.45 vs 0.55 哪个准需硬件实测）；若将来 follower 改为直接调 control_py 的 `RebotArm` 而非 LeRobot 插件，再整体评估全量迁移。

---

## 4. ROS2 / StarArm_102

- **reBotArmController_ROS2**：upstream/main `c6a4e68→a61efe4`，但我们 submodule 指针**已是 `a61efe4`**（`merge-base --is-ancestor upstream/main HEAD` = YES），已含最新，**无需动**。
- **StarArm_102**：fetch 无输出，`e61f34e` = 上游 tip，**无新**。

---

## 5. 收尾

- 合并 `sync/upstream-2026-06-24` → `main`（fast-forward）
- 打 tag `baseline-2026-06-24`
- ⏳ **待用户确认 push**：origin/main + tag + MotorBridge fork origin/main（ff 123a6ad）
- control_py / ROS2 / StarArm_102 三 submodule 指针不变

## 📝 下次检查触发条件

- control_py：若将来打算把 DM follower 改成直接调 `RebotArm` API（弃 LeRobot 插件），需整体评估全量迁移到新 develop 架构。
- MotorBridge：wheel 0.4.8 升级（用户跑，纯追新无收益）。
- StarArm_102 发布 102 装配教程（持续盯）。
- 主仓 / 每周例行。
