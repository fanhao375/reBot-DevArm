# 2026-06-27 上游同步详情

例行检查 5 仓 upstream。**仅主仓 1 条纯文档新提交，已同步；其余 4 仓 0 落后；control_py develop（RS 重构线）维持不追。**

## 检查结果总览

| 仓 | pin | upstream 状态 | 处理 |
|---|---|---|---|
| 主仓 reBot-DevArm | 67d233c(06-24) | **+1** `654ebdc` | ✅ 已同步 |
| MotorBridge | 123a6ad | main +0 | 无需动（v0.4.8 最新） |
| ROS2 | a61efe4 | main +0 | 无需动 |
| StarArm_102 | e61f34e | main +0 | 无需动 |
| control_py | 0163c08 | **develop-dm +0** / develop +17 / main +26 | ✅ DM 线最新；develop(RS 线)不追 |

## ① 主仓 `654ebdc add:optional hardware`（已同步）

- **内容**：仅 5 个多语言 README（README.md / _zh / _Fr / _JP / _es，+409 行）新增「推荐可选硬件」章节——相机（Orbbec Gemini 2 / 336 / 335LG、Intel RealSense D435i / D405、Sensing GMSL2 单目）、夹爪、StarArm 等，附 Seeed 商城链接。**正好列了我们在用的 Gemini 2。**
- **安全性**：纯 README 文本，无大文件（最大 blob 33KB），引用的硬件图（UVC_camera_mount.png / Gemini2.jpg / D435i.jpg…）仓内均已存在，不图裂；654ebdc 未新增任何图片/STEP/STL。
- **合入方式**：遵「主仓不可普通 merge upstream」约束，**只取 `67d233c..654ebdc` 的 5 个 README delta 打补丁**（`git apply`），单亲提交 `4d730a8`，**规避全树 `merge --squash` 把 gitignore 的 powerstep 大图（DM 94MB / RS 42MB）拽回暂存的老坑**。补丁可干净应用（top-level README 在 06-24 已取上游全文，无分叉）。

## ② control_py：develop +17 / main +26 ——**维持 `0163c08` 不追**（与 06-13/06-24 决策一致）

- **`develop-dm` 0 落后** = 我们的 DM pin 正是最新 DM 线。
- `develop` 领先 17 条（`8cf1a44 add EN and fix dm urdf`、`80be71a Merge ... develop-rs`、`6325a6d rs and dm fine`、`a64906c update controller (mit mode with G)` 等）= **RS 线 + EN 多语言 + 06-24 已评估为破坏性的 API 重构**（`RobotArm→RebotArm`+JointGroup、+63MB RS STL、重力补偿 kp 0→2.0）。我们遥操作走 LeRobot `RebotArm102Leader` 插件、不依赖此 API；DM 净收益极小，不值破坏性重写。**不追**，跟 develop-dm。

## ③ MotorBridge / ROS2 / StarArm_102

三仓 upstream/main 均 0 落后，无需动。MotorBridge 仍 v0.4.8（fork main `123a6ad`），wheel 升级仍待用户（纯追新无收益）。

## 收尾

主仓提交 `4d730a8`，打 `baseline-2026-06-27`，push origin/main + tag。4 个 submodule 指针无变化。
