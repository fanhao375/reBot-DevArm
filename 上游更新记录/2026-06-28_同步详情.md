# 2026-06-28 上游检查与同步详情

> 例行检查 5 仓 upstream。**仅 StarArm_102 有 2 条纯文档更新已同步**;主仓/MotorBridge/ROS2 经复核 0 落后;control_py 上游大改但维持 hold。
> 本次检查/同步前按规则 **spawn 独立 subagent 逐仓复核**,纠正了我初判的 3 个理由错误(见下)。

## 一句话结论

| 仓 | pin 变化 | 结论 |
|---|---|---|
| 主仓 reBot-DevArm | 无 | 0 新内容(`654ebdc` 已 blob 级落地,baseline-2026-06-27) |
| software/MotorBridge | 无 | 0 落后(`123a6ad`=v0.4.8 顶端;feature 分支全已并) |
| software/reBotArmController_ROS2 | 无 | 0 落后(`a61efe4`;new-dev 已并) |
| **遥操作/StarArm_102** | **`e61f34e → d2eb059`** | ✅ **同步**(2 条纯文档) |
| software/reBotArm_control_py | `0163c08 → be65b46` | **维持 hold**(仅清 1 个残留 .pyc,不追上游重构) |

## StarArm_102 同步(e61f34e → d2eb059)

upstream/main 落后 2 条,精确只动 4 文件、**无任何 .py/代码改动**:
- `053d2f6 update README structure and images`
- `d2eb059 rename Python SDK guide`
- 文件:`A Media/images/11.png`(2.6MB)、`R084 Python_SDK/README.md → PYTHON_SDK_GUIDE.md`、`M README.md`、`M README_EN.md`

**要点(subagent 复核纠正)**:
- `PYTHON_SDK_GUIDE.md` **不是纯改名**(内容变 16%):新增 **Star Arm 102-HD 锁定功能按键板**用法(`button_enable=True`、`button_id=7`)。这正是记录我们已实现的 HD 控制功能;对应代码 `stararm102_ro.py` 的 button 逻辑在我们 HEAD 与 upstream **完全相同、早已存在**,文档无代码风险。
- **2.6MB 图不需要特殊处理**:这是 submodule,推 pin 即把该 commit 的文件整体带过来,**无法对 checkout 出的上游 commit 做选择性 gitignore**;且 StarArm 仓本就跟踪 4-6MB JPG(`4.jpg`=6.2MB),2.5MB 在惯例内。主仓那套"不上传 94MB powerstep 图"的杠杆在 submodule 内部不适用,别混。
- 操作:fork `origin/main` ff `e61f34e..d2eb059` 已 push;主仓 submodule 指针更新。**回滚点:e61f34e。**

## control_py:维持 hold,但理由链纠正(重要)

上游 `vectorBH6` 把 `develop-dm`/`develop-rs`/`develop-copy` 分支**全删**,DM/RS 线合并回 `develop`+`main`,另有新 `win` 分支。我们 pin `0163c08`(fork develop)。

**继续 hold 的理由(成立)**:
- 破坏性重构 `RobotArm`→`RebotArm` 类 + `JointGroup` 架构在 **develop 和 main 都在**(`actuator/rebotarm.py`),我们 HEAD 仍是老 `class RobotArm`。`HEAD..upstream/develop` = **121 文件 +3496/-4005**,整体合入 = 未验证的架构迁移,风险全在我方,而**我们遥操作走 LeRobot 插件、不依赖 control_py 高层 API**,收益≈0。
- DM 不断粮:`config/rebotarm_dm.yaml`、`example/0x01damiao_test.py` 在 develop/main 都还在;develop-dm 删除无副作用。

**⚠️ subagent 纠正我初判的 3 个错误**:
1. **`idx_q` 限位修复:我说反了。** 我们 `0163c08` 的 `get_joint_limits` 按 `idx_q` 索引(正确);**upstream develop 和 main 至今仍按 `jid`(joint_id)索引 = 未修的 bug**(关节 1-5 限位错、joint6 IndexError)。**这是我们独有、必须保留的唯一硬修复。** 埋雷警告:将来别因"以为上游有了"丢掉我们的版本→会静默回归此 IK bug;若迁移到重构,**必须手工重新移植 idx_q**。(`joint3` 改名上游确已有,我们那条冗余可丢。)
2. **pyc 重跟踪仅 develop 成立**:upstream/develop 跟踪 40 个 .pyc,**upstream/main 为 0**;我们 HEAD 残留 1 个 → 本次清理(见下)。
3. **漏判 `win` 分支**:`vectorBH6/win`(顶 `4a816e6`,比 develop 新,Windows 化打包:加 requirements.txt、删 uv.lock)。我们正在 Windows 上,是上游最贴我们的分支——**但它也建在重构之上**,照样 hold,此处登记备案。

**本次对 control_py 的唯一改动(非同步,纯本机维护)**:清掉残留被跟踪的 `actuator/__pycache__/gripper.cpython-310.pyc`(`.gitignore` 早已覆盖 `__pycache__`),fork develop `0163c08 → be65b46`。**hold 基线本质不变(仍不追 upstream 重构)。**

## hold 策略与触发条件(战略决定)

control_py **无限期 hold,不是"永不维护"**。满足任一**触发条件**即重新评估迁移:
1. 我们真要用某个只在重构后才有的 control_py 功能;
2. 有真机时间能做完整迁移 + 回归测试(重力补偿 / IK / DM 控制全验一遍)。

迁移那天必须手工移植 `idx_q` 修复(上游缺)。原则:不盲目追新破坏验证过的基线,也不假装不管埋债。
