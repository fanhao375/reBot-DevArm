# 2026-07-06 上游同步检查

**结论：同步 MotorBridge(v0.4.9) + StarArm_102(训练/相机) + Seeed 主仓(硬件文档，精瘦 fork 剔 92.7MB 大图)；control_py 暂不同步(Route A，已坐实 ROS2 不受影响)。**

---

## 📌 检查信息

- 检查时间：2026-07-06
- 触发：用户明确要求「同步」
- 流程：先 spawn 4 个独立 subagent 逐仓复核影响（基线规则 3 + AI 记忆约定），再执行；升级走 `sync/upstream-20260706` 分支（规则 5）。

## 📦 各仓库状态

| 仓库 | 当前 commit(前) | 上游最新 | 新增 | 是否同步 |
|------|------|------|------|------|
| Seeed/reBot-DevArm | e41119e | f8d2c2a | 53 | ✅（精瘦，剔大图） |
| MotorBridge | 123a6ad | a3db2fb | 3 | ✅（FF v0.4.9） |
| reBotArm_control_py | be65b46 | (develop 领先17) | 17 | ❌（Route A） |
| reBotArmController_ROS2 | a61efe4 | 同上游 | 0 | — |
| StarArm_102 | d2eb059 | 5ac244d | 4 | ✅（FF） |
| web_arm_ui (Yang-Ci) | db77a4d | 无新提交 | 0 | —（我们领先 11） |

## 🔍 主要变化

### MotorBridge 3 条 → 同步（FF，push origin main 123a6ad..a3db2fb）
v0.4.8→v0.4.9。唯一代码改动=RobStride rs-00/rs-06 的 tmax 对齐电机厂商值（17→14 / 60→36），**完全没碰达妙 DM 路径**；其余版本号+文档。无 API 破坏、无删除、无膨胀。
- 验证：`import motorbridge` 通过（已装 wheel 仍为 0.4.7，源码已 0.4.9；因 tmax 仅 metadata、DM 零改动，wheel 无需重编即用）。

### StarArm_102 4 条 → 同步（FF，push origin main d2eb059..5ac244d）
- `加入训练`：README 新增 LeRobot record→train(ACT)→eval 命令模板 + `Python_SDK/set-param.py`(舵机公共参数写入工具)。与我们 LeRobot 训练路线强相关。
- `加入摄像头参数`：follower 加相机进 observation（采数据训练必需）。
- ⚠️ **joint_ranges 软件限位变化（需重核 102 leader→DM follower 遥操作映射）**：
  pan ±150→±105、shoulder_lift -170→-180、elbow -200→-270、wrist_flex -80→-90、wrist_yaw ±90→**±65(收窄)**、wrist_roll ±90→**±180(放宽)**、gripper 不变。
- ⚠️ `stararm102_ro.py` 端口为上游作者本机值 `ttyUSB2/3`、`filtered_size 30→1`（关滤波）——按机调整/实测再定。无 URDF/关节数/命名改动。

### Seeed 主仓 53 条 → 同步（sync 分支解 7 冲突，精瘦 fork）
全部硬件文档/CAD/图，**零代码、零 submodule 指针、零 .gitmodules**。
- 取上游：根 README×2（Pinocchio→vectorBH6 repo、Isaac Sim→reBot-Isaacsim 链接修正）、`01_Lower_Arm_Filler_M.step`、DM 结构件/整机 STEP、`DM_Motor1_wiring_harness_clip`(线束卡扣打印件，184KB 图保留)、Rail_Bracket/UVC32 归位改名。
- **精瘦策略（用户确认）**：4 个 hardware readme 的「电源实拍图未上传本 fork、去上游拿」注记原样保留；**剔除 92.7MB 电源组装实拍图（31 张 powerstep 大图）**，不入库，留本地 + `.gitignore`（既有规则 line 21/24）。
- 冲突 7 处已解，无残留标记、无 UTF-8 乱码、注记完好（merge commit 7ab909e）。

## 🚫 不同步的原因

### reBotArm_control_py（develop 领先 17）→ Route A 暂不同步
- **仅有的 2 个干净 DM 修复我们已含**：`idx_q 关节限位`(0163c08≡上游6a4afd8)、`joint3 typo`(36d4346≡2828b64)，patch-id 完全一致，无需再拿。
- 上游剩余价值 = **纠缠的 RS 泛化重构**（RebotArm/JointGroup/hardware_yaml 新架构），无法 cherry-pick 拆分；且该重构**把 idx_q 修复退回成 bug 版**（robot_model.py 重写用回 joint_id 索引）+ 重新引入 40 个 pyc。盲目 merge = 自己搞坏修复。
- **「ROS2 会 ImportError」的判断已坐实为不成立**：ROS2 控制器（reBotArmController_ROS2）按其 README 从**独立 `third_party/reBotArm_control_py` clone**（取 vectorBH6 上游最新=已含新 API）取用，**不走我们的 submodule**；我们 submodule（旧 API、develop）是独立控制/仿真用途。两份 checkout 两用途，submodule pin 旧不影响 ROS2。
- **下次触发条件**：当要把 control_py submodule 统一到新架构（RebotArm/JointGroup）时，走 Route B——① 手动保住 idx_q 修复 ② 清 pyc ③ 迁配置 arm.yaml→rebotarm_dm.yaml ④ 跑 fk/ik/traj_sim + ROS2 重编验证（后者需 Linux）。

## ✅ 同步操作记录

- sync 分支：`sync/upstream-20260706`
  - `ca7e3ff` 升级 submodule 指针：MotorBridge→a3db2fb / StarArm_102→5ac244d（control_py 不动）
  - `7ab909e` 合并 Seeed 主仓 upstream/main（解 7 冲突，剔 92.7MB 大图）
- fork push：`fanhao375/motorbridge` 123a6ad..a3db2fb；`fanhao375/Star-Arm-102` d2eb059..5ac244d
- 验证门槛：MotorBridge=import 通过(DM 零改动)；StarArm=软件增量/无 URDF 破坏(joint_ranges 已记录，遥操作硬件重核待硬件)；主仓=低门槛，diff 通读无乱码无段落丢失。
- 待办：合入 main + 打 baseline-2026-07-06 tag + push 主仓。

## 📝 下次检查触发条件

- control_py：进入 ROS2 联调 / 要统一 submodule 到新架构时 → 走 Route B。
- StarArm_102：接硬件做遥操作时，按新 joint_ranges 重做标定、改回实际串口、评估 filtered_size。
- 主仓/MotorBridge：每周检查 / 大阶段前 / 知道有官方更新。
