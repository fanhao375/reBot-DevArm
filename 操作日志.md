# 更新日志

记录本仓库的所有本地修改和操作，方便后续 AI 或协作者理解上下文。

---

## 2026-05-28

### 2026-05-29 本地实操：重力补偿双版本 + LeRobot follower 验证通过

**触发**：用户完成带锁/不带锁重力补偿实操，并进入 LeRobot follower 键盘控制验证；5-29 询问“项目地址或上游有更新，应该同步一下，再同步咱们的进度是不是”。

**先同步上游**：

- 本地 `git fetch --all --prune` 后发现 `origin/main` 从 `f6bb354` 更新到 `fa2f626`，新增 tag `baseline-2026-05-28`。
- 执行 `git merge --ff-only origin/main`，主仓快进到 2026-05-28 基线。
- 执行 `git submodule update --init software/MotorBridge`，`software/MotorBridge` 更新到 `23c2299`。
- 保留本地未跟踪文档和工具，不覆盖用户/本地改动。

**当前重要区分**：

| 项 | 状态 |
|---|---|
| 主仓源码基线 | `baseline-2026-05-28` |
| `software/MotorBridge` submodule | `23c2299`，对应 MotorBridge v0.3.9 |
| WSL 重力补偿 Python 环境 | `motorbridge 0.3.7`，Pinocchio `3.9.0` |
| LeRobot conda 环境 | `lerobot 0.5.2` / `motorbridge 0.3.7` / `lerobot_robot_seeed_b601 0.1.2` |

**真机验证结果**：

| 项 | 结果 |
|---|---|
| 达妙 USB-CAN 串口 | WSL `/dev/ttyACM0` |
| 总线扫描 | 扫到 7 个电机 `0x01..0x07`，反馈 ID `0x11..0x17` |
| 无锁重力补偿 | `example/9_gravity_compensation.py` 短跑通过：连接 OK、使能 OK、MIT 模式 OK、500Hz 控制环、`tau_g` 输出正常 |
| 带锁重力补偿 | `example/10_gravity_compensation_lock.py` 短跑通过：初始目标角度锁定、MIT 模式 OK、`LOCKED` 状态正常 |
| LeRobot follower 校准 | `lerobot-calibrate --robot.type=seeed_b601_dm_follower --robot.port=/dev/ttyACM0 --robot.id=follower1 --robot.can_adapter=damiao` 完成 |
| 校准文件 | `/home/pc/.cache/huggingface/lerobot/calibration/robots/seeed_b601_dm_follower/follower1.json` |
| 校准后只读位置 | 7 个关节读数都在 `0 deg` 附近 |

**键盘控制结论**：

- 官方 LeRobot `--teleop.type=keyboard` 不是给 B601 这种 7 关节 follower 直接做关节 jog 的，action 字段不匹配。
- 本仓新增本地安全小步进脚本：`tools/lerobot_b601_keyboard_jog.py`。
- 默认每按一次移动 `1 deg`，并带 `max_relative_target=2 deg` 保护。
- 推荐第一次只按 `s` 读位置，再试 `6/y` wrist_roll，退出按 `x`。

**文档同步**：

- 更新 `LeRobot_follower验证小白执行手册.md`：增加 2026-05-29 当前进度、验收清单改为已通过、补键盘 jog 路径。
- 更新 `重力补偿小白执行手册.md`：增加无锁/带锁两种验证结果和运行命令。
- 更新 `遥操作与LeRobot待办.md`：阶段 1 单臂基础验证、阶段 2 LeRobot follower 验证改为已完成；当前决策点改为 leader 选型。
- 更新 `项目总览.md`：顶部最后本地进展和更新记录同步到 2026-05-29。

**下一步**：

1. 决策 leader 路线：SO-101 起步，还是 reBot 102 leader。
2. 如果继续 LeRobot，进入 leader 采购/打印/安装/校准。
3. 摄像头和数据采集放在 leader 决策之后。

---

### 上游同步：MotorBridge v0.3.7 → v0.3.9 + 主仓 GEM-4/UVC32/电源 + Python wheel 升 0.3.9

**触发**：5-27 用户问上游，5-27 spawn subagent 复核（达妙零改动 / API 100% 兼容 / 主仓 4 commit 文档资源类），用户 5-28 拍板执行。

**上游变化**：

| 仓 | 新增 commit | 性质 |
|---|---|---|
| 主仓 | 4 | GEM-4 社区视频链接 + UVC32 摄像头支架 STEP + 电源 BOM 文档完善 |
| `MotorBridge` | 2（`a6c14d7 → 23c2299`）| v0.3.8 + v0.3.9 release，全 RobStride / Windows PCAN 增强；达妙路径零改动 |
| 其他 3 仓 | 0 | — |

**重要纠正**：GEM-4 不是 reBot 官方推 Gemma 模型集成，是别人用 Google Gemma 控 reBot Arm 做的 demo 视频，上游加进了 README "Feedback from the community" 表格。性质等同"用户晒图"。

**对项目影响**：
- 达妙路径：完全零改动（实测确认）
- Python API：100% 兼容
- WebSocket scan 协议：向后兼容
- UVC32 摄像头支架：未来"装摄像头"阶段可考虑（详见 `遥操作与LeRobot待办.md §4 平行支线 1`）

**操作**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork MotorBridge fast-forward + push | `a6c14d7..23c2299` |
| 2 | 主仓建 `sync/upstream-2026-05-28` 分支 + commit submodule 指针 | sync commit `e150c82` |
| 3 | 合 sync → main | `50fb1ca` |
| 4 | 主仓 merge upstream/main（4 commit）| `50af470` |
| 5 | Python wheel 0.3.7 → 0.3.9 双端 | ✅ Win + WSL |
| 6 | 打 `baseline-2026-05-28` tag | ✅ |

**详情**：`上游更新记录/2026-05-28_同步详情.md`

---

## 2026-05-25

### 上游同步：MotorBridge v0.3.5 → v0.3.7 + Python wheel 升 0.3.7

**触发**：用户主动询问上游。

**按 memory 规则 spawn subagent 复核**——这次跟初判一致，subagent 实测确认：达妙路径真零改动，scan 改动独立子命令不影响 follower。

**上游变化**：

| 仓 | 新增 commit | 性质 |
|---|---|---|
| `MotorBridge` | 2（`96dddf3 → a6c14d7`）| v0.3.6 + v0.3.7 release：全是 RobStride / Windows PCAN / ws_gateway 增强；达妙路径 0 行改动 |
| 其他 4 仓 | 0 | reBotArm_control_py 在 upstream/main 有 `8427281 add pip` 但 develop 没动，我们追 develop |

**v0.3.6 / v0.3.7 要点**：
- Fixed: Windows PCAN RobStride 多 controller 抢通道 / 扫描误报 / 单 host 扫描实时打印 / Windows 时序 clamp
- Added: MOTORBRIDGE_WS_DEBUG / capabilities op / batch_scan op / scan_progress 流式事件 / PCAN 错误码名称映射
- Python scan 回归测试（test_scan.py）

**对项目影响**：
- 达妙路径：完全零改动（实测 `git diff --stat motor_vendors/damiao/` 空输出）
- Python API：100% 兼容（Controller / Motor / Mode / send_mit / ensure_mode 等签名未变）
- WebSocket `{"op":"scan"}` JSON 协议：向后兼容（旧字段未改）
- `lerobot-robot-seeed-b601` follower 适配器：零调用 scan，无影响

**操作**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork MotorBridge fast-forward + push | `96dddf3..a6c14d7` |
| 2 | 主仓建 `sync/upstream-2026-05-25` 分支 + commit submodule 指针 | sync commit `c8957b6` |
| 3 | 合 sync → main | `556aca1` |
| 4 | Python wheel 0.3.5 → 0.3.7 双端 | ✅ Win + WSL |
| 5 | 打 `baseline-2026-05-25` tag | ✅ |

**详情**：`上游更新记录/2026-05-25_同步详情.md`

---

## 2026-05-21

### 上游同步：MotorBridge v0.3.2 → v0.3.5 + 主仓 OSHW 认证 + Python wheel 升 0.3.5

**触发**：用户主动询问上游。

**按 memory 规则 spawn subagent 复核**——抓到 1 个我的初判错误：`96dddf3 feat: support damiao observation parameter stream` commit message 让我误判为"motor 层达妙改动"，实际是 ws_gateway 层。

**上游变化**：

| 仓 | 新增 commit | 性质 |
|---|---|---|
| 主仓 | 1（`d6ed5f3`）| OSHW 认证标识 + 社区反馈图（无功能影响）|
| `MotorBridge` | 8（`bcfc3c1 → 96dddf3`）| v0.3.5 release：达妙路径 3 文件 bugfix（API 100% 兼容）+ RobStride 流式订阅 + ws_gateway 加达妙 WS 流 + Drop impl 自动清线程 + 新 `__version__` |
| 其他 3 仓 | 0 | — |

**对项目影响**：
- 达妙路径：`ensure_mode()` 行为增强（内置 3 次重试），`open_socketcan→open_can_bus` 跨平台后端选择器（旧名保留），ABI 内部 Rust 重构（FFI 无感）
- Python API 全部签名未变；新增 `motorbridge.__version__` 等
- `lerobot-robot-seeed-b601` follower 适配器**完全兼容**且有利好（Drop polling 清理 / ensure_mode 内置重试）

**操作**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork MotorBridge fast-forward + push | `bcfc3c1..96dddf3` |
| 2 | 主仓建 `sync/upstream-2026-05-21` 分支 + commit submodule 指针 | sync commit `6f5ffc5` |
| 3 | 合 sync → main | `4f1c2b2` |
| 4 | 主仓 merge upstream/main（OSHW README）| `dced175` |
| 5 | Python wheel 0.3.2 → 0.3.5 双端 | ✅ Win + WSL |
| 6 | 打 `baseline-2026-05-21` tag | ✅ |

**详情**：`上游更新记录/2026-05-21_同步详情.md`

---

## 2026-05-18

### 上游同步：MotorBridge v0.3.2 + ROS2 仓 launch 重命名 + Python wheel 升 0.3.2

**触发**：用户主动询问上游更新。

**按 memory 规则 spawn subagent 复核**——抓到我 2 个初判错误（MotorBridge commit 数 5 → 实际 9；ROS2 仓 4 commit 误判为"漏合"实际是真新）。

**上游变化**：

| 仓 | 新增 commit | 性质 |
|---|---|---|
| `MotorBridge` | 9（`681f021 → bcfc3c1`） | v0.2.9 + v0.3.0/0.3.1/0.3.2 + RobStride 协议规范化 + Python CLI 修复 + slcan→canable 脚本重命名（达妙路径基本零改动）|
| `reBotArmController_ROS2` | 4（`92ceb76 → 682d12f`） | ⚠️ **破坏性**：driver_only→driver.launch.py 重命名 / rviz.launch.py 删 / safe_home() 不再自动闭夹爪 + 新增 `GripperCommand.srv`（+853/-241 行 29 文件）|
| `reBotArm_control_py` upstream/develop | 0 | 不动 |
| 主仓 + StarArm_102 | 0 | — |

**对项目影响**：
- 达妙路径：Python wheel 升 0.3.2，**业务零影响**（subagent 实测 `motor_vendors/damiao/` 只有 1 行 typo 级改动）
- ROS2 仓破坏性变更：当前装机阶段没消费这些 launch/msg，**零影响**；等真要跑 ROS2 时按新接口写
- 装机烧录指南脚本路径没引用 slcan_restart.sh，**零影响**

**操作（走 sync 分支）**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork MotorBridge fast-forward + push | `681f021..bcfc3c1` |
| 2 | fork reBotArmController_ROS2 fast-forward + push | `92ceb76..682d12f` |
| 3 | 主仓建 `sync/upstream-2026-05-18` 分支并 commit 双 submodule 指针 | sync commit `0a19218` |
| 4 | 合 sync → main | `23d5a48` |
| 5 | Python wheel 0.2.8 → 0.3.2 双端 | ✅ Win + WSL |
| 6 | 打 `baseline-2026-05-18` tag | ✅ |

**详情**：`上游更新记录/2026-05-18_同步详情.md`

---

## 2026-05-15

### 装机阶段全通关：电机 ID + 零点 + 整机控制全部跑通

**用户实操进展**（用户原话："我电机 reBot Arm B601-DM 已经都配置好了，用那个 MotorBridge 的网页也可以移动测试啥的"）：

| 项 | 状态 |
|---|---|
| 7 个电机 motor_id 烧录（0x01..0x07） | ✅ 全完 |
| 7 个关节零点标定（set_zero_position） | ✅ 全完 |
| 机械臂结构件物理拼装 | ✅ 整机成型 |
| MotorBridge Web UI 整机控制 | ✅ 同时控 7 个电机能动 |
| 24V 电源对地短路检查 | ✅ |
| CAN-H/L 60Ω 终端电阻测试 | 跳过（Web UI 整机通信 OK 反推 CAN 正常） |
| 单电机命令行测试 | 跳过（Web UI 已覆盖等价功能） |

**意义**：装机烧录指南 §1-§5（焊线→拼装→上电→烧 ID→标零点）全部走完，进入 §6 跑代码阶段。

### LeRobot 集成评估：clone Seeed-Projects/lerobot-robot-seeed-b601 等 3 仓 + SO-ARM100 入探索区

**触发**：5-14 找到 Seeed 官方 wiki 提到的 LeRobot 集成方案（含为达妙 reBot Arm B601-DM 写的 follower 适配器）。今日 clone 4 个相关仓到 `_lerobot_experiment/`（gitignored）评估区：

- `huggingface/lerobot`（主线，含 `so_leader` 一等公民支持 SO-100/SO-101）
- `Seeed-Projects/lerobot-robot-seeed-b601`（reBot Arm follower 适配器，确认 import motorbridge 调达妙 CAN）
- `Seeed-Projects/lerobot-teleoperator-rebot-arm-102`（reBot 102 leader 适配器）
- `TheRobotStudio/SO-ARM100`（SO-100/SO-101 硬件仓，含完整 STEP/STL + Seeed 配套安装板设计）

**关键发现**：
- `reBotArm_control_py` 完全没多臂/teleop 代码（grep 0 匹配）—— 多臂逻辑完全在 LeRobot 框架层
- follower 适配器代码里有 `if 'wrist_yaw' not in goal_pos: goal_pos['wrist_yaw'] = 0.0` —— **作者已经为"6 DOF leader 控 7 关节 follower"做了兼容**，意味着 SO-101 leader 控 reBot Arm follower 在代码层已设计支持
- SO-ARM100 仓内有 `Seeedstudio_Mounting_Plate_SO101.step` —— **Seeed 自己提供 SO-101 → reBot Arm 安装板设计**，认可混搭

**未升级到 submodule**：3 个 Seeed 仓 + huggingface/lerobot 都保留在探索区，按"评估通过再 fork + submodule"原则，等阶段 2 LeRobot follower 实操跑通再决定。

详见 `_lerobot_experiment/README.md` + `遥操作与LeRobot待办.md`。

### 装机烧录指南 §6.4 重力补偿：完整 cookbook（WSL 主路径）

**目标**：把"跑重力补偿"从指南骨架里 4 行扩展成完整可执行 cookbook。

**Windows 路径尝试失败**：`pip install pin` 在 Python 3.12 Windows 上**没 wheel**，pip 退回**源码编译**（cmeel-boost + libpinocchio + scipy 等，需 MSVC/Boost/cmake 全套），30 分钟没编出来，已 kill。原因：Pinocchio 在 Windows 主要靠 conda-forge 分发 binary，PyPI 没有 Python 3.12 Windows wheel。

**切到 WSL 主路径**：
- WSL2 Ubuntu Python 3.10 + Pinocchio 3.9.0 已确认装好（之前装的）
- 跟 reBotArm_control_py README 官方推荐 "Operating System: Ubuntu 22.04+" 一致
- 唯一额外配置：Windows 装 `usbipd-win` 把 USB-CAN 设备转发给 WSL

§6.4 新内容（约 150 行）：
- 前置条件 7 项检查清单
- 一次性准备（usbipd-win 安装）
- 实操 8 步（接 USB → bind → attach → WSL chmod → 跑脚本 → 看输出 → 真机验证 → 安全停止）
- 预期终端输出完整示例
- 9 条常见报错排查表
- Windows 失败附录 + conda-forge 备选提示

**待用户实操**：硬件已搭好（电机配置完成），照 §6.4 一步步走即可。

### Memory 更新

- 新建 `feedback_subagent_review_routine.md`：评估/建议/决策类任务都先 spawn subagent 复核（不只上游同步）
- 新建 `project_teleop_leader_choice_risk.md`：reBot 102 vs SO-ARM101 leader 选型风险记录
- 5-15 更新：用户确认**102 和 SO-101 都没买**，决定哪个就 3D 打印；leader 选型变成"完整新装决策"

### 文档同步

- `装机烧录指南.md` §6.4 重写为 WSL 主路径
- `遥操作与LeRobot待办.md` 现状段更新（leader 全空白 + 文档校准声明）
- `操作日志.md` 追加本条
- `项目总览.md` 最后同步时间 + 记录表加 2026.05.15
- `复刻基线维护原则.md` 最后更新日期 → 2026-05-15

---

## 2026-05-14

### 上游同步：MotorBridge 681f021 + ROS2 仓 MoveIt 2 集成 + 主仓 README

> 📌 **日期校准声明**：本条目最初按错误的系统日期记录为"2026-05-13 第二批，傍晚"，5-14 当天发现后修正。tag `baseline-2026-05-13-2` 已重命名为 `baseline-2026-05-14`；上游更新记录文件 `2026-05-13b_同步详情.md` → `2026-05-14_同步详情.md`。git merge commit message 内的 `sync/upstream-2026-05-13b` 字样保留，不重写历史。

**触发**：用户询问上游，发现 4 个仓里有 3 个有更新（主仓 1 + MotorBridge 2 + ROS2 仓 2 + reBotArm_control_py 上游 main 3 但 develop 0）。用户明确"按持续追新原则同步，文档记录变动即可"。

**上游变化**：

| 仓库 | 新增 commit | 性质 |
|------|------------|------|
| `Seeed-Projects/reBot-DevArm` | 1（`53f5d5f`） | README_zh ROS2 wiki 链接更新 |
| `motorbridge/motorbridge` | 2（`681f021` + dependabot） | PCAN 重启文档 + rand 依赖升级 |
| `EclipseaHime017/reBotArmController_ROS2` | 2（`92ceb76` + `802f3c3`） | ⭐ **MoveIt 2 集成 + 柔性夹爪新 URDF**（+4607/-847 行 62 文件） |
| `vectorBH6/reBotArm_control_py` upstream/main | 3 | 删 `1_damiao_text.py` + 5 个 README + IK 测试 |
| `vectorBH6/reBotArm_control_py` upstream/develop | 0 | 无改动 |

**⚠️ reBotArm_control_py 特殊状况**：上游 main 跟 develop 已分叉，今天的 3 个破坏性改动（含删 `1_damiao_text.py`）只在 main，develop 未变。我们 5-08 同步时确立追 develop 分支，所以**本仓今天不同步该 submodule**，等上游把 main 合回 develop 或明确主线方向再处理。`1_damiao_text.py` 在本仓继续可用。

**ROS2 仓 MoveIt 2 集成关键内容**：
- 新增 `rebotarm_moveit_demos` 包：`demo_common.py` / `draw_square.py` / `pick_place.py`
- 消息接口拆分：旧 `JointMotorCmd.msg`（20 字段）→ 新 `JointMitCmd` + `JointPosVelCmd` + `JointVelCmd`（破坏性 ABI 变更，但本仓还没消费这些 msg）
- `hardware_manager.py` +/-708 行重写、`ros_actions.py` -184 行简化

**操作（走 sync 分支）**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork MotorBridge fast-forward + push | `a2b50ed..681f021` |
| 2 | fork reBotArmController_ROS2 fast-forward + push | `d3a415e..92ceb76` |
| 3 | 主仓建 `sync/upstream-2026-05-13b` 分支并 commit 双 submodule 指针 | sync commit `bda8dd7` |
| 4 | 合 sync → main | `a86b8c2` |
| 5 | 主仓 merge upstream/main（README 1 行） | `5ccbcda` |
| 6 | 打 `baseline-2026-05-14` tag（最初误命名 `baseline-2026-05-13-2`，已校准） | ✅ |

**对项目影响**：
- 达妙路径：完全不受影响（MotorBridge 改的是 PCAN 文档 + 依赖）
- Python wheel：不需要再升级（0.2.8 → 0.2.8，今日 2 个 commit 还没发新版）
- ROS2 stack：本仓目前没消费 msg，破坏性变更对本仓零影响；等真要跑 MoveIt 2 演示时按新 msg 接口写代码

**详情**：`上游更新记录/2026-05-14_同步详情.md`

---

## 2026-05-13

### 装机烧录指南：追加 §12 待决项（视频太长无章节标记）

**触发**：用户实操跟读 B 站官方装机视频 BV1K9dbBNEdC 时反馈"一个小时纯机械+穿线，想跳到具体步骤跳不到，挺难受的"。

**改动**：`装机烧录指南.md` 新增 §12.1 待决项，列 3 个候选方案（A. 章节时间戳地图 / B. 重剪视频 / C. 文字+实拍图笔记），优先级未定，等装第二台或做小红书时再判断。

**commit**：`7bde0fd 装机烧录指南：追加 §12 待决项`

### 上游同步：主仓 LICENSE 协议大更新 + MotorBridge 升级 v0.2.6 → v0.2.8

**触发**：用户连续多日询问上游更新状态，今日确认 MotorBridge 0.2.8 发版后授权同步（"那也更新加上吧"）。

**上游变化**：

| 仓库 | 新增 commit | 性质 |
|------|------------|------|
| `Seeed-Projects/reBot-DevArm` | 2（`b738018` + `ae5f123`） | LICENSE 协议大改 + STEP 文件修复 |
| `motorbridge/motorbridge` | 9（`faba15f..a2b50ed`） | 全部 RobStride 路径，达妙零改动 |

**主仓 LICENSE 重构**：从单一 CC BY-NC-SA 4.0（禁商用）拆成"硬件 CERN-OHL-W 2.0 + 软件 Apache 2.0"双协议（允许商业使用）。5 个语言版本 README 同步更新协议徽章。

**MotorBridge 0.2.6 → 0.2.8**：RobStride 型号专属参数表、零点范围、运行时参数补全，跟达妙路径完全无关。

**对项目影响**：
- 达妙路径完全不受影响（`motor_vendors/damiao/` 一行未改，`bindings/python/` 仅版本号 0.2.6 → 0.2.8）
- 主仓 STEP 文件跟用户无关（用户买完整套装，不自己 3D 打印零件）
- 协议变更允许后续考虑公开版/商用衍生品

**操作**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork MotorBridge 内 `git merge --ff-only upstream/main` | submodule HEAD `b9f7065` → `a2b50ed` |
| 2 | `git push origin main`（fanhao375/motorbridge） | `b9f7065..a2b50ed` 已推送 |
| 3 | 主仓 `sync/motorbridge-2026-05-13` 分支 commit submodule 指针 | sync commit `12df10f` |
| 4 | 静态验证：`motor_vendors/damiao/` 零改动 / Python binding 仅版本号变 | ✅ |
| 5 | 合 sync → main | `d6ef5be` |
| 6 | 主仓 `git merge upstream/main` 引入 LICENSE + STEP | `02ba6eb` |
| 7 | Python wheel 双端 0.2.6 → 0.2.8 | ✅ Windows + WSL2 |

**详情**：`上游更新记录/2026-05-13_同步详情.md`

---

## 2026-05-09

### 上游同步：MotorBridge 升级到 b9f7065（v0.2.4 → v0.2.6）

**触发**：检查上游发现 motorbridge 新发 v0.2.5 + v0.2.6 双版本，用户授权同步。

**上游变化**（`motorbridge/motorbridge:main` 新增 3 个 commit）：
- `21c12d7` Release Python package 0.2.5（CLI 增强 + 文档更新）
- `a258ec5` fix(robstride): align scan and id handling for 0.2.6（⭐ RobStride 底层重写 + Python 绑定新增 host_id 变体方法 + ID 范围校验 + 删除 mintlify 文档目录改放 docs/）
- `b9f7065` docs（CAN 排障链接）

**对项目影响**：
- 达妙路径完全不受影响（add_damiao_motor / Mode / Controller 没动）
- reBotArm_control_py 配置全部 `vendor: damiao`，不走改动的 RobStride 分支
- 即便将来切 RobStride，本仓 motor_id 0x01..0x07、feedback_id 0x11..0x17 都在新版校验范围内
- Python 环境侧：Windows Python 3.12 当前是 wheel 0.2.4，submodule 升级不自动同步到环境，需 Rust 工具链 cargo build + pip install -e --force-reinstall

**操作（按复刻基线维护原则规则 5 走 sync 分支）**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork 内 `git merge --ff-only upstream/main`（无本地分歧，fast-forward） | submodule HEAD `f7037b0` → `b9f7065` |
| 2 | `git push origin main`（fanhao375/motorbridge） | `f7037b0..b9f7065` 已推送 |
| 3 | 主仓新建 `sync/motorbridge-2026-05-09` 分支并 commit submodule 指针 | sync commit `a581732` |
| 4 | 静态验证 + 隔离性验证 | ✅ AST 全过、改动 100% 隔离在 RobStride 路径、达妙 API 未动 |
| 5 | 合入 main + 打 baseline tag | ✅ `baseline-2026-05-09` 已打并 push |
| 6 | Python 环境升级 wheel 到 0.2.6 | ✅ Windows Python 3.12 + WSL2 Ubuntu Python 3.10 双端从 PyPI 直接装预编译 wheel（不需要 Rust 工具链）；新符号 `motor_handle_robstride_ping_host_id` / `motor_handle_robstride_get_param_f32_host_id` 已在 .dll/.so 解析；`Motor.robstride_ping_host_id` / `robstride_get_param_f32_host_id` 方法可见；`add_robstride_motor` 1..255 范围校验生效 |

**关键发现**：上游 motorbridge 已发布 PyPI wheel（0.2.6 / 0.2.5 / 0.2.4 ...），不必本地 cargo build，`pip install --upgrade motorbridge` 直接拉预编译版本。

**详情**：`上游更新记录/2026-05-09_同步详情.md`

---

## 2026-05-08

### 上游同步：reBotArm_control_py 升级到 062bef9（含 RobotArm.fresh()）

**触发**：用户明确要求升级 `reBotArm_control_py`（`2026-05-06_同步详情.md` 写明此触发条件）。

**上游变化**（`vectorBH6/reBotArm_control_py:develop` 新增 1 个 commit `5537201 fix G`）：
- 新增 `RobotArm.fresh()` 空指令方法（不切换模式，把控制量清零）
- `example/9_gravity_compensation.py`：`mode_mit()` 后调用 `arm.fresh()`
- `example/10_gravity_compensation_lock.py`：`_KP 8.0→7.0`、`_KD 1.0→0.8`，并加 `arm.fresh()`
- 上游噪声：误提交 `__pycache__/arm.cpython-310.pyc`（fork 已 .gitignore，合并时保持删除）

**操作（按复刻基线维护原则规则 5 走 sync 分支）**：

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | fork 内 `git merge --no-ff upstream/develop`，解决 pyc 冲突（保持删除） | submodule HEAD `5ae4d56` → `062bef9` |
| 2 | `git push origin develop`（fanhao375/reBotArm_control_py） | `5ae4d56..062bef9` 已推送 |
| 3 | 主仓新建 `sync/rebotarm-control-py-2026-05-08` 分支并 commit submodule 指针 | sync commit `dcca918` |
| 4 | 静态验证 | ✅ AST 全过、`fresh()` 方法存在、9/10 号示例已调用、KP/KD 参数到位 |
| 5 | 运行验证（fk_sim/ik_sim/traj_sim） | ⏳ 待 Python 3.10 + uv + Pinocchio 环境 |
| 6 | 合入 main + 打 baseline tag | ⏳ 等用户决策 |

**详情**：`上游更新记录/2026-05-08_同步详情.md`

---

## 2026-05-07

### 发现：官方 wiki 文档中的 ROS2 仓库地址问题

**背景**：检查官方 wiki 文档 [reBot Arm B601-DM ROS2 Integration](https://wiki.seeedstudio.com/rebot_arm_b601_dm_ros2_integration/)，发现文档中给出的仓库地址存在问题。

**官方文档中提到的地址**：
1. `https://github.com/Seeed-Projects/reBotArmController_ROS2.git` - 作为主要安装地址
2. `https://github.com/EclipseaHime017/reBotArmController_ROS2.git` - 作为"开发仓库"备选

**实际验证结果**：
- ❌ `Seeed-Projects/reBotArmController_ROS2` - **仓库不存在**（GitHub 返回 404）
- ✅ `EclipseaHime017/reBotArmController_ROS2` - 仓库存在且可用（3 commits, v0.0.2）

**当前配置**：
- 本地 submodule 已正确配置：
  - `origin`: `https://github.com/fanhao375/reBotArmController_ROS2.git` (个人 fork)
  - `upstream`: `https://github.com/EclipseaHime017/reBotArmController_ROS2.git` (实际可用的上游)

**结论**：
- 官方文档存在错误，`Seeed-Projects` 组织下的仓库尚未创建
- 目前唯一可用的上游是 `EclipseaHime017` 的个人开发仓库
- 本项目的 fork 和 upstream 配置是正确的

**后续观察**：
- 持续关注官方是否会创建 `Seeed-Projects/reBotArmController_ROS2` 正式仓库
- 如果官方创建正式仓库，届时可考虑切换 upstream 指向

---

## 2026-05-06

### 上游同步：ROS2 集成状态 + 独立 ROS2 submodule

**背景**：用户已 fork `fanhao375/reBotArmController_ROS2`，并询问 Seeed 官方 README 中的 ROS2 完成是否对应实际源码。检查后确认：主仓库只更新 README 状态和链接，ROS2 源码在独立工作区仓库里。

**同步结果**：

| 项目 | 结果 |
|------|------|
| `Seeed-Projects/reBot-DevArm` | 已合并 `a7d923b..511e140`，本地 merge commit `9fe4cf2` |
| `software/reBotArmController_ROS2` | 新增 submodule，指向 `fanhao375/reBotArmController_ROS2` 的 `d3a415e` |
| `Seeed-Projects/reBotArmController_ROS2` | Git / GitHub API 当前仍返回 404，暂不作为 upstream |
| `EclipseaHime017/reBotArmController_ROS2` | 当前可用上游，submodule 内已配置为 `upstream` |

**主要判断**：
- 官方主仓库本次只改 `README.md` / `README_zh.md`，没有新增 ROS2 包、launch、URDF、`package.xml` 等源码文件。
- ROS2 集成应按独立项目管理，放在 `software/reBotArmController_ROS2`，不复制进主仓库普通目录。
- `reBotArm_control_py` 的 `upstream/develop` 有 `5537201 fix G`，但属于功能性控制代码升级，本次不混入，后续按 submodule 升级流程单独验证。

**提交**：
- `6a41652` 接入 reBotArmController_ROS2 submodule
- `9fe4cf2` Merge remote-tracking branch 'upstream/main'

**文档更新**：
- `项目总览.md`
- `software/README_zh.md`
- `复刻基线维护原则.md`
- `Git操作指南.md`
- `上游更新记录/README.md`
- `上游更新记录/2026-05-06_同步详情.md`

**待验证**：
- ROS2 Humble 环境下 `colcon build`
- `rclpy` / `motorbridge` / `reBotArm_control_py` 同环境 import
- `bringup.launch.py` / `driver_only.launch.py` 启动验证
- 真机 ROS2 topic/service/action 调用验证

---

## 2026-05-05

### 上游同步：主仓库 + reBotArm_control_py

**背景**：检查上游发现两处更新，按"持续追上游最新进展"原则同步。

**同步结果**：

| 仓库 | 升级前 | 升级后 | 增量 |
|------|--------|--------|------|
| 主仓库 reBot-DevArm | c337d44 | a006965 | +1 commit |
| reBotArm_control_py | a5078ae | 5ae4d56 | +11 commit |

**主要变化**：
- **主仓库**：新增软体夹爪（Soft Gripper）的 3D 打印件 STEP 文件 + 5 个 README 多语言更新
- **reBotArm_control_py**：补齐 4 种语言 README（中/西/日/法）+ 新增 `example/1_damiao_text.py` 达妙电机示例 + 合并外部贡献者 PR ⭐

**操作流程**：
1. 主仓库：`git fetch upstream` + `git merge upstream/main`（fast-merge，1 个 commit）
2. reBotArm_control_py（在 fork 的 develop 分支上）：
   - `git checkout develop` + `git merge upstream/main`（无冲突，本地修复 a5078ae 保留）
   - `git push origin develop`（推到 fanhao375/reBotArm_control_py）
   - 回主仓库 `git add software/reBotArm_control_py` + commit

**提交**：
- `a006965` Merge remote-tracking branch 'upstream/main'（主仓库）
- `a00cb9d` 升级 reBotArm_control_py submodule: a5078ae → 5ae4d56

**详细记录**：[`上游更新记录/2026-05-05_同步详情.md`](上游更新记录/2026-05-05_同步详情.md)

**待验证**：
- 主仓库：软体夹爪 STEP 是否能在 CAD 打开
- reBotArm_control_py：⭐ **优先看 `example/1_damiao_text.py`**（达妙电机示例，对当前调试直接有用）；fk/ik/traj/gravity_sim 是否仍能跑

### 2. 阅读 example/1_damiao_text.py 单电机 REPL 工具

- 是个交互式命令行工具，相当于"DMTool 的 Python 版"
- 支持 enable/disable/set_zero/state/mode/mit/posvel/vel 命令
- 跑前必须改 3 个配置：
  - `CHANNEL`（WSL2: `/dev/ttyACM0`，Windows: `COMx`）
  - `MOTOR_ID` / `FEEDBACK_ID`（电机 CAN ID）
  - `MODEL` ⚠️ 默认 `"4340P"`，但 reBot 小关节是 **`"4310"`**，必须改
- 文件位置：`software/reBotArm_control_py/example/1_damiao_text.py`

### 3. 修正：安装 motorbridge Python 包到 v0.2.4

**背景**：4-29 升级 `software/MotorBridge` submodule 到 v0.2.4，但**只升级了源码**，Python 环境里还是 4-13 装的 v0.1.7 旧版。今天发现并补装。

**之前给错的命令**：`pip install -e software/MotorBridge`
**正确路径**：`pip install -e software/MotorBridge/bindings/python`（Python SDK 在 bindings/ 子目录里，主目录是 Rust 项目）

**WSL2 安装结果**：
```
Found existing installation: motorbridge 0.1.7
Successfully installed motorbridge-0.2.4
```

**验证通过**：
- `pip show motorbridge` → Version: 0.2.4 ✅
- `from motorbridge import Controller, Mode; print('OK')` → OK ✅
- `dir(motorbridge)` 列出完整 API：Controller / Motor / Mode / MotorState + 全套 RID_* 常量 + 达妙专用工具

**新版的好东西**（旧版没有）：
- 寄存器命名常量：`RID_KP_ASR=25`、`RID_KI_ASR=26`、`RID_KP_APR=27`、`RID_KI_APR=28`、`RID_PMAX/VMAX/TMAX`、`RID_CTRL_MODE`、`RID_ESC_ID`、`RID_MST_ID`、`RID_TIMEOUT`
- 达妙专用工具：`DAMIAO_HIGH_IMPACT_RIDS`（高风险写错会失控的）/ `DAMIAO_PROTECTION_RIDS`（保护类）/ `DAMIAO_RW_REGISTERS`（全部可读写）/ `get_damiao_register_spec()`
- 4 种模式常量：`MODE_MIT` / `MODE_POS_VEL` / `MODE_VEL` / `MODE_FORCE_POS`

**新版的小坑**：
- 不再提供 `motorbridge.__version__`，查版本必须用 `pip show motorbridge`

**Why**：`pip install -e software/MotorBridge` 会失败因为根目录是 Rust 项目（`Cargo.toml`），Python SDK 在 `bindings/python/`。

---

## 2026-04-29

### 1. 为电机说明书 MD 文档添加图片

- 用 PyMuPDF 从 PDF 提取 80 张原始高清图片到 `hardware/reBot_B601_DM/Motor_Datasheets/docs_md/images/`
- 第一次用 pdfplumber 提取图片质量差，重新用 fitz (PyMuPDF) `extract_image()` 拿到原始字节，质量与 PDF 一致
- 把关键图片嵌入到对应 MD 章节：
  - `01_产品介绍.md` - 电机外观、命名规则
  - `02_产品规格.md` - TN 曲线
  - `03_硬件说明.md` - 接口线序、尺寸安装图
  - `04_工作模式.md` - 4 种控制模式框图
  - `06_电机调试流程.md` - 校准波形图、调试界面
  - `07_调试操作.md` - 各模式调试界面截图
  - `08_固件升级.md` - 版本查看、升级界面
  - `09_调试助手使用.md` - 连接界面

### 2. 调试达妙电机 DM-J4310-2EC，发现调试工具版本问题

**问题现象**：
- 使用 `Debugging_Tools_v.1.6.8.8.exe` 调试电机时，串口在校准等操作中频繁出现乱码
- 反馈帧 ID 不一致（一会儿是 6，一会儿是 4）
- 读取参数命令无响应
- 校准时提示"数据传输失败"
- 反馈状态码 ERR=3（输出轴校准异常）

**根本原因**：
- **调试工具版本不兼容**！`Debugging_Tools_v.1.6.8.8.exe` 与电机固件不匹配
- 正确工具应使用：`DMTool v2.1.6.0-FD加速(1).exe`

**工具位置**：
- 错误版本：`hardware/测试工具/Debugging_Tools_v.1.6.8.8.exe`（不要用）
- ✅ 正确版本：`hardware/测试工具/DMTool v2.1.6.0-FD加速(1).exe`

**经验教训**：
- 达妙电机调试必须使用配套版本的 DMTool
- 文档要求 V2.0.0.0 及以上版本，但实际需要 v2.1.6.0-FD 才能稳定通信
- 遇到串口乱码、ID 错乱、读参数无响应等问题，**先排查工具版本**

### 3. 学习达妙电机 4 种控制模式

- 通过 CAN 命令实测：发送使能 + 速度命令，确认 CAN 通信流程
- 解析反馈帧字节含义：D[0] 高 4 位是错误码 ERR，低 4 位是 ID
- 学到 MIT 模式的来源（MIT Cheetah 机器人）和 PVT 模式的含义（Position+Velocity+Torque）
- 详细总结见 `学习笔记.md`

### 4. 检查上游更新

- 主仓库 `Seeed-Projects/reBot-DevArm` ✅ 无新提交
- Submodules 有更新但**暂不同步**：
  - `software/MotorBridge`: 50 个新提交（架构重构 + 版本 0.1.8→0.2.4），改动太大，等用到再升级
  - `software/reBotArm_control_py`: 0 个新提交
  - `遥操作/StarArm_102`: 4 个新提交（模型更新），等遥操作开发时再同步
- 详见 `上游更新记录/2026-04-29_同步详情.md`

### 5. 调整 .gitignore 和文件结构

- `.gitignore` 新增：
  - `hardware/测试工具/`（DMTool 等 exe 文件 33MB，太大不适合上传）
  - `hardware/测试数据/`（个人调试数据，对别人没用）
- 项目总览和操作日志中保留这两个目录的说明（让 AI 知道本地有但不在 git 里）

### 6. 确立"复刻基线维护原则"

**背景**：检查上游时发现 MotorBridge 有 50 个新提交，引发对项目同步策略的根本讨论。

**核心结论**：
- 项目定位是**复刻集成工程**，不是单一软件包维护
- 目标：**持续追上游最新进展**，但 main 分支只接受**验证过的最新**
- 一句话原则：追最新，但不盲目追最新；更新必须记录来源、原因、验证结果和回滚点
- 三层结构：sync 分支（试升级）→ main（已验证的最新）→ baseline tag（阶段性快照）
- 所有 4 个核心依赖都应 fork 到 fanhao375 账号下（Seeed/MotorBridge/reBotArm_control_py/Star-Arm-102）
- 主仓库 submodule 永远只指向自己的 fork
- 每次同步检查都要记录，不同步也要写原因 + 下次触发条件
- Submodule 升级必须走 sync 分支 + 验证流程
- 不擅自 push、不擅自改 .gitmodules、不擅自升级 submodule

**落地动作**（阶段 A，今天完成）：
- 新建 `复刻基线维护原则.md`（主仓库根目录，作为最高规则文档）
- 更新 `项目总览.md` AI 接手提示（明确读文档顺序 + 核心定位）
- 本日志记录决策过程

**核心定位修正记录**：
- 初稿写的是"5 年后 clone 还能跑，不追最新"
- 用户指出表述不准确："不追最新"会误解成保守不动，实际是"追最新但有质量门"
- 修正为"持续追上游最新进展，但 main 分支只接受验证过的最新"
- 这个修正不改变方向，是把复刻基线从"静态快照"概念改成"持续验证的滚动基线"

**待办**（阶段 B，需用户配合）：
- [x] 用户 fork motorbridge/motorbridge → fanhao375/motorbridge
- [x] 用户 fork servodevelop/Star-Arm-102 → fanhao375/Star-Arm-102
- [x] 修改 `.gitmodules` 指向自己的 fork
- [x] 在每个 fork 内配置 upstream remote
- [x] 在当前锁定的 commit 上打本地 baseline tag：`baseline-2026-04-29`（待用户确认后再 push tag）
- [x] 单独 commit submodule URL 变更

### 7. 补充 Git 操作指南

把今天做的复杂 git 操作沉淀到 `Git操作指南.md`，新增"进阶：复刻工程的 Git 操作"章节，含 8 个主题：

1. 检查上游有没有更新（主仓库 + submodule）
2. 把 submodule 切换到自己的 fork（修改 .gitmodules + sync + 内部 remote 配置）
3. 打 baseline tag（阶段性可复现快照）
4. submodule 升级走 sync 分支（不直接在 main 上做）
5. 中文 commit message 用文件而非 HEREDOC（Windows 下 HEREDOC 会出现字符截断乱码）
6. 撤销刚刚的 commit（reset --soft 而非 amend）
7. commit 要分层（每类工作单独 commit，便于 bisect/revert）
8. 保护 upstream 不被误推送（set-url --push DISABLED）

**Why:** 今天实际操作时遇到了几个不在原 Git 操作指南里的复杂场景，把实战经验沉淀下来给后续协作者参考。零基础部分保持不变，进阶内容是补充。

### 8. 同步 MotorBridge / StarArm_102 到上游最新

**背景**：用户明确要求本地文件跟进上游最新进度。此前"暂不同步"的判断改为"同步但记录验证状态"，符合复刻基线维护原则里的"持续追上游最新进展，main 只接受验证过的最新"。

**同步结果**：
- `software/MotorBridge`: `f63896a` → `f7037b0`（跟进 50 个提交，版本到 v0.2.4 附近）
- `遥操作/StarArm_102`: `aaa80d7` → `d777473`（跟进 4 个提交）
- `software/reBotArm_control_py`: 无更新，保持 `a5078ae`

**主要变化**：
- MotorBridge：Studio UI 拆到 `motorbridge/motorbridge-studio`，Python 包版本升级到 0.2.4，达妙寄存器值同步，RobStride 参数兼容性和模式切换稳定性改进，新增 macOS PCBUSB PCAN / standalone gateway 等能力。
- StarArm_102：更新 README / Hardware Overview / Joint Limit，更新 ROS2 Gazebo 模型、URDF、MoveIt/ros2 controller 配置，`link7_left.STL` 和 `link7_right.STL` 文件大小变化明显。

**提交**：
- `7a1bacc` 升级 MotorBridge submodule: `f63896a` → `f7037b0`
- `a132345` 升级 StarArm_102 submodule: `aaa80d7` → `d777473`

**详细记录**：
- `上游更新记录/2026-04-29_submodule升级.md`
- `上游更新记录/2026-04-29_同步详情.md` 已标记为当日早些时候的"暂不同步"旧决策，后续以 submodule 升级记录为准。

**待验证**：
- MotorBridge：Python 安装、`import motorbridge`、示例初始化、与 `reBotArm_control_py` 的兼容性、硬件 CAN/电机扫描/速度模式/位置模式。
- StarArm_102：STL 模型加载、URDF 关节/命名一致性、Joint Limit 变化是否影响遥操作标定。

---

## 2026-04-27

### 整理电机说明书为 Markdown 文档

- 提取 `DM-J4310-2EC V1.2减速电机说明书V1.2定稿.pdf` 全部 45 页内容
- 按章节拆分为 9 个独立 Markdown 文档，保存到 `hardware/reBot_B601_DM/Motor_Datasheets/docs_md/`：
  - `00_目录.md` - 总目录导航
  - `01_产品介绍.md` - 免责声明、注意事项、电机特色
  - `02_产品规格.md` - 特征参数、工作电压、扭矩系数
  - `03_硬件说明.md` - 物品清单、接口线序、指示灯
  - `04_工作模式.md` - MIT/位置速度/速度/力位混控模式
  - `05_CAN通信.md` - 波特率、反馈帧、控制帧、寄存器列表
  - `06_电机调试流程.md` - 连接设备、编码器校准、参数标定
  - `07_调试操作.md` - 各模式详细调试步骤
  - `08_固件升级.md` - 版本查看、串口/CAN 升级
  - `09_调试助手使用.md` - 达妙科技调试助手使用说明
- 清理临时文件：删除 `_pages_18_40/`、`extracted_pages/`、`电机调试指南.md`
- 更新 `hardware/reBot_B601_DM/Motor_Datasheets/README.md`，添加 Markdown 文档导航

**Why:** PDF 不方便 AI 阅读和查询，拆分成 Markdown 后可以快速定位到具体章节，方便调试时查阅。

---

## 2026-04-21

### 同步上游 Seeed-Projects/reBot-DevArm

- `git fetch upstream` + `git merge upstream/main`，merge commit `50a52ee`
- 上游 2 个新 commit（`4420c46..b837833`）：新增腕部相机支架
  - `hardware/reBot_B601_DM/3D_Printed_Parts/D435_Gemini2_Mount.step`（D435i / Gemini 2）
  - `hardware/reBot_B601_DM/3D_Printed_Parts/D405_305_Mount.step`（D405 / Gemini 305）
  - 配套效果图 3 张 + Star Arm 102 主臂图
  - README_zh / README_es 新增"可选硬件"章节（腕部相机 + 主臂选项）
- 新建 `上游更新记录/2026-04-21_同步详情.md` 记录详细内容
- `git push origin main` 推送到 fork

---

## 2026-04-17

### 同步上游仓库 Seeed-Projects/reBot-DevArm

- 修复 git 全局代理为 Clash 端口 `http://127.0.0.1:7890`（原 10808 失效）
- `git fetch upstream` 抓取上游 10 个新提交（b876c69..4420c46）
- `git merge upstream/main` 合并到本地 main，无冲突
  - merge 提交：`36c42ce`
  - 本地未提交修改先 stash，merge 后 pop 恢复
- 上游新增内容：
  - `hardware/reBot_B601_DM/3D_Printed_Parts/01_Joint6_7_Cable Restraint_A.step`（新增，关节 6/7 线缆约束件 A）
  - `hardware/reBot_B601_DM/3D_Printed_Parts/01_Joint6_7_Cable Restraint_B.step`（新增，关节 6/7 线缆约束件 B）
- 上游更新内容：
  - 多语言根 README（购买链接、leader arm 链接、路线图、Python SDK wiki、演示视频、商用条款/免责声明修正）
  - `hardware/reBot_B601_DM/readme*.md` 各语言版本同步更新
- 未影响 `software/reBotArm_control_py` 控制代码目录和 URDF，学习进度不受影响
- 尚未推送到 fork `origin/main`（等确认后再 push）

---

## 2026-04-12

### 1. 同步官方仓库
- `git pull origin main` 拉取最新代码（11个新提交）
- 主要变化：Python SDK 和 Pinocchio 标记为✅完成，指向外部仓库

### 2. 补充硬件资料
- 新建 `hardware/reBot_B601_DM/Motor_Datasheets/`
  - 下载 `DM-J4310-2EC_V1.1_Manual.pdf`（达妙小电机说明书）
  - 下载 `DM-J4340P-2EC_Manual.pdf`（达妙大电机说明书）
  - 编写 `README.md` 电机参数汇总 + CAN 协议整理
- 编写 `hardware/reBot_B601_DM/Hardware_Summary_zh.md` 硬件总结文档

### 3. 拉取软件仓库
- 新建 `software/` 目录
- 克隆 `MotorBridge`（github.com/tianrking/MotorBridge）→ `software/MotorBridge/`
  - 底层通用电机控制栈，Rust 核心 + Python/C++ 绑定
  - 支持达妙、RobStride、MyActuator、HighTorque、Hexfellow 五个品牌
- 克隆 `reBotArm_control_py`（github.com/vectorBH6/reBotArm_control_py）→ `software/reBotArm_control_py/`
  - 上层运动学控制库，基于 Pinocchio
  - 注意：代码在 **develop 分支**，main 分支只有一个空 README
  - 包含 URDF 模型、正逆运动学、轨迹规划、重力补偿、示例程序

### 4. 抓取 wiki 教程
- 用 OpenCLI 抓取 Pinocchio 教程页面 → `software/wiki_docs/`
- 原始 URL：https://wiki.seeedstudio.com/rebot_arm_b601_dm_pinocchio_meshcat/

### 5. 编写说明文档
- `PROJECT_NOTE.md` — 项目总览，文件结构说明
- `software/README_zh.md` — 软件部分详细说明 + 学习路径
- `software/architecture_diagram.md` — 控制架构说明（Mermaid 格式）
- `software/architecture_diagram.png` — 控制架构图（PNG 渲染）
- `software/architecture_diagram.drawio` — 控制架构图（可编辑 Draw.io 格式）

### 6. 环境调整
- `software/reBotArm_control_py/pyproject.toml` 中 `requires-python` 从 `==3.10.*` 改为 `>=3.10`
  - 原因：本机只有 Python 3.12/3.13，放宽版本限制
- 安装依赖：meshcat、numpy、pyyaml、matplotlib
- pinocchio (pin) 待安装

### 7. VS Code 插件
- 安装 `bierner.markdown-mermaid`（Mermaid 预览）
- 安装 `hediet.vscode-drawio`（Draw.io 可视化编辑）
- 以上插件装在 VS Code（F:\vscode\），不是 Cursor（D:\cursor\）

### 8. 文档体系建立
- 新建 `LEARNING_LOG.md` — 学习笔记（踩坑、理解、发现）
- 新建 `LEARNING_GUIDE_01.md` — 复现教程（环境搭建 + 运动学 + 电机控制）
- 确立 5 个文档的分工规则，写入 AI 记忆

---

## 2026-04-13

### 1. WSL2 Ubuntu 22.04 安装
- 目标：Pinocchio 在 Windows pip 编译卡死，改用 WSL2 Linux 环境
- 开启 WSL 功能：`wsl --install --no-distribution`，重启电脑
- 下载 Ubuntu 22.04 镜像：从微软官方下载 appx 包（1.1GB），需设置代理端口 7890
- 安装位置：**F:\WSL\Ubuntu**（C 盘空间不足，通过 --import 直接装到 F 盘）
- 安装步骤：appx → 解压 → 找到 install.tar.gz → `wsl --import` 到 F 盘
- 状态：✅ Ubuntu 已进入，Python 环境待配置

### 2. WSL2 Python 环境配置
- Ubuntu 自带 Python 3.10.12
- 用清华镜像安装全部依赖：`pip3 install pin numpy pyyaml matplotlib meshcat motorbridge -i https://pypi.tuna.tsinghua.edu.cn/simple`
- Pinocchio 3.9.0 安装成功（Linux 下有预编译二进制，秒装）
- 状态：✅ 环境就绪

### 3. 正运动学测试通过
- 运行 `python3 example/5_fk_test.py`
- 归零位置末端：X=0.253m, Y=0, Z=0.172m，URDF 模型验证正确
- 状态：✅ 通过

### 4. 逆运动学测试 — 修复官方 bug 后通过
- `example/6_ik_test.py` 原始代码有 bug，接口参数名与库不匹配：
  - `model=model` → 函数不接受此参数（内部自动加载）
  - `target_position` → 实际参数名为 `target_pos`
  - `target_rotation` → 实际参数名为 `target_rot`
  - 返回值字段 `converged` → 实际为 `success`
  - 返回值字段 `residual_trans/residual_rot` → 实际为 `error`
- 修复后测试：输入归零位置坐标 (0.253, 0, 0.172) → 解出 (0,0,0,0,0,0)，结果正确
- 状态：✅ 修复并通过

### 5. reBotArm_control_py 代码架构梳理
- 通读 develop 分支代码（main 分支是空的）
- 更新 `software/reBotArm_control_py_architecture.drawio` 架构图
  - 原图 dynamics 只画了 4 个功能框，实际有 7 个文件（补全 forward_dynamics / inertia / centroidal / derivatives / energy）
  - trajectory 子模块文件名修正为实际文件：sampler.py / clik_tracker.py / trajectory_planner.py
  - 补充 kinematics 和 dynamics **各有一个** robot_model.py 的说明
  - 硬件接口层细化：motorbridge.Controller + Mode 枚举 + 串口桥 + CAN
  - 增加外部依赖：motorbridge SDK、matplotlib
- 新建 `software/reBotArm_control_py_说明.md`
  - 一句话总结 + 目录结构 + 四个核心子模块详解（每个文件的核心函数）
  - 控制器 ArmEndPos 用法 + 三种控制模式对比（MIT / POS_VEL / VEL）
  - 示例程序索引（0~10）+ robot.yaml 配置说明
  - 完整调用链路（move_to_traj 为例）+ 外部依赖一览

### 6. 运行流程图（末端坐标 → 电机转动）
- 新建 `software/reBotArm_control_py_运行流程.drawio`
  - 回答「这个库怎么把 (x,y,z) 翻译成电机指令」的问题
  - 三条执行路径并列展示：
    - **路径 A（红）**：`move_to_traj()` 轨迹规划 — IK → FK → SE(3)测地线 → CLIK → _send_loop
    - **路径 B（蓝）**：`move_to_ik()` 直达模式 — 只做 IK，一步到位
    - **路径 C（绿）**：重力补偿 — MIT 模式 + g(q) 前馈力矩
  - 三条路径最终汇入 500Hz 公共控制循环 → MotorBridge → CAN → 电机
- 每个流程节点都标注了**源文件路径 + 函数名**，方便对照代码阅读
  - 例如 IK 步骤标注 `controllers/arm_endpos_controller.py → _solve_ik()` + `kinematics/inverse_kinematics.py → solve_ik()`
  - 重力计算标注 `dynamics/inverse_dynamics.py → compute_generalized_gravity()`
  - 底层指令标注 `actuator/arm.py → RobotArm.pos_vel() / mit() / get_state()`

### 7. MotorBridge 代码架构梳理
- 通读 MotorBridge 仓库（Rust workspace 多 crate 结构）
- 新建 `software/MotorBridge_说明.md`
  - 一句话总结 + 项目本质（Rust Cargo workspace）
  - 完整目录结构（motor_core / motor_abi / motor_vendors/{damiao,robstride,...} / bindings/{python,cpp}）
  - **五层调用栈** ASCII 图：用户代码 → Python 绑定 → C ABI → 厂商驱动 → 核心+硬件层 → CAN 总线
  - Python API 7 大块（创建 Controller / 添加电机 / 使能 / 4 种控制模式 / 读状态 / 寄存器 / 清理）
  - 达妙 9 款电机型号表（pmax/vmax/tmax，标注 reBot 用 4310 + 4340P）
  - 4 种控制模式对比表（MIT / POS_VEL / VEL / FORCE_POS）
  - 多线程模型说明（polling_thread 后台接收，缓存 + 直发互不干扰）
  - **与 reBotArm_control_py 的映射关系表**（RobotArm 方法 ↔ MotorBridge API）
  - 完整调用链示例（send_mit 6 层穿透到 CAN 帧）
  - 入口文件索引（按需读什么文件）
- 新建 `software/MotorBridge_architecture.drawio`
  - 5 层水平横条架构图（用户代码 / Python 绑定 / C ABI / 厂商 / 核心+总线 / 硬件）
  - 每层列出所有构成文件，并展开 damiao 子框（motor.rs/protocol.rs/controller.rs/registers.rs）
  - 层间用箭头标注转换方式（ctypes FFI / trait dispatch / CAN encode 等）
- 新建 `software/MotorBridge_运行流程.drawio`
  - 双列布局：**左列发送流（红，自上而下 10 步）** + **右列反馈流（蓝，自下而上 7 步）**
  - 每个步骤标注源文件路径 + 函数名（📂 风格与 reBotArm_control_py 流程图一致）
  - 中间共享状态缓存框（💾）连接两条路径（写：on_feedback；读：get_state）
  - 跨列耦合箭头：电机执行 → 反馈广播
  - 底部 4 条关键理解注释（异步、缓存、多线程安全、跨厂商通用性）

### 8. MeshCat 可视化三件套（无硬件学习）
- **环境修复**：WSL2 访问 Windows 文件时 Git symlink 失效
  - `urdf/reBot-DevArm_description_fixend` 在 Windows 下被存成文本文件（31 字节）
  - 手动在 WSL2 里重建软链接：`ln -s reBot-DevArm_fixend_description reBot-DevArm_description_fixend`
- 跑通 `example/sim/fk_sim.py`（正运动学可视化）
  - 输入关节角 → 浏览器 `http://127.0.0.1:7000/static/` 实时显示 3D 机械臂姿态
  - 测试归零位 `0 0 0 0 0 0` 和复杂姿态 `45 -30 15 -60 90 180`
- **改进 `example/sim/ik_sim.py`（逆运动学可视化）**
  - 原版问题：缺少目标点可视化，每次从零位出发导致不收敛
  - 改进 1：添加 `viz.show_ik_pose()` 调用，显示红球 + 三色坐标轴
  - 改进 2：保存 `q_current`，使用上一次的解作为下一次初值（连续求解策略）
  - 改进 3：增强调试输出，解析失败时显示原始输入
  - 测试结果：三个点全部收敛（`0.25 0.0 0.2` / `0.3 0.0 0.25` / `0.2 0.15 0.3`），迭代 9-12 次，误差 < 0.07mm
- **跑通 `example/sim/traj_sim.py`（SE(3) 测地线轨迹规划 + CLIK 跟踪）⭐**
  - 测试 1：`0.3 0.0 0.25` → IK 成功率 96.1%，误差 0.05mm，51 点 1.0s
  - 测试 2：`0.2 0.15 0.35 0 0.5 0.3`（含姿态）→ **IK 成功率 100%**，误差 0.057mm，121 点 2.4s，6 关节全动
  - 测试 3：`0.15 0.2 0.3` → IK 无解（超出工作空间）
  - 浏览器显示：灰色参考路径 + 绿色实际路径 + 机械臂动画回放
  - **这是 `reBotArm_control_py_运行流程.drawio` 里路径 A（move_to_traj）的无硬件版**
- 关键收获：
  - MeshCat = 浏览器里的"数字孪生"，无需硬件即可验证运动学算法
  - 零位末端位置 `(0.253, 0.000, 0.172)` m
  - 推荐工作半径 < 450mm（70% reach）
  - CLIK 跟踪精度 < 0.1mm，算法实现质量高

### 9. 新手友好文档编写
- 新建 `software/URDF_入门指南.md`
  - 面向机械工程师/硬件工程师的 URDF 快速入门
  - 用机械图纸类比解释 Link（零件）和 Joint（连接方式）
  - reBot-DevArm URDF 结构详解（6 个关节对应的实体零件和电机型号）
  - 关键参数解读（关节轴心位置、转动范围、扭矩限制、质量惯性）
  - 常见操作（修改关节限位、更新零件质量、添加末端工具）
  - 工具推荐（urdf_viz / RViz / check_urdf）
  - 常见问题排查（mesh 找不到、关节限位冲突、质量参数不准）
- 新建 `software/MeshCat_可视化指南.md`
  - 面向机械工程师/硬件工程师的 MeshCat 快速上手教程
  - 什么是 MeshCat（浏览器里的 3D 预览工具）
  - 工作原理（Python 后端 + WebSocket + Three.js 前端）
  - 三个可视化工具详解（fk_sim / ik_sim / traj_sim）
  - 浏览器操作指南（旋转/平移/缩放视角）
  - 常见问题排查（代理拦截、URDF 路径错误、WebGL 未启用、动画卡顿）
  - 高级技巧（录屏、自定义标记、多机器人同时显示）
- 新建 `software/reBot-DevArm_URDF详解.md`
  - 逐行解读 `reBot-DevArm_fixend.urdf` 文件（448 行完整注释）
  - 每个 link 的惯性参数含义（质量、质心、转动惯量）
  - 每个 joint 的关键参数（origin 位置、rpy 旋转、axis 转轴、limit 限位）
  - 为什么 joint2 的 `rpy="-1.5708 0 0"`（坐标系旋转 90°）
  - 为什么 joint2 的 `upper="0"`（机械限位，不能往上抬超过水平）
  - 为什么 link2 质量最重（1.327kg，包含大臂和肘关节电机）
  - 末端 end_joint 为什么是 `type="fixed"`（法兰固定连接）
  - 完整的机械结构对应关系（URDF 名称 ↔ 实体零件 ↔ 电机型号）
- **目标**：让不懂 ROS/Pinocchio 的硬件工程师也能快速上手可视化工具，看懂 URDF 每一行的物理含义

### 10. 文档归类整理
- 将新手文档从 `software/` 根目录移入 `software/docs/` 分类文件夹：
  - `docs/00_新手入门/` — URDF 入门指南、MeshCat 可视化指南
  - `docs/01_项目专用/` — reBot-DevArm URDF 详解
  - `docs/02_reBotArm_control_py/` — 运动学控制库架构说明、架构图、运行流程图
  - `docs/03_MotorBridge/` — 底层电机库架构说明、架构图、运行流程图
- 每个文件夹编写 `README.md` 索引
- 编写 `software/docs/README.md` 文档中心导航（3 条学习路径 + 速查表）
- 更新 `software/README_zh.md` 目录结构和学习路径（新增"第零阶段：看文档"）

---

## 2026-04-14

### 1. 抓取 Wiki 文档（OpenCLI）
- 修复 OpenCLI 使用问题：`generate/explore` 命令在当前环境报错，改用 `web read` 命令成功
- 正确命令：`opencli web read --url <URL> --output <DIR> --download-images true`
- 抓取以下页面到 `software/wiki_docs/`：

| 页面 | 大小 | 关系 |
|------|------|------|
| 达妙系列电机 | 41.5 KB | 直接相关 — 项目使用的电机 |
| RobStride 电机控制完整指南 | 20.2 KB | 直接相关 — RS 版本会用 |
| DM Gripper 夹爪组装指南 | 6.2 KB | 直接相关 — 末端执行器 |
| SoArm in LeRobot | 85.1 KB | 参考 — 路线图 LeRobot 适配 |
| StarAI Arm in ROS2 MoveIt | 7.7 KB | 参考 — 路线图 MoveIt2 适配 |
| ROS2 Humble 安装教程 | 3.8 KB | 参考 — ROS2 基础环境 |
| Robotics 总页面 | 5.4 KB | 索引 — 全部机器人资源导航 |

### 2. 电机 Wiki 双份存放
- 硬件相关的 wiki（达妙电机、RobStride、DM Gripper）复制到 `hardware/reBot_B601_DM/Motor_Wiki/`
- `software/wiki_docs/` 保留完整副本
- 两处各编写 `README.md` 索引文档

### 3. Wiki 文档格式修复
- OpenCLI 抓取的 HTML 表格全部变成了一行一个值的纯文本，代码块被压成单行
- 逐个修复全部 5 个有问题的文档：

| 文档 | 修复内容 |
|------|---------|
| 达妙系列电机 | 规格参数表拆成 3 个表格（基础规格/电气参数/控制参数），Python 代码（damiao_motor.py + damiao_test.py）恢复缩进 |
| RobStride 电机控制完整指南 | 5 个表格重建（电机型号/语言对比/CAN帧格式/性能基准/错误代码），30+ 处代码块恢复换行 |
| DM Gripper 夹爪 | 5 个 BOM 分类表格重建，警告块格式化 |
| StarAI Arm MoveIt | 规格表重建（3 款机械臂对比），C++ 代码恢复，shell 命令拆行 |
| SoArm LeRobot | 7 个表格重建，50+ 处代码块恢复，JSON 配置恢复缩进，警告块修复 |

- 同步修复后的电机文档到 `hardware/reBot_B601_DM/Motor_Wiki/` 副本

### 4. MeshCat 仿真代码详解文档
- 新建 `software/docs/01_项目专用/reBot-DevArm_MeshCat仿真代码详解.md`
- 对 4 个仿真文件逐函数解读：
  - `visualizer.py` — Visualizer 类初始化流程、6 个核心方法（update/show_ik_pose/draw_path/play_trajectory 等）
  - `fk_sim.py` — 正运动学可视化流程（输入关节角 → 计算末端位姿 → 浏览器显示）
  - `ik_sim.py` — 逆运动学可视化流程（输入目标位姿 → 解算关节角 → 连续初值策略）
  - `traj_sim.py` — 轨迹规划仿真流程（SE(3)测地线 → CLIK跟踪 → MeshCat动画回放）
- 包含调用关系图、关键参数说明、仿真 ↔ 真机代码映射表、核心概念速查
- 更新 `software/docs/01_项目专用/README.md` 索引和 `software/docs/README.md` 速查表及学习路径

### 5. Fork 到 GitHub + Git 操作指南
- 在 GitHub 上 Fork 官方仓库到 `fanhao375/reBot-DevArm`
- 配置本地 git remote：
  - `origin` → 个人 Fork（可推送）
  - `upstream` → 官方仓库（只读，用于同步）
- 配置 git 用户信息（user.name / user.email）
- 提交所有补充内容（160 个文件，12999 行新增）
- 推送到 GitHub（处理 rebase 冲突，同步官方 PR #13 的 Pinocchio 链接更新）
- 新建 `GIT_GUIDE.md` — 零基础 Git 操作指南：
  - 为什么要 Fork（对比直接下载/Clone 的优缺点）
  - 如何在 GitHub 上 Fork 仓库（图文教程）
  - 如何关联本地仓库到 Fork（origin/upstream 配置）
  - 如何提交和推送修改（add/commit/push 完整流程）
  - 如何同步官方仓库更新（pull upstream + push origin）
  - 常见问题排查（换行符/嵌套仓库/认证/撤销提交）
  - 开源协议注意事项（CC BY-NC-SA 4.0 解读）
  - 推荐工作流程（日常开发/定期同步/分支开发）


---

## 2026-04-16

### 1. 安装可视化 Skills（markdown-viewer/skills）
- 克隆 `https://github.com/markdown-viewer/skills` 到 `/tmp/markdown-viewer-skills`
- 软链接 4 个有用的 skills 到全局目录 `~/.claude/skills/`：
  - `uml` — UML 类图、序列图、活动图（PlantUML）
  - `architecture` — 分层架构图（HTML/CSS 嵌入）
  - `infographic` — 流程图、时间线、对比图（YAML 语法）
  - `mindmap` — 思维导图（PlantUML）
- 目的：以后读代码时可以快速生成可视化图表

### 2. 重力补偿代码解析文档
- 新建 `software/docs/02_reBotArm_control_py/重力补偿详解/` 目录
- 生成 4 个文档：
  - `README.md` — 完整教程（7789 字节）
    - 什么是重力补偿（问题 + 解决方案）
    - 物理原理（动力学方程 τ = M·q̈ + C·q̇ + g(q)）
    - 数学推导（RNEA 算法 + MIT 控制律）
    - 代码架构（4 层架构）
    - 代码详解（核心代码逐行解释）
    - 实战示例（9 号和 10 号例程）
    - 常见问题（FAQ）
  - `architecture_代码架构.md` — 4 层架构图（HTML/CSS）
    - 业务层（蓝色）：9/10 号例程
    - 封装层（紫色）：dynamics API
    - 计算层（绿色）：inverse_dynamics + inertia
    - 底层（红色）：Pinocchio
  - `sequence_函数调用序列.md` — UML 序列图
    - 用户代码 → 控制循环 → API → 计算层 → Pinocchio
    - 500Hz 循环的完整调用链
  - `control_loop_控制循环流程.md` — Infographic 流程图
    - 5 个步骤：读取关节位置 → 计算重力补偿 → MIT 控制 → 打印力矩 → 循环

### 3. 文档结构统一规范
- 更新 `software/docs/README.md` 速查表，加入重力补偿详解
- 更新 `software/docs/02_reBotArm_control_py/README.md`：
  - 文档列表加入重力补偿详解/
  - 学习路径分为路径 1：运动学入门和路径 2：动力学进阶
- 文件命名规范：统一使用英文_中文格式（如 `sequence_函数调用序列.md`）
- 确保所有代码解析文档都在 `software/docs/` 下，按模块分类

### 4. 同步 upstream 更新
- 配置 git 代理（端口 10808）
- `git fetch upstream` 发现 2 个新提交：
  - `b876c69` Merge pull request #15 from tianrking/patch-1
  - `c3cee75` Create DISCLAIMER.md（免责声明文档）
- DISCLAIMER.md 内容：
  - 产品定位：DIY 套件，需自行组装调试
  - 安全风险：高速高扭矩，可能夹伤、撞击、砸伤
  - 责任划分：厂家只对出厂材料缺陷负责，用户自行组装错误/超载/改装等导致的事故不负责
  - 禁止场景：医疗、生命支持、载人、水下、工业高危等
  - 免保条款：拆机、改装、进水、过保、无发票等不保修

### 5. 修复图表渲染问题（PlantUML/Infographic → Mermaid）
- 问题：VS Code 中除 `architecture_代码架构.md`（HTML/CSS）外，PlantUML 序列图和 Infographic 流程图都无法直接渲染（需要额外插件/浏览器扩展）
- 解决：利用已安装的 `bierner.markdown-mermaid` 插件，把两个不渲染的图改写为 mermaid 格式
- 改动文件：
  - `sequence_函数调用序列.md` — PlantUML `@startuml` → `mermaid sequenceDiagram`
    - 保留 actor / participant / activate / Note 语义
    - 末尾追加一个简化的 `flowchart LR` 调用层次图
  - `control_loop_控制循环流程.md` — Infographic YAML → `mermaid flowchart TD`
    - 5 个步骤节点 + 循环判断
    - 用 `classDef` 着色：初始化（蓝）/ 循环体（黄）/ 计算（红）/ 控制（绿）/ 结束（紫）
- 选型理由：mermaid 在 VS Code、GitHub、Typora 都原生支持，跨平台一致；HTML/CSS 架构图也保留（universal 渲染）

### 6. 建立 Codex 本地项目记忆
- 新建本地目录 `.codex/`，用于存放 Codex 专用项目记忆快照
- 新建 `.codex/PROJECT_MEMORY_项目记忆.md`
  - 来源：`小红书进度/AI_HANDOFF_AI接手指南.md`
  - 同步内容：
    - 项目定位与当前阶段
    - 文档体系分工与自动更新规则
    - 环境约束（VS Code 路径、Pinocchio/WSL2、Git 代理 10808）
    - 图表规范（首选 Mermaid，其次 HTML/CSS）
    - 小红书子项目分离规则与工具链约定
    - OpenCLI 用法、命名规范、常见翻车点
- 更新 `.gitignore`：忽略 `.codex/`，确保该记忆文件只做本地使用、不上传 GitHub
- 更新 `PROJECT_NOTE.md`：登记 `.codex/` 本地工具目录和本次新增记忆文件

### 7. 补齐重力补偿仿真版（example/sim/gravity_sim.py）
- 背景：`example/sim/` 下原有 fk_sim / ik_sim / traj_sim 三件套（对应作者真机例程 5/6/8 号），唯独 9/10 号重力补偿没有仿真版
- 新建 `software/reBotArm_control_py/example/sim/gravity_sim.py`（约 170 行）
  - 对应真机例程：`example/9_gravity_compensation.py`
  - 模式参考：`example/sim/fk_sim.py`（交互输入关节角）
  - 复用 `example/sim/visualizer.py` 的 `Visualizer` 类，通过其 `meshcat` 属性扩展自定义节点
- 功能：
  - 交互输入 6 个关节角（度）→ MeshCat 实时显示姿态
  - 在每个 link 质心位置画**红色球**（半径 ∝ √质量）
  - 从每个质心向下画**绿色重力线段**（长度 ∝ m·g）
  - 终端打印 6 个关节的 τ_g（带条形图，附 max 和 Σ|τ_g|）
- 关键 Pinocchio API：
  - `model.inertias[i].mass` / `.lever` — link i 的质量和质心（局部坐标）
  - `data.oMi[i].act(lever)` — 质心转世界坐标
  - `compute_generalized_gravity(model, q, data)` 内部已更新 `data.oMi`，无需重复 forwardKinematics
- 后续：跑通后整理小红书素材（推荐姿态对比图：归零 vs 大臂水平 vs 折叠）

### 8. 修复 gravity_sim.py 上游 API 不兼容
- 症状：`get_gravity(model)` 报 `AttributeError: 'numpy.ndarray' object has no attribute 'x'`
- 根因：上游 `dynamics/robot_model.py:133` 用 `g.linear.x/.y/.z` 取分量，这是 Pinocchio 2.x 的 API；新版 3.9.0 已把 `pin.Motion.linear` 改成直接返回 `numpy.ndarray`
- 处理：**不动上游**（避免和官方分叉），在 `gravity_sim.py` 里用 `np.asarray(model.gravity.linear)` 直接读
- 附记：这类 `Motion.linear.x` / `Placement.translation.x` 的老 API 在上游可能还有，遇到再绕

--- 


---

## 2026-04-22

### 1. 电机说明书版本整理
- 新增最新版说明书 PDF：
  - `DM-J4310-2EC V1.2减速电机说明书V1.2定稿.pdf`（2025.11，对应关节 2-5 小关节）
  - `DM-J4340-2ECV1.1减速电机说明书 V1.1 定稿.pdf`（2025.11，45 页完整版，对应关节 1 和肩部大关节）
- 旧版 PDF 归档到 `Motor_Datasheets/旧版/`（V1.0/V1.1 保留备查）
- 更新 `Motor_Datasheets/README.md`：文件名、章节标题同步为最新版本号
- 用 pypdf 验证新版 PDF 比旧版内容更完整（J4340：14页→45页，版本 V1.0→V1.1）

### 2. 新建遥操作系统目录
- 在项目根目录新建 `遥操作/` 文件夹
- 以 git submodule 方式引入 [servodevelop/Star-Arm-102](https://github.com/servodevelop/Star-Arm-102) → `遥操作/StarArm_102/`
- 编写 `遥操作/README.md`：遥操作概念说明、Star Arm 102-LD 与 reBot Arm B601 的主从关系、LeRobot 数据采集工作流、submodule 初始化命令
- 更新 `PROJECT_NOTE.md` 项目结构树，添加 `遥操作/` 条目

---

## 2026-04-23

### 1. 五个自建文档重命名为中文
- `CHANGELOG.md` → `操作日志.md`
- `PROJECT_NOTE.md` → `项目总览.md`
- `LEARNING_LOG.md` → `学习笔记.md`
- `LEARNING_GUIDE_01.md` → `复现教程_01.md`
- `GIT_GUIDE.md` → `Git操作指南.md`
- 用 `git mv` 保留历史，同步更新 `software/docs/README.md`、`software/docs/00_新手入门/README.md`、`上游更新记录/README.md`、`Git操作指南.md` 里的所有引用

### 2. 新增重力补偿锁止仿真（教学解剖版）
- 新建 `software/reBotArm_control_py/example/sim/gravity_lock_sim.py`
- 对应真机例程 `10_gravity_compensation_lock.py`（末端速度锁止版重力补偿）
- 不做动力学积分，而是"静态解剖式"把 10 号的控制律分 5 步打印：
  1. 雅可比 J(q) 形状
  2. 末端线速度 v_ee = J[:3] · q̇ 和角速度 w_ee = J[3:] · q̇
  3. 阈值判断（V_TH=0.04 m/s, W_TH=0.08 rad/s）
  4. LOCKED 保持 q_target / UPDATE 跟随当前 q
  5. 最终 τ = τ_g + kp·(q_target - q) + kd·(0 - q̇)，每项带条形图
- MeshCat 可视化：末端线速度蓝色箭头 + 角速度橙色箭头 + 锁止状态球（绿=LOCKED，红=UPDATE）
- 4 个预设场景（s0 归零静止 / s1 微扰动 / s2 人在推 / s3 末端快转）

### 3. 同步上游 2026-04-23
- 上游 commit 范围：`b837833..33f57b2`（6 个 commit），本地 merge commit：`1c1bfe1`
- 核心更新：官方新增「深度相机视觉抓取 Demo」状态行（YOLO + 深度相机）
- D405 相机支架 STEP 小修订；4 个翻译版 README 的 Pinocchio 链接换成 GitHub 仓库地址
- 新建 `上游更新记录/2026-04-23_同步详情.md` 记录详细内容
- 更新 `项目总览.md` 官方更新进度表：组装视频/腕部相机支架/视觉抓取 Demo 标 ✅

### 4. 复现教程补充 WSL2 USB 直通章节
- 在 `复现教程_01.md` 第五步前新增 5.0 小节「WSL2 访问 USB 设备（一次性配置）」
  - 5.0.1 用 `winget install dorssel.usbipd-win` 安装微软官方 USB passthrough 工具
  - 5.0.2 插 USB 后的 `usbipd bind/attach` 两步
  - 5.0.3 拔 USB 前的 `detach`（可选但推荐）
- 同步更新 5.1 硬件连接，补充两条可选路径：
  - 路径 A: 达妙 USB2CAN + MotorBridge（Python SDK，跑真机例程）
  - 路径 B: 创芯 CANalyst-II + ECAN Tools（Windows GUI 验货）
- 实际验证：本机 usbipd-win 已装（v5.3.0），等达妙 USB2CAN 到货后走完整流程

### 5. 事后修正（2026-04-23 晚）
**起因**：用户指出上述第 2、3 条的文档结论写得比证据更满，存在 5 处实质错误。

**修正内容**：
1. **gravity_*_sim.py 未在主仓库**
   - 问题：`操作日志.md` 和 `项目总览.md` 把这两个脚本写成"已新增"，但它们只在 `software/reBotArm_control_py` 未提交工作区里（`??` 状态），主仓库 gitlink 仍指向 962cc8d，别人 clone 拿不到
   - 修正：移动到主仓库 `software/sim_extras/`，作为正式跟踪文件；新建 `software/sim_extras/README.md` 说明这是自建学习脚本目录；修改两个脚本的 `sys.path` 和用法注释

2. **视觉抓取 Demo 链接 404**
   - 问题：`项目总览.md`、`上游更新记录/2026-04-23_同步详情.md` 把"深度相机视觉抓取 Demo"写成已完成并指向官方 wiki，但中英文链接都返回 404
   - 修正：`项目总览.md` 把该行状态改回 🚧，注明"README 加了链接但 wiki 页面 404，疑似抢发"；`上游更新记录/2026-04-23_同步详情.md` 重写，删除所有推断（"Eye-in-Hand 路径彻底跑通""技术栈 = YOLO + 深度图反投影""vectorBH6 是事实上的上游""不需要 ROS2"），只保留事实（"官方在 README 加了这行 + 链接 404"）

3. **中英文 README LeRobot 状态矛盾**
   - 问题：英文 README.md L96 标 ✅ Completed，中文 README_zh.md L94 标 🚧 进行中，`项目总览.md` 按中文汇总但没说明
   - 修正：`项目总览.md` 标题改为"按中文 README_zh.md 汇总"，新增警告框说明中英文冲突、以中文版为准

4. **WSL USB 教程表述不够严谨**
   - 问题：`复现教程_01.md` L285 说"换 USB 口也不用重新绑"（官方文档只说 per-device 持久化，没承诺跨 BUSID）；L291 用 `/dev/ttyACM*/ttyUSB*` 作为通用验证方法（只适合串口类设备，不适合相机）
   - 修正：L285 改为"per-device 持久化，但换 USB 口 BUSID 会变，需重新 bind"；验证方法改用 `lsusb`（通用），并注明串口类设备可额外检查 `/dev/tty*`、相机类设备不会出现在 `/dev/tty*` 里

5. **记忆系统新增纪律**
   - 新增 UTF-8 验证规则：Edit 工具改中文文件后必须立刻 `grep "�"` 验证，不等用户发现（本次修正过程中又引入 2 处乱码，已修复）

**反思**：这几处都犯了同一类错——把"官方 README 新增一行"当成"官方已交付该功能"，没点开链接验证，也没对照其它语言。下次见到"✅ + 外链"至少 curl 确认一下。

### 6. 二次修正（2026-04-23 晚）
**起因**：用户指出上述修正中仍有 4 处文档口径问题。

**修正内容**：
1. **视觉抓取 Demo wiki 已上线**：重新验证链接返回 200，更新 `项目总览.md` 和 `上游更新记录/2026-04-23_同步详情.md`，改回 ✅ 完成
2. **08 号算法编号错误**：`学习笔记.md` 里把 08 号标成"固定目标重力补偿"，但实际 08 号是 `8_arm_traj_control.py`（轨迹控制），已删除错误对比行并加注释
3. **积分项表述修正**：`学习笔记.md` 里"误差 1rad 每秒累积约 1.0 N·m"有误，实际是每个控制周期（2ms）累积，已改为"每个控制周期累积"
4. **操作日志表述修正**：上条"新建 `feedback_utf8_verification.md`"改为"新增 UTF-8 验证规则"（该文件未实际创建）

**反思**：第一次修正时过于匆忙，没有重新验证网络链接状态，也没有仔细核对 example 文件编号。修正文档时也要保持严谨。

### 7. 用 OpenCLI 补抓新上线 wiki（2026-04-23 夜）
**起因**：官方中文 wiki 已上线，需要把新页面离线归档进 `software/wiki_docs/`，同时把正确的 `opencli` 用法记住。

**执行结果**：
1. **确认 opencli 正确用法**
   - 参考项目本地记忆和 OpenCLI 官方仓库说明，`web read` 是正确命令；`Browser Bridge not connected` 是它定义的常见报错
   - 在当前 Windows + Codex 环境里，直接 PowerShell 调 `opencli` 不稳定，改为用 Git Bash 进入 `/c/Users/12440/AppData/Roaming/npm` 执行 `./opencli web read ...`

2. **新增两份离线 wiki 快照**
   - `software/wiki_docs/reBot_Arm_B601-DM_Visual_Grasping_Demo/`
   - `software/wiki_docs/reBot_Arm_B601-DM_in_Lerobot/`
   - 两份文档都保留了本地 `images/` 资源，断网可读

3. **整理归档结构和索引**
   - 拍平 OpenCLI 自动生成的双层目录，保持 `wiki_docs/` 下每篇文档一层目录的现有约定
   - 更新 `software/wiki_docs/README.md`，补充两份新文档并把“最后更新”改为 `2026.04.23`

**反思**：以后在这台机器上用 OpenCLI，优先沿用已验证的 Git Bash 路径和命令模板，不再临时猜 PowerShell 调法。

### 8. 正规化 software 外部仓库为正式 submodule（2026-04-23 夜）
**起因**：`software/MotorBridge` 和 `software/reBotArm_control_py` 之前虽然在索引里是 gitlink，但 `.gitmodules` 没有映射，属于“半挂状态”；clone / pull / 状态判断都容易让人误解。

**处理内容**：
1. 在根目录 `.gitmodules` 中补充：
   - `software/MotorBridge` → `https://github.com/tianrking/MotorBridge.git`（`main`）
   - `software/reBotArm_control_py` → `https://github.com/vectorBH6/reBotArm_control_py.git`（`develop`）
2. 执行 `git submodule absorbgitdirs`，把两个目录从“各自带 `.git/` 的嵌套仓库”收口为主仓库统一管理的标准 submodule 结构
3. 更新 `项目总览.md`、`software/README_zh.md`、`Git操作指南.md`，统一说明：
   - 这是外部仓库，不是官方主仓库原生代码
   - 但当前项目已经以正式 submodule 方式引用它们
   - clone / pull 后需要 `git submodule update --init --recursive`

**边界说明**：
- `software/reBotArm_control_py` 本机还有一个本地提交 `822d342`（清理 `.pyc` + 补 `.gitignore` + 保留有效源码修正）
- 因为还没有你自己的公开 fork 来承接这个提交，所以本次**不更新父仓库 gitlink 到该提交**，只先把 submodule 结构正规化，保证远端可复现

**反思**：以后引入外部仓库，要么一开始就用正式 `git submodule add`，要么彻底 vendoring；不要再长期维持“索引是 gitlink、但 `.gitmodules` 没登记”的中间态。

### 9. 为 reBotArm_control_py 建立个人 fork 并闭环 submodule 更新（2026-04-23 夜）
**起因**：`software/reBotArm_control_py` 已有本地修复提交，若不为子仓库建立可公开访问的远端，主仓库无法安全引用该提交。

**处理内容**：
1. 通过 GitHub API 创建 `fanhao375/reBotArm_control_py`，作为 `vectorBH6/reBotArm_control_py` 的个人 fork
2. 将子仓库 remote 规范为：
   - `origin` → `https://github.com/fanhao375/reBotArm_control_py.git`
   - `upstream` → `https://github.com/vectorBH6/reBotArm_control_py.git`
3. 发现个人 fork 的 `develop` 已领先于本地旧基线 `962cc8d`，因此将本地清理提交重放到 fork 的最新 `develop` 上：
   - 原本地提交：`822d342`
   - 重放后提交：`a5078ae`
4. 将 `a5078ae` 推送到个人 fork 的 `develop`
5. 更新主仓库 `.gitmodules`，让 `software/reBotArm_control_py` 正式指向个人 fork；随后父仓库可安全更新 gitlink 到 `a5078ae`

**结果**：
- `software/reBotArm_control_py` 的 `.pyc` 清理和本地修复现在已是远端可下载状态
- 主仓库不再依赖“只有本机才有”的子仓库提交
- 以后修改该子仓库的标准流程是：先改 fork，再回主仓库更新 submodule 指针

### 10. 在 项目总览.md 顶部补 AI 接手提示（2026-04-23 夜）
**起因**：希望后续接手的 AI 不用翻长日志，也能一眼看懂 submodule 结构和更新规则。

**补充内容**：
1. 在 `项目总览.md` 顶部新增 `AI 接手提示`
2. 明确三个子模块的：
   - 主要来源仓库
   - 当前 submodule URL
   - 是否只读跟踪 / 是否通过个人 fork 承接修复
3. 明确关键规则：
   - clone 要带 `--recurse-submodules`
   - pull 后要跑 `git submodule update --init --recursive`
   - 主仓库锁定的是子模块提交号，不是分支名
   - `reBotArm_control_py` 如需同步源码上游，先看 `upstream/develop`

**结果**：以后别的 AI 只要先读 `项目总览.md`，就能知道这块不是普通目录，避免再把 submodule 当成普通文件夹乱改。

---

## 2026-04-26

### 同步上游：reBot v1.0 → v1.1 重大版本更新

- 上游 commit 范围：`33f57b2..c337d44`（2 个 commit），本地 merge commit：`c3e730e`
- 官方 changelog（4 点）：
  1. 末端 3 个关节加线束卡口（防松防脱落）
  2. 关节 1 电机型号修正：4310 → 4340P
  3. 底部新增 CNC 件 `02_Base_Reinforcement_Part.step` 加强强度
  4. 新增线束卡扣
- 整机文件改名：`reBot_B601_DM_v1.0_20260331.step` → `reBot_B601_DM_v1.1_20260425.step`
- 新增零件：`02_Rack` / `03_Link1~5` / `Gemini2_mount` / `UVC32_mount` / 3 个 Cable Restraint
- BOM 关键变化：齿轮内孔 5 → 内孔 8、螺丝体系细分（HM/KM 多型号）、铜柱 M4*50 取消、定位销 M4*8 → M4*7
- 5 语言版 README 全更新；新增 `media/v1.1.png` 和 `v1.1_2.png` 整机预览图
- 详细见 `上游更新记录/2026-04-26_同步详情.md`

**对学习项目的影响**：
- 算法层（URDF / 运动学 / 控制代码）不受影响
- 硬件复刻路径需重新规划：齿轮规格变了必须按 v1.1 买、3D 打印件需补打 5 个 Cable Restraint、CNC 件需补加工底座加强件
- 已下单的 3×4340P + 4×4310 电机配置不变（v1.1 修正后 j1 也是 4340P）

---

> ⚠️ **2026-05-05 ~ 2026-05-31 的操作记录补登说明**：这段时间的操作（多次上游同步、Python wheel 升级、装机阶段、LeRobot follower 验证等）当时记在了 `项目总览.md` 的「更新记录」表格里，本操作日志一度断更。自 2026-06-01 起回归原始规则，操作记录恢复登记于本文件。历史明细见 `项目总览.md` 更新记录表 + `上游更新记录/` 各同步详情。

## 2026-06-01

### 1. 从 fork 同步本地（拉另一台电脑的进度）
- `git pull` 拉下另一台电脑 push 到 `origin/main` 的 2 个 commit（`2d9c99e` / `30f0bb1`）：LeRobot follower 验证小白手册 + 重力补偿小白手册 + `tools/lerobot_b601_keyboard_jog.py`，快进同步无冲突。
- 清理 submodule `reBotArm_control_py` 内一个 `.pyc` 噪声（`git checkout` 还原）。

### 2. 提交《AI机器人路径选择扫盲》PDF 并 push fork
- `git add` 根目录 `AI机器人路径选择扫盲.pdf`（882K，配套同名 md），commit `111fd9b`。
- `git push origin main`：首次网络中断（curl 55 Connection reset），`git config http.postBuffer 524288000` 后重试成功。

### 3. 上游同步：DM 电源组装资料 + 摄像头支架 STEP 修正
- 上游 `af0bb58..6f7b33f`（5 commit，全在 `hardware/reBot_B601_DM/`）：摄像头支架 4 STEP 重导出 + 删冗余 UVC32；社区 PR #33（LAN-GER）DM 电源组装资料（电源外壳 STEP×3 + 外购件图×7 + 渲染图×3 + readme 电源章节）。
- ⚠️ **本地化处理**：组装实拍图 `Assembly_Steps/powerstep_images/`（20 张约 94MB）只留本地、不上传 GitHub（加 `.gitignore` + readme 注明在线版见上游）；剔除上游误提交的 `.obsidian/`。
- **技术关键**：用 squash 单亲提交而非 merge，避免上游 commit 成祖先把 94MB 大图带进可推送历史（实证 `git rev-list --objects main` 0 个 powerstep 对象，push 体积 ≈7.78MB）。commit `b1f606b`，文档 commit `8517f45`，打 `baseline-2026-05-31` tag，已 push。
- ⭐ 同步前 spawn subagent 复核影响（风险低）。详见 `上游更新记录/2026-05-31_同步详情.md`。

### 4. 记录引导臂选型落定（遥操作主线决策）
- 用户决定 leader 做 **reBot Arm 102**（舵机到货 + 自己 3D 打印结构件，进入装配）。
- 更新 `遥操作与LeRobot待办.md`：§1/§2/§4 标记已决策，新增 §3.7（官方装配教程未发布）、§3.8（跨系统遥操作 102 leader→B601-DM follower 适配器配对待确认）。commit `70dd56a`。
- 同步更新 memory `project_teleop_leader_choice_risk.md` + `MEMORY.md`。

### 5. 记录摄像头选型结论
- 调研结论：LeRobot AI 主线买**普通 RGB USB 摄像头 ×2**（腕部小模组 + 俯视罗技）即可，**深度相机非必需**（只有官方 YOLO 抓取 demo 才用深度）。
- 更新 `遥操作与LeRobot待办.md`：新增 §3.9（完整论证 + 购物清单），阶段4/支线1 定调。commit `6bccece`。
- 详细原理同步登记到 `学习笔记.md`（见 2026-06-01 条）。

### 6. 文档体系合规补登
- 按项目规则回归：操作记录补登本文件（操作日志），原理理解补登 `学习笔记.md`。

## 2026-06-07

### 1. 云端/本地双向同步对齐
- 检查发现分叉：本地领先 3 个文档 commit（102 选型 / 摄像头选型 / 合规补登），云端领先 1 个 commit `44a123c`（另一台电脑推的《LeRobot_Arm102LD_B601DM遥操作小白执行手册.md》，698 行——正是 §3.8 跨系统遥操作主题）。
- 两边文件零重叠，`git merge origin/main` 干净合并（merge commit `3daacde`），`git push origin main` 推 4 个 commit，云端本地完全对齐。
- 🟡 待办：读新手册，把 `遥操作与LeRobot待办.md` §3.8 的"适配器配对待确认"更新成实际结论。

### 2. 摄像头最终采购决定（推翻"只买 USB"，两条路都玩）
- 用户决定深度 + USB 都买：**Orbbec Gemini 2 已下单（¥1750 实付，天猫奥比中光官方旗舰店，3D 双目结构光）** + 普通 USB 摄像头（俯视，型号待补）。
- Gemini 2 = 官方 YOLO 抓取 demo 同款 + 仓内现成腕部支架 `D435_Gemini2_Mount.step`；计划装腕部（eye-in-hand），其 RGB 流兼当 LeRobot 腕部相机；USB 当俯视。
- 更新 `遥操作与LeRobot待办.md`：§3.9 加"最终采购决定"、阶段4 定调改为实际采购、支线1 表格标注已下单/不买项。

### 3. 上游同步：三仓全部更新到最新（用户拍板"全部更新"）
- ⭐ spawn subagent 复核三仓影响后执行。走 `sync/upstream-2026-06-07` 分支，验证通过 ff 合入 main，打 `baseline-2026-06-07` tag。
- **主仓** `6f7b33f..5606170`（11 commit，squash 单亲法）：README 社区贡献表 + 4 张社区图 + DM 电源上盖/滑盖 STEP XT60E 母座内移修正（打印用此版）。⚠️ 永久约束确认：主仓历史含 94MB blob，以后一律 squash、不可普通 merge upstream。
- **MotorBridge v0.3.9 → v0.4.5**（27 commit）：达妙模式写入重试增强 + `request_fresh_state`；dm-device 可选传输后端复核为**非强制依赖**（wheel 不打包、现有路径零影响）；API 100% 兼容。fork 已 fast-forward push。
- **reBotArmController_ROS2 → c6a4e68**（5 commit）：MoveIt2 Humble 修复 + `fresh()` 用法修正。fork 已 push。
- **Python wheel 升级**：Windows `E:\Python312` 0.3.9→0.4.5 ✅（import + Controller/Mode/add_damiao_motor/from_dm_device 验证过）；WSL 系统 python3 0.3.9→0.4.5 ✅；**LeRobot 环境（miniforge3 conda env `lerobot`）查实在另一台电脑上（本机 WSL 无 conda 无校准缓存），待在那台 pull 后 `conda activate lerobot && pip install -U motorbridge==0.4.5`**。
- ⚠️ 硬件未验证：下次真机上电跑重力补偿确认 0.4.5 正常；异常回滚 `pip install motorbridge==0.3.9` + `baseline-2026-05-31` tag。
- 详见 `上游更新记录/2026-06-07_同步详情.md`。

---

## 2026-06-11

### 上游同步：三仓全部更新到最新（用户拍板"都同步"）
- ⭐ 同步前 spawn 3 个独立 subagent 分仓复核影响（主仓 / MotorBridge / control_py），结论均回写详情文件；subagent 纠正我一处背景误判（control_py develop 与 main 是真分叉，非单纯领先）。
- 走 `sync/upstream-2026-06-11` 分支，3 个源码 commit + 1 个 docs commit，ff 合入 main，打 `baseline-2026-06-11` tag。
- **主仓** `5606170..1cf0ec9`（13 commit，squash 单亲 `4a5210f`）：README 加 reSpeaker 语音集成行 + 社区贡献表重排（gamepad IK 遥操作 / D405 eye-in-hand TF）；⭐ 柔性夹爪 2 STEP 优化重导出；XT30U→XT60 外购件图 + 电源 BOM 行。**本地化**：用「无本地改动文件直接取 upstream 版 + 2 readme 只打上游 delta 补丁」精准法，powerstep 实拍图未上传注记完整保留；实测可推送历史 0 个 powerstep 大 blob。⚠️ 永久约束：主仓不可普通 merge upstream。
- **MotorBridge** v0.4.5 → `53fe6dd`（指针 `94d5812`）：ws_gateway `handle_damiao_state_many` 改先 `get_motor` 复用避免重复注册撞 ID。达妙核心+bindings 零改动、无版本 bump → **wheel 不用升**，仅更新指针。
- **reBotArm_control_py** → `0163c08`（指针 `f205358`）：追 develop 策略不变；develop 无新上游提交，但在 fork develop **cherry-pick 2 个 main bugfix**（`36d4346` URDF join3→joint3、`0163c08` get_joint_limits 改按 idx_q）。subagent 实测零冲突、对当前重力补偿/IK 实跑无影响（预防性）。fork develop 现比 upstream/develop 多 2 个自定义 commit。
- **ROS2 / StarArm_102**：无新（102 装配教程仍未发布）。
- ⚠️ **三个远端 push 待用户确认**：主仓 origin、MotorBridge fork origin/main、control_py fork origin/develop。硬件未验证（下次上电确认）；回滚点 `baseline-2026-06-07`，wheel 本次未动无需回滚。
- 详见 `上游更新记录/2026-06-11_同步详情.md`。
- ✅ 已 push：主仓 `9984906..23b7a3f` main + `baseline-2026-06-11` tag；MotorBridge fork `73443c7..53fe6dd`；control_py fork `062bef9..0163c08`。三端已对齐。

### 遥操作待办新增：纯仿真遥操作支线（手柄+IK）+ D405 eye-in-hand TF 参考
- 由上游同步进来的社区贡献（@danieldoradotalaveron-rb）引出讨论，记进 `遥操作与LeRobot待办.md`：
  - 新增「平行支线 3：纯仿真遥操作（无硬件学习路径，候选）」——手柄+IK 在 RViz 跑、仅仿真；定位为 102 物理遥操作前的预热 + 小红书选题；前提是先立 ROS2 Humble + 手柄 + 社区实验代码；标 🟡 候选先占位。
  - 支线1（摄像头）补 D405 eye-in-hand TF 交叉引用（跟 Gemini 2 装腕部同思路，ROS2 腕部视觉可借鉴）。

### 102 主动臂烧录：逆推 ID 对应 + 自建烧录指南
- 用户告知 **102 结构装配已完成（2026-06-11）**，问舵机烧录 ID 对应，官方无教程。
- **从代码逆出完整 ID↔关节↔B601 对应**（三处 102 代码 + 两仓 B601 配置交叉验证）：102 舵机 ID **0~6**（型号 rx8-u50，波特率 1M），对应 B601 达妙 **0x01~0x07**，规则 **102 ID = B601 ID − 1**（102 从 0、B601 从 1 编号）；两边都用 joint1-6+gripper 命名。
  - 102 来源：`Python_SDK/stararm102_ro.py`、`ROS2_HUMBLE/robo_driver.py`、`Lerobot/.../stararm102_leader.py`；B601 来源：`reBotArm_control_py/config/arm.yaml` + `gripper.yaml`。
- **查证教程出处**：Seeed 的 102 整臂 wiki（StarAI Arm in LeRobot）+ 仓内文档 **均无烧 ID 内容**（只有 `lerobot-calibrate` 校准）；**权威资料在 FashionStar 厂商 wiki**——`舵机离线ID设定、零位设置`（硬件设定板，长按按钮）+ `舵机上位机常用功能操作手册`（软件 GUI 写 ID + 改波特率）。⚠️ 一次只能连一颗舵机。
- **新建 `102主动臂烧录指南.md`**（根目录，跟 `装机烧录指南.md` 平行）：🟢 ID 对照表/参数 + ⚠️ 五个坑（**0-6 不是 1-7**、波特率 1M、单个烧、达妙/FashionStar 工具不通用、夹爪方向反）+ 烧录工具/流程/验证/参考链接（含 FashionStar wiki URL）。
- 交叉引用更新：`遥操作与LeRobot待办.md` §1.1（102 装配完成→进入烧录）+ §3.7（指向新指南）；`项目总览.md` 结构图登记新文档。
- 🟡 待实操确认：RA8 出厂默认 ID/波特率（FashionStar wiki 未写）、选定烧录工具、是否需显式标零点。

### 深度相机 Gemini 2 到货：建 YOLO 视觉抓取上手指南
- 用户告知 **Orbbec Gemini 2 已到货（未上电）**，定调"两条都跑、深度相机先弄"——先做官方 YOLO 抓取 demo（深度相机主用途；普通 USB 俯视相机排后）。
- 读透仓内官方教程 `视觉夹取_Demo.md` + 查 Orbbec 官方上手资料，理清完整流程。**关键事实**：
  - Gemini 2 **USB 供电**（USB-C 插 USB 3.0 口，无单独电源），先用 **Orbbec Viewer**（Windows）点亮验证硬件。
  - **demo 代码是独立仓** `github.com/Seeed-Projects/reBot-DevArm-Grasp`（≠ 本仓），用 `pyorbbecsdk`（需编译）+ conda env `rebotarm` + Eye-in-Hand 手眼标定（打印 10cm ArUco → `hand_eye.npz`）。
  - demo 跑 Ubuntu/Linux → ⚠️ 本机 WSL2 要 usbipd-win **同时透传相机+CAN 两设备**，USB3.0 深度相机带宽存疑，可能要权衡原生 Ubuntu。
  - 🟢 一致性：demo 克隆 vectorBH6/reBotArm_control_py **main**（含 idx_q 限位修复），跟本仓 fork develop 今天 cherry-pick 的修复一致，IK 限位都对。
- **新建 `Gemini2视觉抓取上手指南.md`**（根目录）：全景路线 + 先决准备 + 装环境命令 + 手眼标定 + 三段跑法 + 坑/FAQ + 待办 + 参考链接。
- 交叉引用更新：`遥操作与LeRobot待办.md` §3.9（到货进展 + 指向新指南）；`项目总览.md` 结构图登记。
- 🟡 待实操：选 WSL vs 原生 Ubuntu、打印 ArUco + 腕部支架、clone demo 仓跑通。

## 2026-06-12

### Arm102 LD + B601-DM LeRobot 真机联调：环境、校准、单轴 direct follow 验证

- **环境切换到 Seeed 官方教程路线**：WSL `Ubuntu-22.04` + Miniforge `~/miniforge3` + conda env `lerobot`，工作目录 `~/rebot_lerobot`。确认版本：`lerobot 0.4.4`、`lerobot_teleoperator_rebot_arm_102 1.0.0`、`lerobot_robot_seeed_b601 1.0.0`、`motorbridge 0.4.5`，`python -m pip check` 通过。
- **串口和校准状态**：当前 102 主臂为 CH340/COM11 → WSL `/dev/ttyUSB0`，B601 为 HDSC CDC/COM8 → WSL `/dev/ttyACM0`；校准文件已存在：`~/.cache/huggingface/lerobot/calibration/teleoperators/rebot_arm_102_leader/rebot_arm_102_leader.json` 和 `~/.cache/huggingface/lerobot/calibration/robots/seeed_b601_dm_follower/follower1.json`。
- **踩坑 1：PowerShell 和 WSL 命令混用**。Miniforge 安装时曾在 Windows PowerShell 里执行 `uname/source` 等 Linux 命令导致报错；已在手册里明确“先 `wsl -d Ubuntu-22.04`，再执行 Linux 命令”。
- **踩坑 2：LeRobot 0.5.x 与 Seeed 官方路线混装**。早期走过 Hugging Face 主线 `lerobot 0.5.2`，后切回 Seeed 教程的 `lerobot 0.4.4`；手册已记录不要混装，命令必须来自 `/home/pc/miniforge3/envs/lerobot/bin/`。
- **踩坑 3：官方 `lerobot-teleoperate` 能连接但不适合当前 WSL 实时遥操**。命令能连上 102 和 B601，但官方 `teleop_loop()` 每帧先 `robot.get_observation()`，对 B601 逐电机 `request_feedback()`；WSL USB/IP 下实测 `Teleop loop time ≈ 614ms (2Hz)`，并反复 `request_feedback failed: dm-serial write failed: Operation timed out`。结论：官方命令暂作对照，不作为当前主入口。
- **新增调试脚本**：`tools/lerobot_debug/arm102_to_b601_direct_follow.py`。它仍使用 Seeed 官方 102 leader 和 B601 follower 类，但控制循环跳过每帧 B601 feedback，直接读取 102 action 后向 B601 `send_action()`；支持 `--send-joints` 单轴测试和 `--invert-raw-joints` 在 102 原始角度裁剪前反向。
- **踩坑 4：2/3 轴“不动”不是电机坏，而是方向 + 限位裁剪**。B601 的 `shoulder_lift` / `elbow_flex` 限位为负区间，未反向时目标会被裁成 `0.00`。实测加 `--invert-raw-joints shoulder_lift,elbow_flex` 后 2/3 轴可动。
- **单轴验证结果**：
  - `shoulder_pan`：有效，`follower_target = -leader`
  - `shoulder_lift`：有效，但需要 `--invert-raw-joints shoulder_lift`
  - `elbow_flex`：有效，但需要 `--invert-raw-joints elbow_flex`
  - `wrist_flex`：有效，`follower_target = leader`
  - `wrist_yaw`：有效，`follower_target = leader`
  - `wrist_roll`：有效，`follower_target = -leader`
  - `gripper`：有效，`follower_target = -6 * leader`，并裁剪到 `[-270, 0]`
- **踩坑 5：`--send-joints` 只测试指定轴**。曾因只发 `shoulder_lift,elbow_flex` 误以为其他轴“不行”；已修脚本，`--send-joints` 模式下不会偷偷补发 `wrist_yaw=0`，并在手册中明确单轴测试方法。
- **文档同步**：更新 `LeRobot_Arm102LD_B601DM遥操作小白执行手册.md`、`遥操作与LeRobot待办.md`、`项目总览.md`，把当前推荐入口改为 direct follow。
- **下一步**：跑全轴小幅 direct follow，不要先用官方 `lerobot-teleoperate`：

```bash
python -u /mnt/d/Robot/reBot-DevArm/tools/lerobot_debug/arm102_to_b601_direct_follow.py --leader-port /dev/ttyUSB0 --follower-port /dev/ttyACM0 --fps 5 --invert-raw-joints shoulder_lift,elbow_flex
```

---

## 2026-06-13

### 0. 本地与远端对齐（拉另一台机的遥操作进展）
- 本地落后 origin/main **5 个 commit**（另一台机推的 Arm102→B601 遥操作进展：`tools/lerobot_debug/` 8 个调试脚本 + 执行手册 +859 行 + 待办/总览更新，无 submodule 指针变动）。
- `git merge --ff-only origin/main` 干净 ff 到 `c7b742e`，`git submodule update` 指针未变；本地 = 远端对齐。

### 1. 上游同步：仅 MotorBridge v0.4.5 → v0.4.6
- 例行检查 5 仓：主仓 Seeed / control_py(develop&main) / ROS2 / StarArm_102 **均无新**；只 **MotorBridge** 出 v0.4.6（`53fe6dd → 38b8a56`，4 commit）。
- ⭐ spawn 独立 subagent 复核 v0.4.6 达妙影响（碰了 motor_vendors/damiao，故必复核）：确认**低风险纯增量**——
  - `b4a839a` 达妙型号表新增 `4340_v20`/`4340X` 别名（DM4340 高转速变种 vmax20）：纯加一行 + display 函数，**现有 4340P/4340/4310 逐字节未动**；按 limits 反查无歧义（vmax 20 vs 10，差值远大于 tol）。
  - `13c6414` ws_gateway 浏览器 query 传 token：loopback 无 token 用法零影响，不碰达妙控制路径。
  - Python API 100% 兼容；版本 bump → 需升 wheel。
- 走 `sync/upstream-2026-06-13` 分支：提交 submodule 指针 `129eee0` + docs commit，ff 合入 main，打 `baseline-2026-06-13` tag。MotorBridge fork main ff `53fe6dd→38b8a56`（待 push）。
- **wheel 升 0.4.6**：Windows `E:\Python312` ✅（import + `__version__==0.4.6` + add_damiao_motor 验证）、WSL 系统 python3 ✅；⚠️ **LeRobot 环境（另一台机 0.4.5）待自升**——用户正用 0.4.5 调遥操作，建议调稳后再升（4340_v20 对其无影响）。
- 硬件未验证（风险极低）；回滚点 `baseline-2026-06-11` + wheel `pip install motorbridge==0.4.5`。详见 `上游更新记录/2026-06-13_同步详情.md`。

### 2. 新建 Ubuntu 双系统安装指南（为上原生 Linux 做准备）
- 背景：WSL 走 usbipd 转 USB，官方 `lerobot-teleoperate` 每帧逐电机读 feedback 往返延迟大（~614ms/2Hz + timeout）；官方闭环遥操作 + Gemini2/YOLO 抓取 demo 都要原生 Linux。结论：WSL 串口"能收但往返延迟大"，坑 3（官方路径慢）是 WSL 背锅，原生 Linux 可解；但坑 4/5（关节 invert 映射 / wrist_yaw 补 0）是软件映射问题，换系统也省不掉。
- 用户决定在**另一台 Windows 电脑（单块 SSD）压缩 C 盘装 Ubuntu 22.04 双系统**。
- **新建 `Ubuntu双系统安装指南.md`**（根目录）：确认下载 **Ubuntu 22.04.5 LTS**（对得上 Seeed lerobot 0.4.4）+ Rufus；从安全准备（备份/关 BitLocker/关快速启动）→ 磁盘管理压缩 C 盘 → Rufus 做启动盘 → 启动菜单键从 U 盘启动 → **"与 Windows 共存"安装**（⚠️ 别选 Erase disk）→ GRUB 选系统 → 装完接机器人（指回遥操作手册 + Gemini2 指南）+ 避雷速查表。
- 交叉引用：`项目总览.md` 结构图登记。
- 🟡 待用户提供：那台电脑品牌型号（对应启动键/BIOS）+ C 盘是否开 BitLocker + 给 Ubuntu 分多大。

### 3. 上游同步（当日第二次）：StarArm_102 d777473 → e8b871f
- 当日晚些再查上游，发现 **StarArm_102（102 主臂 SDK）上游推了 4 个新 commit**（上午查时还没有）；主仓/MotorBridge/ROS2/control_py 仍无新。
- ⭐ spawn 独立 subagent 复核：极低风险，全量 ff 镜像 upstream（垃圾仅进 fork、对象层面仅 ~248KB，主仓只存指针 SHA）。**纠正我 1 处误判**：所谓"6MB STL 垃圾"实际与现有 `ROS2_HUMBLE/src/` 同 blob SHA、git 去重，没那么大。
- **新增 Python_SDK 控制脚本（参考价值，非开箱即用）**：`stararm102_ro_hover.py`（悬停=角度静止检测+位置锁+推动切 unlocked 0x10 近似，**非真力矩**；电流估外力/pinocchio RNEA 路径都注释掉了 WIP）+ `stararm102_ro_button.py`（按键控制）+ `stararm102_ro_mode.py`（hover+重力补偿+过温保护，依赖 pinocchio 不能直接跑）；`stararm102_ro.py` 夹爪加 0~90° 限幅。
- 无删除破坏；对用户当前 `direct_follow` 遥操作工作流零影响（不走此 SDK）。⚠️ 含上游误提交垃圾（output.txt/rosbag/.vscode/重复 description），留 fork 历史不进主仓。
- 走 `sync/upstream-2026-06-13b` 分支，ff 合入 main，打 `baseline-2026-06-13b` tag。StarArm_102 fork main ff（待 push）。回滚点 `baseline-2026-06-13`。详见 `上游更新记录/2026-06-13_同步详情.md` 补充段。

### 4. 上游检查（当日第三次）：StarArm_102 上游重写历史 re-mirror + control_py main 评估不同步
- 再查上游，StarArm_102 和 control_py main 有新动静（主仓/MotorBridge/ROS2 无新）。⭐ 并行 spawn 2 个 subagent 复核，**都推翻了我的初判**。
- **StarArm_102 `e8b871f → e61f34e`（re-mirror，指针 `e55a60c`，tag `baseline-2026-06-13c`）**：上游作者把我上午刚同步的 4 个 commit **重写压缩成更干净的 `e61f34e`**（已分叉非 ff）。新增官方 `Lerobot/lerobot-stararm102/` 适配器包（HD=leader/FL=follower 角色对 + 官方标定 joint 方向/范围，参考价值高，但 102 同构非 B601 异构不能直接用）+ 清垃圾（output.txt/rosbag/.vscode）+ button/mode 合并进 ro.py。净瘦身无大 blob，功能不丢，对 direct_follow 零影响。执行 fork `reset --hard` + **force-push**（内容都是上游自己提交，可接受）。用户拍板"对齐"。
- **control_py main 8 commit（`893f095→b338d8b`）：❌ 评估后不同步**。subagent 复核：不是"RS+STL"那么简单，是**破坏性 API 重构**（RobotArm→RebotArm、删 arm.py/arm.yaml、重写所有 example）；DM 改进（safe_home/MIT 带重力）**全寄生新 API 无法干净 cherry-pick**；硬摘要拖 ~70MB RS STL + 跟 idx_q cherry-pick 冲突 + 把重力补偿 kp=0 改成 2.0 改手感。DM URDF 改动只是 mesh 路径重写零数值影响。**维持 develop 0163c08 不动**（追 develop + develop 无新=已最新）。用户拍板"跳过"。
- 🟡 RS 版未来若上：那套重构（含 RS URDF/STL）届时才相关，正确做法是等它合进 develop 后整体同步，而非现在 cherry-pick。详见 `上游更新记录/2026-06-13_同步详情.md` 补充2。
### 5. Arm102→B601 direct follow 全轴遥操确认通过 + 一键启动脚本入仓

- 用户将电脑、102 主臂、B601 从臂重新上电后，请求重新跑遥操。
- Windows `usbipd list` 确认当前设备：
  - `1-6 1a86:7523 USB-SERIAL CH340 (COM11)` = 102 主臂
  - `1-7 2e88:4603 USB 串行设备 (COM8)` = B601 从臂
- WSL 初始为 stopped；启动 WSL 后执行 `usbipd attach --wsl --busid 1-6` 和 `usbipd attach --wsl --busid 1-7`，两个设备变成 `Attached`。
- 用 root 方式加载/授权串口，避免后台 `sudo` 等密码卡住：

```powershell
wsl -d Ubuntu-22.04 -u root -- bash -lc "modprobe ch341 || true; modprobe cdc_acm || true; chmod 666 /dev/ttyUSB* /dev/ttyACM* 2>/dev/null || true; ls -l /dev/ttyUSB* /dev/ttyACM*"
```

- 确认 WSL 串口：
  - `/dev/ttyUSB0` = 102 主臂
  - `/dev/ttyACM0` = B601 从臂
- 跑 102 只读短测，`read_raw_angles.py` 能连续输出 `shoulder_pan / shoulder_lift / elbow_flex / wrist_flex / wrist_yaw / wrist_roll / gripper`。
- 启动全轴 direct follow：

```bash
python -u /mnt/d/Robot/reBot-DevArm/tools/lerobot_debug/arm102_to_b601_direct_follow.py --leader-port /dev/ttyUSB0 --follower-port /dev/ttyACM0 --fps 5 --invert-raw-joints shoulder_lift,elbow_flex
```

- 用户确认：这次遥操效果很好。阶段结论更新为 **Arm102→B601 direct follow 全轴基础遥操已通过**。
- 新增 PowerShell 一键启动脚本：

```text
tools/lerobot_debug/start_direct_follow_wsl.ps1
```

脚本内容：进入 WSL、激活 `lerobot` conda 环境、进入 `~/rebot_lerobot`、执行全轴 direct follow 命令，并保留窗口方便 `Ctrl+C` 停止和查看输出。

当前推荐启动命令：

```powershell
powershell -ExecutionPolicy Bypass -File D:\Robot\reBot-DevArm\tools\lerobot_debug\start_direct_follow_wsl.ps1
```

注意：该脚本默认 USB 已经 attach 到 WSL；如果重启后没有 `/dev/ttyUSB0` / `/dev/ttyACM0`，先按手册重新 attach USB。

---

## 2026-06-18

### 1. 三仓上游同步：MotorBridge v0.4.7 + ROS2 9 commit + 主仓 5 文档（用户拍板"全部同步到最新"）

- **起点对齐检查**：本地已提交内容 == origin/main（0/0），仅 2 个未提交本地文件（`tools/lerobot_debug/start_direct_follow_wsl.ps1` 改动 + `set_zero_base.py` 新文件），本次同步**不混入**（commit 分层）。
- **网络**：直连 GitHub 被挡（443 连不上），经本地代理 `127.0.0.1:7890` fetch 主仓 + 4 submodule 上游。
- ⭐ **并行 3 个 subagent 分仓复核**（MotorBridge / ROS2 / 主仓），全部完成、纠正/确认初判后才执行。

**各仓结果**：

| 仓库 | 变化 | 处置 |
|---|---|---|
| MotorBridge | `38b8a56`(v0.4.6)→`2b7b350`(v0.4.7) 3 commit | ✅ 升级，指针 `1bf9665` |
| reBotArmController_ROS2 | `c6a4e68`→`a61efe4` 9 commit | ✅ 升级，指针 `f7c9645`（含 63MB RS STL，用户确认接受） |
| Seeed 主仓 | 真新增 5 commit（剔除 squash 重复） | ✅ squash 单亲 `63a3cfb` |
| reBotArm_control_py | develop 无新 | — |
| StarArm_102 | 无新 | — |

- **MotorBridge v0.4.7（⭐ 对症遥操作超时）**：`dadfe8d` dm-serial 串口超时预算 **1ms→10ms**；`bbcca2d` 达妙模式切换加固（写寄存器等 `0x55` write-ack 3 次重试 + `ensure_control_mode` 共享超时预算 + 切换前后持位 + store_parameters 自动 disable）。达妙 +约 600 行新单测，Python API 100% 兼容。**直接对症** 06-13 遥操作的 `dm-serial write failed: Operation timed out` / `clear_error timed out`。⚠️ 行为变更：写寄存器现等 write-ack，若 DM4340P/4310 固件不回 `0x55` 会从静默成功变超时报错——**待下次上电冒烟测**，若见 `write ack not received` 则回退 0.4.5。
- **ROS2 a61efe4（B601-RS 为主线）**：唯一 DM 相关 `d943b8f` 修 `rebotarm_hardware.yaml` 中 `models.dm.gravity_compensation` 的 kp/kd（误填 MIT 数组 `[120,120,120,18,18,18]`/`[8,8,8,2,2,2]` → 标量 `2.0`/`1.5`，已确认入树）；其余全是 RS 版 URDF/MoveIt2/夹爪 + 30 个 RS STL（63MB，DM 实操零价值）。⚠️ 破坏性变更（删 `JointVelCmd.msg`/`SetMode.srv`、launch 参数改名、config 整合）本仓未进 ROS2 阶段不消费，**当前零影响**，登记待进 ROS2 阶段复核。
- **主仓 5 commit（squash 单亲法）**：边界 `1cf0ec9`（06-11 squash 已吸收）三重验证确认；真新增 = README×5 文本（RS 五种套件选项 + DM/RS 参数对比 + 48V 电源推荐 + banner 换 `RS5_56.png`）+ readme_zh 1 行（电源京东链接 URL，**保留本地 94MB 警告块**）+ `media/RS5_56.png` 7.75MB（可推送）。验证 `git rev-list --objects HEAD` **powerstep 对象 = 0**，无大 blob 进可推送历史。

### 2. wheel 全部升级到 0.4.7（PyPI 已发布）

| 环境 | 升级 | 验证 |
|---|---|---|
| Windows PATH python | 0.3.7 → 0.4.7 | import OK |
| WSL 系统 python3 | 0.3.7 → 0.4.7 | import OK |
| ⭐ WSL conda `lerobot`（遥操作环境） | 0.4.5 → 0.4.7 | import OK，Controller/Mode 可用 |

- 注：文档曾记 Windows wheel 在 `E:\Python312`，本机不存在该路径，实为 PATH python（印证项目跨两台机，wheel 记录可能是另一台的）。
- 回滚：任一环境 `pip install motorbridge==0.4.5`（lerobot 环境）/`==0.4.6`。

### 3. 同步收尾

- sync 分支 `sync/upstream-2026-06-18`（3 个分层 commit）合入 main，打 `baseline-2026-06-18` tag。
- fork push（ff）：`fanhao375/motorbridge` `38b8a56→2b7b350`；`fanhao375/reBotArmController_ROS2` `c6a4e68→a61efe4`。
- 新建 `上游更新记录/2026-06-18_同步详情.md`。
- 🟡 **下次上电遥操作 = v0.4.7 write-ack 行为冒烟测**：重点看 `dm-serial write timeout` 是否减少 + 是否出现 `write ack not received`。
- 那 2 个未提交本地文件已分层提交（见下节 4），不混入上游同步。
- ⚠️ `复刻基线维护原则.md` §3 各仓"当前状态"行（仍记 MotorBridge v0.3.9 / 2026-05-28）已滞后多轮，按该文件"更新需双方确认"规则未擅自改。

### 4. 本地遥操作工具提交（一键脚本升级 + 底座设零工具）

承接上游同步后，把会话前晾着的 2 个本地工具文件分层提交并 push：

- **`40e42db` 一键遥操脚本升级**：`tools/lerobot_debug/start_direct_follow_wsl.ps1` 从"只启动遥操作"升级为端到端一键——按 VID:PID 解析当前 BUSID（Arm102 CH340 `1a86:7523` / B601 桥 `2e88:4603`，换 USB 口不写死）→ 拉起 WSL → `usbipd attach` 两个串口 → `modprobe ch341/cdc_acm` → 轮询等 `/dev/ttyUSB0`+`/dev/ttyACM0` 就绪 → `chmod 666` → 启动 direct follow。全中文提示 + 失败排查指引。
- **`e992cee` 新增 B601 底座设零小工具**：`tools/lerobot_debug/set_zero_base.py`（51 行）只对底座 shoulder_pan（id `0x01/0x11`, 4340P）操作，**全程 `disable_all` 不 enable**（避免连接时底座先跳）。`disable` 模式松力 + 读当前角度；`setzero` 模式松力 + `set_zero_position` 设当前物理位置为新零点 + 读结果确认接近 0。
- 二者均为 6-18 当日本地实操产物（非上游），单独提交、不进 baseline-2026-06-18 同步范围。

### 5. 决定就在本机上 Ubuntu 双系统 + 前期准备实测

- 用户结论：**WSL 虚拟机 usbipd 串口延迟还是不行，最终上原生 Linux**——且改为**就在 D:\Robot 这台遥操作机上装双系统**（原指南设想的是另一台）。
- 直接读硬件做前期体检（只读，未改系统）：
  - 组装台式机，主板 **Intel SKYBAY**，BIOS 5.12(2020)，**UEFI + GPT**，单块 **477G SATA SSD**。
  - 分区：EFI/MSR + **C: 225G(剩165G)** + **D: 251G(剩207G，盘尾分区)**；页面文件在 **C:**（压 D: 不被挡）；D: 仅用 ~44G（**WSL 占 40G**，上原生后可回收）；内存 16G。
  - 快速启动**开着**（HiberbootEnabled=1，待关）；BitLocker 读不到需管理员确认。
- **定制方案**：从 **D: 末尾压 150G**（`153600`，盘尾未分配最干净，C: 不动）；Rufus GPT/UEFI；启动键 **F10**(Intel 主板菜单)/F2-Del(设置)；共存安装；conda 装 `motorbridge==0.4.7`。
- 文档：`Ubuntu双系统安装指南.md` 新增"★ 本机配置 & 定制方案"节 + §10 待办勾掉已实测项 + §8.3 wheel 对齐 0.4.7。
- ✅ 装前两雷已排（2026-06-18 用户管理员实测）：**BitLocker 没开**（版本=无/完全解密/保护关闭/0.0%）+ **快速启动已关**（reg HiberbootEnabled=0）。盘可安全压。
- ✅ **D 盘已压**（2026-06-18，管理员 `Resize-Partition -DriveLetter D -Size (Size-150GB)`）：D: 251.5→101.5G（最小可缩到 44.8G，余量充足），盘尾空出 **150G 未分配**，位置在盘最末尾、留给 Ubuntu，未格式化。
- 🟡 仍待用户：下载 Ubuntu 22.04.5 ISO（`ubuntu-22.04.5-desktop-amd64.iso` 4.4G，认准 desktop 版非 server/wsl）+ Rufus + ≥8G U 盘 → Rufus 做启动盘(GPT/UEFI) → 重启 F10 进 U 盘 → "与 Windows 共存"安装（分区那屏停下来发我）。
- 远程访问方案（异地，装完再配）：主力 **Tailscale + SSH**（组虚拟局域网，异地变同网段）；图形界面加 **NoMachine / 向日葵 / RustDesk**。⚠️ 实时遥操作必须人在现场（手摆主臂），远程只用于启动/监控/采数据/训练/调试。已写入 `Ubuntu双系统安装指南.md` §11。
