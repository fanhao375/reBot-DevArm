# 装机烧录指南

> 给自己用的笔记。从焊线到机臂能跑代码的完整流程，慢慢往里填。
> **状态**：骨架（2026-05-09 起）。看完 B 站视频 + 实操踩坑后逐步细化。

---

## 0. 这是啥 / 怎么用这份文档

reBot Arm B601-DM 整机从"焊完线"到"能跑示例代码"的端到端流程笔记。

**取舍**：
- 只走**达妙 DM 路径**（你机臂用的就是 DM；RobStride 暂不写）
- 烧 ID + 标零点优先用 **MotorBridge Web UI**（不用 DMTool 走老路径）；DMTool 留作 fallback
- 上游唯一详细教学是 B 站视频 [BV1K9dbBNEdC](https://www.bilibili.com/video/BV1K9dbBNEdC/)，本文档用来**对照视频做笔记**，不是替代视频

**符号约定**：
- 🟢 已确认事实（来自现有代码/配置/学习笔记/上游 wiki）
- 🟡 TODO（看视频或实操后填）
- ⚠️ 已知坑（你已经踩过或上游明确写出的安全要求）

### 0.1 工具链架构（要先明白）🟢

```text
┌──────────────────────────────┐
│  浏览器（Chrome/Edge）        │  打开 https://rebot-devarm.w0x7ce.eu/
│  └ Web UI 按钮 + 拖滑块界面   │  ← 官方云端托管，不用装
└──────────────┬───────────────┘
               │ WebSocket  (127.0.0.1:9002)
               ▼
┌──────────────────────────────┐
│  motorbridge-gateway 进程     │  ← 本地装的，已就绪
│  作用：把 WS 指令翻译成 CAN 帧 │     Win: E:\Python312\Scripts\
│                              │     WSL: /usr/local/bin/
└──────────────┬───────────────┘
               │ USB 串口
               ▼
┌──────────────────────────────┐
│  USB-CAN 转接板（达妙官方）    │
└──────────────┬───────────────┘
               │ CAN 总线（CAN-H / CAN-L / GND）
               ▼
        7 个达妙电机（0x01..0x07）
```

**核心理解**：
- **Web UI 在云端、不用装**，浏览器打开 URL 即可
- 你电脑上跑的是 **gateway 进程**，负责"浏览器 ↔ USB-CAN ↔ 电机"的桥接
- 浏览器和 gateway 是**通过 localhost:9002 WebSocket** 通信
- 三条路殊途同归：Web UI 按钮 / `motorbridge-cli` 命令行 / Python `motorbridge` 包，**都是发同一套 JSON op 指令到 gateway**

---

## 1. 准备清单

### 1.1 硬件

| 物品 | 数量 | 状态 |
|---|---|---|
| 🟢 reBot Arm B601-DM 整机（含 6+1 电机） | 1 | 电机已到，结构件待装 |
| 🟢 USB-CAN 转接板（达妙官方） | 1 | TODO 是否在手 |
| 🟢 信号电源隔离板（XT30 2+2） | 1 | TODO 是否在手 |
| 🟢 24V 15A XT30 输出电源 | 1 | TODO 是否在手 |
| 🟢 木工夹具（≥3 英寸） | 2 | 上游 wiki 要求 |
| 🟢 USB-C 数据线 | 1 | |
| 🟢 万用表 | 1 | 焊线后检线必须 |
| 🟡 电烙铁 / 焊锡 / 热缩管规格 | — | TODO 看视频是否提到推荐型号 |

### 1.2 软件（已就绪 ✅）

| 项目 | 当前版本 | 位置 |
|---|---|---|
| `motorbridge` Python 包 | 0.2.8 | Windows `E:\Python312` + WSL2 Ubuntu Python 3.10 |
| `motorbridge-gateway`（Win） | 0.2.8 | `E:\Python312\Scripts\motorbridge-gateway.exe` |
| `motorbridge-gateway`（WSL） | 0.2.8 | `/usr/local/bin/motorbridge-gateway` |
| `motorbridge-cli`（Win/WSL） | 0.2.8 | 跟 gateway 同目录，备用命令行 |
| `reBotArm_control_py` | submodule `062bef9`（含 `RobotArm.fresh()`） | `software/reBotArm_control_py/` |
| MotorBridge Web UI | Seeed 在线托管 | https://rebot-devarm.w0x7ce.eu/ |
| DMTool（fallback） | v2.1.6.0-FD | `hardware/测试工具/`（本地不入 git） |

> 🟢 `motorbridge-gateway` / `motorbridge-cli` 两个命令行都是 `pip install motorbridge` 时**自动随 wheel 装上**的，不用单独编译 Rust。

### 1.3 不要用的工具 ⚠️

- ❌ `Debugging_Tools_v.1.6.8.8.exe` — 已踩坑：串口乱码 / ID 错乱 / 读参数无响应（详见 `学习笔记.md` 2026-04-29 调试达妙电机条目）
- ❓ `DM_Tools_v.1.8.0.1.exe` — 上游快速入门 wiki 推荐版本，**未亲自验证**；如果 Web UI 路径不通再考虑

---

## 2. 阶段 0：焊线 + 检线（明天动手）

### 2.1 焊线要点（🟡 TODO 看视频后填）

视频里需要重点看的：
- [ ] XT30 2+2 接头正负极标记 / 焊接顺序
- [ ] CAN-H / CAN-L 线序（参考 `hardware/reBot_B601_DM/Motor_Datasheets/docs_md/03_硬件说明.md`）
- [ ] 屏蔽层是否接地、接哪一头
- [ ] 首末电机是否需要 **120Ω CAN 终端电阻**？怎么接？
- [ ] 焊接后是否需要套热缩管 / 灌胶

### 2.2 检线清单（🟡 TODO 看视频补充）

万用表测：
- [ ] 24V 电源对地电阻（防止短路）
- [ ] CAN-H 对 CAN-L 电阻（应该约 60Ω = 两个 120Ω 终端电阻并联）
- [ ] 各电机 XT30 端子的正负极顺序一致

---

## 3. 阶段 1：单电机烧 ID（×7）

### 3.1 电机 ID 分配（🟢 来自 arm.yaml + gripper.yaml）

| 关节 | motor_id | feedback_id | model | 说明 |
|---|---|---|---|---|
| J1（底座） | `0x01` | `0x11` | **4340P** | 大关节 |
| J2 | `0x02` | `0x12` | **4340P** | 大关节 |
| J3 | `0x03` | `0x13` | **4340P** | 大关节 |
| J4 | `0x04` | `0x14` | **4310** | 小关节 |
| J5 | `0x05` | `0x15` | **4310** | 小关节 |
| J6（腕） | `0x06` | `0x16` | **4310** | 小关节 |
| 夹爪 | `0x07` | `0x17` | **4310** | 小关节 |

规律：`feedback_id = 0x10 + motor_id`；大关节（DM-J4340P）×3，小关节（DM-J4310）×4。

电机出厂默认 `motor_id = 0x01`。**所以 J1 不需要烧（如果出厂值就是想要的）；其他 6 个必须烧**。

### 3.2 用 MotorBridge Web UI 烧 ID（推荐路径）

**为什么用 Web UI 而不是 DMTool**：见对话记录 — DMTool 版本碎片化（v1.6.8.8 ❌ / v2.1.6 ✅ / v1.8 ❓），Web UI 是上游主推一站式方案。

#### 步骤 1：硬件接线（单个电机）
```
24V 电源 → 信号电源隔离板 → 单个电机的 XT30 2+2 接口
USB-CAN 板 → 电机的 CAN 口
USB-CAN 板 → 电脑 USB
```

#### 步骤 2：查 USB-CAN 板的 COM 口
Windows：设备管理器 → 端口（COM 和 LPT）→ 找 USB-Serial / 达妙转接板，记下 `COM3` 还是 `COM4`。

#### 步骤 3：启动本机 gateway

```powershell
motorbridge-gateway --bind 127.0.0.1:9002 --vendor damiao --transport dm-serial --serial-port COM3 --serial-baud 921600
```
- 把 `COM3` 改成你实际的 COM 口
- 这个窗口**别关**，要一直跑
- 出错先看下面 §7 已知坑

#### 步骤 4：浏览器连入 Web UI

打开 https://rebot-devarm.w0x7ce.eu/，连接状态变绿色 = 跟你本机 gateway 通了。

#### 步骤 5：在 Web UI 烧 ID 🟢

Web UI 背后调的是 WebSocket JSON 协议（参考 `software/MotorBridge/integrations/ws_gateway/README.zh-CN.md`）。烧 ID 实际是 3 步：

**5.1 扫描总线（确认当前电机 ID）**

Web UI 点 "Scan" 按钮 = 发送
```json
{"op":"scan","start_id":1,"end_id":16,"feedback_base":16,"timeout_ms":100}
```

预期返回：单电机出厂默认 `motor_id=0x01`，扫到的是 1。

**5.2 写 ID（带验证 + 持久化）**

Web UI 改 ID 按钮 = 发送
```json
{
  "op":"set_id",
  "vendor":"damiao",
  "old_motor_id":1, "old_feedback_id":17,
  "new_motor_id":2, "new_feedback_id":18,
  "store":true,
  "verify":true
}
```

参数解释：
- `old_motor_id` / `old_feedback_id`：当前电机的 ID（出厂 1 / 17）
- `new_motor_id` / `new_feedback_id`：要写入的新 ID（按 §3.1 表填）
- `store: true`：**持久化到电机内部 flash**（断电不丢失），必须 true
- `verify: true`：写入后立刻读回校验

**5.3 单独验证（可选）**

```json
{"op":"verify","motor_id":2,"feedback_id":18,"timeout_ms":1000}
```

返回 `{"ok":true,...}` = 烧录成功。

**🟡 待你实操后补**：
- [ ] Web UI 上对应按钮叫什么（"Set ID" / "改 ID" / "写入"）
- [ ] 烧完是否需要断电再上电（理论上 `store:true` 直接生效不用断电，待验证）
- [ ] 实际界面截图（占位）

#### 步骤 6：烧完一个换下一个 ⚠️

**断电** → 拔 CAN 线 → 换下一个电机 → 接线 → 重启 gateway → 烧。

**绝对不能两个 motor_id = 0x01 的电机同时挂在 CAN 总线上**——会冲突无法识别。

### 3.3 单电机自测（验证烧录成功）

跑 `software/reBotArm_control_py/example/1_damiao_text.py`：

```python
# ⚠️ 跑前必须改 3 个配置
CHANNEL    = "COM3"     # Win: COM3 / WSL2: /dev/ttyACM0
MOTOR_ID   = 0x01       # 改成你刚烧的 ID
FEEDBACK_ID = 0x11      # = 0x10 + MOTOR_ID
MODEL      = "4310"     # ⚠️ 默认是 "4340P"，小关节必须改为 "4310"
```

跑起来后试 `enable` → `mit` 命令，看电机能不能动。

---

## 4. 阶段 2：整机零点设定

### 4.1 装好整机 → 接好 CAN 总线

7 个电机的 CAN 全部挂上同一根 CAN 总线（USB-CAN 板）。

启动 gateway 时不指定 `--motor-id`，让它自动扫：

```powershell
motorbridge-gateway --bind 127.0.0.1:9002 --vendor damiao --transport dm-serial --serial-port COM3 --serial-baud 921600
```

Web UI 应该显示 7 个电机全部识别到（0x01..0x07）。

### 4.2 标零点协议层 🟢

**Web UI 一步记录零点 = 发送 `set_zero_position` op**（参考 `ws_gateway/README.zh-CN.md`）：

```json
{"op":"set_zero_position"}
```

**前提**：调用前必须先用 `set_target` 选中要标的电机（或 gateway 启动时已经指定 `--motor-id`）。

#### 标准做法（来自 `MotorBridge/docs/zh/operation_manual.md §9` 夹爪校准流程）

> ⚠️ 达妙电机用单圈编码器，**断电零点会丢失**。每次重新装机后都要重新标。

逐关节流程：

1. **MIT 模式低力矩"推"到机械限位**（碰到机械停止就停，不会硬撞）
   ```json
   {"op":"set_target","vendor":"damiao","motor_id":1,"feedback_id":17,"model":"4340P"}
   {"op":"enable"}
   {"op":"mit","pos":-5.0,"vel":0,"kp":5.0,"kd":0.5,"tau":0,"continuous":true}
   ```
   - `kp=5, kd=0.5, tau=0`：力矩极低，安全
   - 大关节 4340P 用上面参数；小关节 4310 同参数也可

2. **等反馈稳定**（`vel ≈ 0` 且 `pos` 不变 = 顶到位了）
   ```json
   {"op":"state_once"}
   ```

3. **一键写入零点**
   ```json
   {"op":"set_zero_position"}
   {"op":"store_parameters"}   // 持久化到 flash
   ```

4. **下一关节**：换 `set_target` 重复 1-3。

### 4.3 标零点姿势 🟡（看视频后填）

- [ ] 零点姿势是什么？（参考视频 / `urdf/reBot-DevArm_fixend_description/`）
  - 假设：所有关节回到机械限位的一端 = "折叠/收起"姿势
  - 待视频确认
- [ ] 木工夹具夹哪几个位置帮助保持姿势？
- [ ] Web UI 里"一键全标"是不是 7 个关节一起标，还是必须逐个
- [ ] 标完零点是否要断电再上电

### 4.4 零点验证（🟡 TODO）

- [ ] 跑 `fk_sim.py` 看正运动学算出的末端位置和真机姿势是否对得上
- [ ] 跑 `9_gravity_compensation.py` 看重力补偿是否能让机臂"漂浮"

---

## 5. 阶段 3：第一次通电安全清单 ⚠️

来自上游 wiki + 学习笔记：

- [ ] 保持至少 **1 米安全距离**
- [ ] **禁止热插拔** XT30 2+2 接口（必须断电再插拔）
- [ ] 禁止电机超载、超速
- [ ] 周围清空，没有易碎/贵重物品
- [ ] 急停预案：**手能直接拔 XT30** 的位置
- [ ] 第一次启动时使用最小角度 / 最低速度
- [ ] 🟡 TODO 看视频是否提到其他安全要求

---

## 6. 阶段 4：第一次跑代码

### 6.1 单电机命令行自测
- 工具：`software/reBotArm_control_py/example/1_damiao_text.py`（DMTool 的 Python 替代品）
- 用途：每个关节单独跑一下，确认每个电机使能/旋转都正常

### 6.2 整机 Web UI 拖滑块测试
- 工具：MotorBridge Web UI
- 用途：在 Web UI 里拖滑块单独转某个关节，确认 7 个电机都能转

### 6.3 仿真验证（无电机也能跑）
- 工具：`software/reBotArm_control_py/example/sim/fk_sim.py` / `ik_sim.py` / `traj_sim.py`
- 环境：WSL2 + Python 3.10 + Pinocchio + MeshCat（🟡 TODO 待装）
- 用途：在浏览器 MeshCat 里看正/逆运动学和轨迹规划，不需要真机

### 6.4 重力补偿（最有成就感的演示）🟢 完整步骤（WSL 主路径）

- 工具：`software/reBotArm_control_py/example/9_gravity_compensation.py`
- 含 5-08 同步的 `RobotArm.fresh()` 调用，进入 MIT 模式时控制量干净
- 控制律：MIT 位置闭环 + 重力前馈，`kp=0, kd=1`
- 预期现象：机臂启动后**可以用手掰到任何姿势**，松手**不下坠**（所谓"漂浮"）

> ⚠️ **路径决策（2026-05-15）**：尝试 Windows + Python 3.12 跑 `pip install pin` 失败——PyPI 没 Windows 3.12 wheel，pip 退回**源码编译**（要 MSVC + Boost + cmake 全套，大概率失败）。已切到 **WSL Ubuntu Python 3.10 + Pinocchio 3.9.0** 路径，跟 reBotArm_control_py README 官方推荐"Ubuntu 22.04+"一致。

#### 前置条件检查清单 ✅

| 项 | 怎么验证 |
|---|---|
| 7 个电机 ID 烧录完成 | Web UI scan 能扫到 0x01..0x07 |
| 7 个关节零点已标 | Web UI 一键校准完成 |
| 机械臂结构件装好 | 整机成型，关节能自由活动 |
| 24V 电源对地短路检查通过 | 万用表确认 24V+ 对 24V- 不短路 |
| WSL Pinocchio 已装 | `wsl -- python3 -c "import pinocchio; print(pinocchio.__version__)"` → 3.9.0 ✅ |
| MotorBridge Web UI gateway **已关闭** | ⚠️ **必须关**——串口独占，gateway 占着脚本拿不到（且 USB-CAN attach 到 WSL 后 Windows 端拿不到） |
| USB-CAN 转接板已接电脑 USB | Windows 设备管理器能看到 |

#### 一次性准备（首次运行前做一次，以后不用重做）

**1. Windows 端装 `usbipd-win`**（把 USB 设备从 Windows 转发到 WSL）

```powershell
# 管理员 PowerShell
winget install --interactive --exact dorssel.usbipd-win
# 装完重启 PowerShell（让 PATH 生效）
```

> 来源：https://github.com/dorssel/usbipd-win

**2. `arm.yaml` channel 默认值已对**（WSL 路径用 `/dev/ttyACM0`，**不用改**）

```yaml
# software/reBotArm_control_py/config/arm.yaml
channel: /dev/ttyACM0          # ← Linux 默认，WSL 直接用
```

#### 实操步骤 🟢

**1. 接好 USB-CAN 转接板到电脑 USB**

**2. 找到 USB-CAN 的 BUSID**（Windows PowerShell）

```powershell
usbipd list
# 输出示例：
# BUSID  VID:PID    DEVICE                                                        STATE
# 2-1    1a86:7523  USB-SERIAL CH340 / USB-Enhanced-SERIAL CH340                 Not shared
# ↑ 找到 USB-CAN 那一行（通常是 CH340/CP210x/CDC），记下 BUSID（这里是 2-1）
```

**3. 绑定 + 转发到 WSL**（管理员 PowerShell）

```powershell
# bind 是一次性的，每个设备只 bind 一次
usbipd bind --busid 2-1                # 替换成你实际 BUSID

# attach 每次重启电脑/拔插 USB 后都要重做一次
usbipd attach --wsl --busid 2-1
```

> 💡 **重启后要重新 attach**。如果嫌烦，可加 `--auto-attach` 选项：`usbipd attach --wsl --busid 2-1 --auto-attach`

**4. WSL 端确认看得到串口 + 给权限**

```bash
wsl
ls /dev/ttyACM*
# 应该看到 /dev/ttyACM0

sudo chmod 666 /dev/ttyACM0     # 每次插拔后都要做（除非配 udev 规则）
```

**5. 启动重力补偿脚本**

```bash
cd /mnt/f/chengshenzhilu/Robot/reBot-DevArm/software/reBotArm_control_py
python3 example/9_gravity_compensation.py
```

⚠️ **第一次跑前确认**：
- 手放在 XT30 电源接头附近，**随时能拔**急停
- 周围空旷，机臂活动范围内没人/没易碎物
- 用**收起姿势**启动（折叠的状态，重力前馈最小）

**6. 预期终端输出**

```text
============================================================
  reBotArm 重力补偿演示
  预计行为: 机械臂维持位置不动，可以手动掰动至任何位置
  Ctrl+C 停止并断开连接
============================================================

[模型] nq=6, nv=6
[重力] [ 0.    0.   -9.81]  m/s²

[连接] OK
[使能] OK
[MIT模式] OK（kp=2, kd=1）
[控制循环] 启动 @ 500.0 Hz
------------------------------------------------------------
step  tau_g (N·m)
------------------------------------------------------------
[  20] tau_g = -0.012  +1.234  -0.567  +0.089  -0.003  +0.001  N·m
[  40] tau_g = -0.012  +1.234  -0.567  +0.089  -0.003  +0.001  N·m
...
```

每 20 个控制周期打印一次 6 个关节的期望力矩。手推机臂时数字会变（前馈跟随当前姿态）。

**7. 真机验证**

- ✋ **用手轻推**机臂某个关节 → 应该**有阻尼感**（kd=1 提供）
- 🎯 移动到任意姿势**松手** → **不下坠不漂移**（重力前馈抵消重力）
- 📊 终端打印的 `tau_g` 数值随姿态变化

**8. 安全停止**

`Ctrl+C` → 脚本自动失能 + 关闭连接：

```text
[gravity_comp] 收到 Ctrl+C，准备停止...
[停止] 关闭控制循环...
[完成] 已安全断开连接
```

#### 常见报错和排查 ⚠️

| 报错/现象 | 原因 | 解法 |
|---|---|---|
| `No module named 'pinocchio'` | WSL Python 没装 pin | `wsl -- python3 -c "import pinocchio"` 验证；不通的话 `pip install pin` |
| `Could not open serial port '/dev/ttyACM0'` | usbipd 没 attach / Web UI gateway 占着 / 串口权限不够 | 检查 `usbipd list` STATE 是 Attached；检查 Web UI 关了；`sudo chmod 666 /dev/ttyACM0` |
| `/dev/ttyACM0: No such file` | usbipd attach 失败 | `usbipd list` 看设备状态；重新 `usbipd attach --wsl --busid X-Y` |
| `model handshake failed` | 电机 ID/型号不匹配 | 检查 `arm.yaml` 跟实际烧的 ID 一致 |
| 启动后机臂直接下坠 | 重力补偿没生效（前馈力矩 0） | 看终端 `tau_g` 是不是全 0；检查零点是否标错 |
| 启动后机臂剧烈抖动 | `kp/kd` 太高 | 别动 9_gravity_compensation.py，默认参数已调好 |
| 几秒后电机自动失能 | CAN Timeout（不发命令保护机制） | 脚本本来就在持续发 mit 命令；如果还失能说明控制循环没起来 |
| 关节移动方向跟预期相反 | 电机方向定义反 | URDF 跟实物方向不一致，要看 `urdf/reBot-DevArm_fixend_description/` |
| 重启电脑后 WSL 看不到 `/dev/ttyACM0` | usbipd attach 是一次性的，重启后失效 | 重新 `usbipd attach --wsl --busid X-Y`；或加 `--auto-attach` 自动 |

#### 附录：Windows 路径（已验证失败，仅记录）

2026-05-15 尝试过 Windows + Python 3.12 路径：

```powershell
& "E:\Python312\python.exe" -m pip install pin
# 实际结果：PyPI 没 Python 3.12 Windows wheel
# pip 退回源码编译：cmeel-boost / libpinocchio / scipy 等
# 30 分钟没编出来，已 kill
```

**为什么不可行**：
- Pinocchio 在 Windows 上**主要靠 conda-forge** 分发 binary，PyPI 上的 `pin` 包对 Windows + Python 3.12 没 wheel
- 源码编译需要 MSVC + Boost C++ + CMake + Visual Studio 一整套，且未必能成
- 上游 reBotArm_control_py README 就明确写 "Operating System: Ubuntu 22.04+"

**Windows 上如果一定要跑的备选**（未验证）：
- 装 Miniforge 用 conda（`conda install -c conda-forge pinocchio`）
- 降到 Python 3.10/3.11 试 wheel

### 6.5 进阶：ROS2 / LeRobot
- 等基础流程跑通后看 `software/reBotArmController_ROS2/`（已 submodule）

---

## 7. 已知坑速查

来自 `学习笔记.md` 和现有踩坑：

| 现象 | 原因 | 解法 |
|---|---|---|
| 串口乱码 / ID 错乱 / 读参数无响应 | DMTool 用了 v1.6.8.8 旧版 | 用 Web UI 或 DMTool v2.1.6.0-FD |
| `1_damiao_text.py` 电机不响应 | `MODEL` 默认 `"4340P"` 但小关节实际是 `"4310"` | 改 `MODEL` 字段 |
| 反馈帧 `D[0]` 高 4 位 = 3 | ERR=3 输出轴校准异常 | 看达妙说明书 06 章重新校准；Compensation Data 不超 ±300 |
| 不发命令电机几秒就停 | CAN Timeout 自动失能（保护机制） | DMTool 勾"定时发送 10ms"；代码里用控制循环持续发命令 |
| `pip install -e software/MotorBridge` 失败 | 主目录是 Rust 项目（`Cargo.toml`） | 装 `software/MotorBridge/bindings/python` |
| `pip install -e ...` 装的 wheel 没有新符号 | editable 模式不带预编译 .dll | 直接 `pip install --upgrade motorbridge` 拉 PyPI 预编译 wheel（5-09 验证） |
| 🟡 TODO 焊线坑 | | |
| 🟡 TODO 烧 ID 坑 | | |
| 🟡 TODO 标零点坑 | | |

---

## 8. DMTool ↔ Web UI ↔ 命令行 三路对照表

视频是 4 月初拍的，大概率全程 DMTool。三种工具最终都是发同样的 CAN 帧，只是壳子不同：

| 任务 | DMTool 操作 | Web UI 操作 | 命令行 / JSON 协议（🟢 已确认） |
|---|---|---|---|
| 连接 | 选 COM 口 + 波特率 | 浏览器打开 URL，gateway 已启动 | `motorbridge-gateway --transport dm-serial --serial-port COM3 --serial-baud 921600` |
| 扫描电机 | "搜索"按钮 | "Scan" 按钮 | `{"op":"scan","start_id":1,"end_id":16,"feedback_base":16}` |
| 选中某个电机 | 列表里点一行 | 列表点一行 | `{"op":"set_target","vendor":"damiao","motor_id":1,"feedback_id":17,"model":"4340P"}` |
| 写 motor_id（烧 ID）| "修改 ID"对话框 | 🟡 待看 | `{"op":"set_id","old_motor_id":1,"old_feedback_id":17,"new_motor_id":2,"new_feedback_id":18,"store":true,"verify":true}` |
| 使能 / 失能 | "Enable" / "Disable" | 同名按钮 | `{"op":"enable"}` / `{"op":"disable"}` |
| 切 MIT 模式控制 | MIT 标签页输入 pos/kp/kd | MIT 滑块 | `{"op":"mit","pos":0,"vel":0,"kp":20,"kd":1,"tau":0,"continuous":true}` |
| 切位置-速度模式 | POS_VEL 标签页 | POS_VEL 滑块 | `{"op":"pos_vel","pos":3.1,"vlim":1.5,"continuous":true}` |
| **设零点** ⭐ | "保存当前位置为零点"按钮 | **"一键零点校准"按钮** | `{"op":"set_zero_position"}` |
| 持久化参数到 flash | "保存"按钮 | 🟡 待看 | `{"op":"store_parameters"}` |
| 读实时反馈 | "状态"窗口 | 实时 pos/vel/torque 显示 | `{"op":"state_once"}` / `{"op":"poll_feedback_once"}` |
| 单电机调试（拖动条）| 拖动 pos 输入 | "拖拽式电机控制" | 循环发 `mit` 或 `pos_vel` |
| 急停 | "Stop"按钮 | 同名按钮 | `{"op":"stop"}` |
| 清错误 | "Clear Error" | 同名 | `{"op":"clear_error"}` |

> 🟢 JSON 协议列来源：`software/MotorBridge/integrations/ws_gateway/README.zh-CN.md` 第 177 行起"入站命令示例"

> 🟡 实操时填："Web UI 操作"列里那几个 🟡 待看，等你今天 Web UI 一打开就能补

---

## 9. 故障排查（🟡 TODO 遇到再填）

格式：`【现象】→【可能原因】→【验证方法】→【解法】`

- 暂无

---

## 10. 资料链接

### 上游官方
- 项目仓库：https://github.com/Seeed-Projects/reBot-DevArm
- 快速入门 wiki：https://wiki.seeedstudio.com/cn/rebot_b601_dm_getting_started/
- B 站组装视频：https://www.bilibili.com/video/BV1K9dbBNEdC/
- MotorBridge Web UI：https://rebot-devarm.w0x7ce.eu/
- 达妙电机 wiki：https://wiki.seeedstudio.com/cn/damiao_series/

### 本仓离线档
- 快速入门 wiki 离线版：`software/wiki_docs/reBot_Arm_B601-DM_快速入门/`
- 达妙说明书 9 章：`hardware/reBot_B601_DM/Motor_Datasheets/docs_md/`
  - 重点：`06_电机调试流程.md`（编码器校准 / Compensation Data）
  - `07_调试操作.md`（4 种工作模式调试）
  - `09_调试助手使用.md`
- 达妙系列电机 wiki：`software/wiki_docs/达妙系列电机/`
- 学习笔记里的电机踩坑：`学习笔记.md`（搜 `DMTool` / `ERR=3` / `CAN Timeout`）

### MotorBridge 上游文档（⭐ Web UI / 协议层权威来源）
- 操作手册（含夹爪零点校准标准流程）：`software/MotorBridge/docs/zh/operation_manual.md`
- WebSocket 网关协议（Web UI 背后的全部 JSON op）：`software/MotorBridge/integrations/ws_gateway/README.zh-CN.md`
- CAN 调试 / 排障：`software/MotorBridge/docs/zh/can_debugging.md`
- CLI 参考（命令行替代 Web UI）：`software/MotorBridge/docs/zh/cli.md`

### 本仓配置
- 整机 ID 配置：`software/reBotArm_control_py/config/arm.yaml`
- 夹爪配置：`software/reBotArm_control_py/config/gripper.yaml`
- URDF 模型：`software/reBotArm_control_py/urdf/reBot-DevArm_fixend_description/`

### 本仓示例代码
- 单电机 REPL：`software/reBotArm_control_py/example/1_damiao_text.py`
- 重力补偿：`software/reBotArm_control_py/example/9_gravity_compensation.py`
- MeshCat 仿真三件套：`software/reBotArm_control_py/example/sim/{fk_sim,ik_sim,traj_sim}.py`

---

## 11. 文档维护

- 看视频时把 🟡 TODO 段落对应的内容填进去
- 每次实操踩到新坑就追加到 §7 表格
- DMTool 步骤填进 §8 对照表
- 故障排查内容追加到 §9
- 内容稳定后考虑改写成"对外公开版"（语气更严谨、附实拍照片）

> 当前是骨架阶段，不强求语气和格式完美，先把事实和路径记下来。

---

## 12. 待决项

### 12.1 官方视频太长缺章节标记 🟡（2026-05-11 记录）

**痛点**（用户实操反馈）：
- 官方 B 站 [BV1K9dbBNEdC](https://www.bilibili.com/video/BV1K9dbBNEdC/) 视频时长一小时多，**纯机械装配 + 穿线**就占满一小时，节奏密集
- B 站默认不分 P、无章节标记、无可跳转的时间戳目录
- 装的时候想跳到具体步骤复看找不到，"想看一个要跳跳不到及时的页面"
- 装完整机回头查"那根线怎么走"也不知道翻到几分几秒

**待决方案**（择一或并行，等后续判断）：

| 方案 | 说明 | 工作量 |
|---|---|---|
| **A. 章节时间戳地图** | 在本文档加一节《视频章节地图》，按 "分:秒 → 内容 → 对应指南章节" 列表，让查阅者跳着看 | 低；只需看一遍视频做时间戳标记 |
| **B. 重剪视频** | 把 1 小时视频按章节剪成 5-10 个短片（每个 5-15 分钟），可发自己渠道（B站/YouTube/小红书） | 高；需要剪辑工具 + 时间投入 |
| **C. 文字+实拍图笔记** | 直接把视频步骤转写成图文教程（这份指南最终演化目标），脱离视频 | 中；需要装第二台/拆解一台时拍图 |

当前优先级未定。**先收集自己装机踩坑反馈、看看复刻第二台/做小红书时哪种产出最有价值再定**。
