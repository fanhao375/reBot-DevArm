# reBot Arm 重力补偿小白执行手册

这份文档只讲一件事：在这台 Windows 工作站上，用 WSL 跑 `9_gravity_compensation.py` 重力补偿。

结论先放前面：

- Windows 负责插 USB、转发 USB 设备、关掉 Web UI gateway。
- WSL 负责真正运行重力补偿程序。
- 不建议在 Windows 原生 Python 里跑重力补偿，因为 Pinocchio 在 Windows + Python 3.12 下安装很容易卡在源码编译。
- 第一次跑必须手扶机械臂，准备随时 `Ctrl+C` 或断 24V 电源。

## 当前本机进度（2026-05-29）

这台机器已经完成两种重力补偿真机验证：

- 无锁版本 `example/9_gravity_compensation.py` 已跑通。
- 带锁版本 `example/10_gravity_compensation_lock.py` 已跑通。
- WSL Python：`3.10.12`。
- WSL Pinocchio：`3.9.0`。
- WSL `motorbridge`：`0.3.7`，这是本次重力补偿实测版本。
- 串口使用 `/dev/ttyACM0`。

无锁版本验证时看到：

```text
[连接] OK
[使能] OK
[MIT模式] OK（kp=2, kd=1）
[控制循环] 启动 @ 500.0 Hz
tau_g = ...
```

带锁版本验证时看到：

```text
[连接] OK
[使能] OK
[目标角度] 初始锁定: ...
[MIT模式] OK（kp=7.0, kd=0.8）
LOCKED ...
```

---

## 0. 看懂两个终端

后面命令会分成两类。

Windows PowerShell 长这样：

```powershell
PS D:\Robot\reBot-DevArm>
```

WSL Ubuntu 终端长这样：

```bash
pc@xxx:~$
```

文档里：

- 标了 `powershell` 的命令，在 Windows PowerShell 里执行。
- 标了 `bash` 的命令，在 WSL Ubuntu 里执行。
- 不要把 WSL 命令粘到 PowerShell，也不要把 PowerShell 命令粘到 WSL。

---

## 1. 开跑前检查

先确认这些事情都满足：

- 机械臂已经完成 ID 烧录。
- 机械臂已经完成零点设置。
- Web UI 已经能控制机械臂动。
- 24V 电源接好。
- USB-CAN / 达妙串口板插在电脑上。
- 机械臂周围没有人手、线缆、工具会被扫到。

重力补偿要占用串口，所以先关掉 Web UI gateway：

- 如果有运行 `motorbridge-gateway ...` 的 PowerShell 窗口，按 `Ctrl+C` 停掉。
- 如果不确定有没有旧进程，执行下面命令检查。

```powershell
Get-Process ws_gateway,motorbridge-gateway -ErrorAction SilentlyContinue
```

如果有输出，停掉它：

```powershell
Get-Process ws_gateway,motorbridge-gateway -ErrorAction SilentlyContinue | Stop-Process
```

---

## 2. Windows 端：确认 WSL 和 usbipd

打开 Windows PowerShell，执行：

```powershell
wsl -l -v
```

正常会看到类似：

```text
NAME            STATE      VERSION
Ubuntu-22.04    Stopped    2
```

再检查 `usbipd`：

```powershell
usbipd --version
```

如果能显示版本号，说明工具已经有了。

---

## 3. Windows 端：找到 USB-CAN 设备

插上 USB-CAN / 达妙串口板后，在 PowerShell 执行：

```powershell
usbipd list
```

你要在 `Connected:` 下面找一个像 USB 串口、USB Serial、USB-CAN、CH340、CP210x、STM32、Damiao 之类的设备。

输出大概长这样：

```text
Connected:
BUSID  VID:PID    DEVICE                         STATE
2-1    xxxx:xxxx  USB Serial Device              Not shared
```

这里最重要的是 `BUSID`，比如上面是 `2-1`。

如果你只看到鼠标、键盘、无线网卡，看不到 USB 串口设备：

- 重新插拔 USB-CAN 板。
- 换 USB 线。
- 换 USB 口。
- 打开设备管理器，看“端口 (COM 和 LPT)”里有没有 USB 串行设备。

---

## 4. Windows 端：把 USB 设备转发给 WSL

下面假设你的 BUSID 是 `2-1`。实际执行时把 `2-1` 换成你自己的。

第一次绑定需要管理员 PowerShell：

1. 开始菜单搜索 `PowerShell`。
2. 右键选择“以管理员身份运行”。
3. 执行：

```powershell
usbipd bind --busid 2-1
```

然后 attach 到 WSL：

```powershell
usbipd attach --wsl --busid 2-1
```

再检查状态：

```powershell
usbipd list
```

正常应该看到这个设备的状态类似：

```text
Attached
```

注意：

- 电脑重启后通常要重新 `attach`。
- USB 设备 attach 给 WSL 后，Windows 这边的 COM 口可能会消失，这是正常的。
- 这时不要再启动 Windows Web UI gateway，因为串口已经交给 WSL 了。

---

## 5. 进入 WSL

在 Windows PowerShell 里执行：

```powershell
wsl
```

如果你想明确进入 Ubuntu 22.04，也可以执行：

```powershell
wsl -d Ubuntu-22.04
```

进去以后，提示符会变成类似：

```bash
pc@xxx:~$
```

如果想退出 WSL，输入：

```bash
exit
```

---

## 6. WSL 端：确认串口出现

进入 WSL 后执行：

```bash
ls -l /dev/ttyACM* /dev/ttyUSB*
```

正常会看到类似：

```text
/dev/ttyACM0
```

或者：

```text
/dev/ttyUSB0
```

如果显示找不到文件，说明 USB 还没成功 attach 到 WSL，回到第 3、4 步重新检查。

给串口权限：

```bash
sudo chmod 666 /dev/ttyACM0
```

如果你的设备是 `/dev/ttyUSB0`，就执行：

```bash
sudo chmod 666 /dev/ttyUSB0
```

---

## 7. WSL 端：确认 Python 环境

执行：

```bash
python3 --version
```

期望是 Python 3.10 左右。

检查 Pinocchio：

```bash
python3 -c "import pinocchio as pin; print(pin.__version__)"
```

期望能输出版本号，比如：

```text
3.9.0
```

检查 MotorBridge：

```bash
python3 -c "import importlib.metadata as m; print(m.version('motorbridge'))"
```

期望输出：

```text
0.3.7
```

如果 MotorBridge 不是 `0.3.7`，执行：

```bash
python3 -m pip install --user --upgrade motorbridge==0.3.7
```

---

## 8. WSL 端：进入项目目录

执行：

```bash
cd /mnt/d/Robot/reBot-DevArm/software/reBotArm_control_py
```

确认脚本存在：

```bash
ls example/9_gravity_compensation.py
```

确认配置里的通道是 Linux 串口：

```bash
grep "^channel:" config/arm.yaml
```

正常应该是：

```text
channel: /dev/ttyACM0
```

如果你的设备实际是 `/dev/ttyUSB0`，需要把 `config/arm.yaml` 里的 channel 改成：

```yaml
channel: /dev/ttyUSB0
```

大多数情况下是 `/dev/ttyACM0`，先不要乱改。

---

## 9. 正式跑重力补偿

### 9.1 无锁版本：可以轻推拖动

先摆好机械臂：

- 人站在旁边，手扶住机械臂。
- 不要把脸、手指、线缆放在机械臂运动范围内。
- 准备好 `Ctrl+C`。
- 准备好断 24V 电源。

执行：

```bash
python3 example/9_gravity_compensation.py
```

正常现象：

- 终端显示连接成功。
- 电机使能成功。
- 进入 MIT 模式。
- 终端周期性打印 `tau_g = ...`。
- 手轻推机械臂时，它不会明显下坠，会有“漂浮/托住”的感觉。

停止程序：

```text
Ctrl+C
```

按下后等它打印断开完成，不要直接关窗口。

### 9.2 带锁版本：停住时会锁当前位置

确认 9 号无锁版本稳定后，再跑 10 号带锁版本：

```bash
python3 example/10_gravity_compensation_lock.py
```

正常现象：

- 终端显示连接成功。
- 电机使能成功。
- 出现 `[目标角度] 初始锁定`。
- 进入 MIT 模式。
- 终端周期性打印 `LOCKED`。
- 机械臂会尽量保持当前位置，用力推超过阈值才会更新目标角度。

停止同样按：

```text
Ctrl+C
```

注意：带锁版本比无锁版本“硬”，不要猛推硬掰。

---

## 10. 如果一跑就不对劲

立刻处理：

- 机械臂明显下坠：手扶住，按 `Ctrl+C`，必要时断 24V。
- 机械臂抖动很大：按 `Ctrl+C`，必要时断 24V。
- 某个关节方向明显不对：按 `Ctrl+C`，不要继续试。

常见原因：

- 零点不对。
- 机械臂姿态和 URDF 模型不一致。
- 串口没有接到 WSL。
- Web UI gateway 还占着串口。
- `arm.yaml` 里的 channel 不对。

---

## 11. 常见报错

### 找不到 `/dev/ttyACM0`

现象：

```text
Could not open serial port '/dev/ttyACM0'
```

处理：

```bash
ls -l /dev/ttyACM* /dev/ttyUSB*
```

如果没有任何设备，回 Windows 重新：

```powershell
usbipd list
usbipd attach --wsl --busid 你的BUSID
```

### 权限不够

处理：

```bash
sudo chmod 666 /dev/ttyACM0
```

或者：

```bash
sudo chmod 666 /dev/ttyUSB0
```

### No module named pinocchio

说明 WSL 里没装 Pinocchio：

```bash
python3 -m pip install --user pin
```

### No module named motorbridge

说明 WSL 里没装 MotorBridge：

```bash
python3 -m pip install --user motorbridge==0.3.7
```

### Address already in use

这是 Windows Web UI gateway 的端口占用问题，不是重力补偿脚本的主要问题。

如果你还开着 gateway，关掉它：

```powershell
Get-Process ws_gateway,motorbridge-gateway -ErrorAction SilentlyContinue | Stop-Process
```

---

## 12. 每次重启电脑后怎么做

电脑重启后，一般只需要重复这些步骤：

Windows PowerShell：

```powershell
usbipd list
usbipd attach --wsl --busid 你的BUSID
wsl
```

WSL：

```bash
ls -l /dev/ttyACM* /dev/ttyUSB*
sudo chmod 666 /dev/ttyACM0
cd /mnt/d/Robot/reBot-DevArm/software/reBotArm_control_py
python3 example/9_gravity_compensation.py
```

如果设备是 `/dev/ttyUSB0`，把 `/dev/ttyACM0` 换成 `/dev/ttyUSB0`。

---

## 13. 成功以后下一步

本机已经完成：

- `9_gravity_compensation.py` 无锁重力补偿验证。
- `10_gravity_compensation_lock.py` 带锁重力补偿验证。
- LeRobot follower 校准验证。
- 本地键盘 1 度小步进 jog 工具。

后面可以进入两条路线：

1. 继续 LeRobot：决策 leader（SO-101 或 reBot 102），再做 leader 控 follower。
2. 继续传统机器人栈：研究 ROS2 / MoveIt 2。

结束 
